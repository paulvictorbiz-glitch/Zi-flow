# Handoff — last updated 2026-07-23

> Read this first when resuming. Then skim the top of CHANGELOG.md for change details,
> and the memory files for deeper context.
>
> **Machine note:** this is **Paul's new/temporary desktop** (`C:\Users\Paul\dev\footagebrain\Zi-flow`),
> set up because the original laptop died (motherboard) and its SSD (attached here as `D:`)
> is currently unreadable (access denied). This machine is meant to be temporary — see
> "Keeping this clean" below before doing a lot of work here.
> Cross-session memory: `C:\Users\Paul\.claude\projects\C--Users-Paul\memory\` (fresh on
> this machine — the prior `C:\Users\Mi\…` and `C:\Users\Leroy\…` memory paths referenced
> elsewhere are NOT accessible from here).

## TL;DR of this session
- Old laptop is dead; its SSD is attached as `D:` but **inaccessible** (permission denied, not resolved).
- Rebuilt the whole dev environment from GitHub + the live Hetzner box instead: installed git/gh/Node/ssh, cloned all 3 public repos to `C:\Users\Paul\dev\footagebrain\`, checked `Zi-flow` out to the **actually-live branch** `feat/capcut-replica-v2` (not `main`, which was 37 commits stale).
- Found and worked around a real repo bug: `.gitignore`'s `data/` rule swallows `src/data/`, so `src/data/globe.json` (a source asset, not a runtime artifact) has never shipped in a clean clone. Restored it locally from the identical dataset in `node_modules/three-globe`; **not yet fixed in the repo** (see next steps).
- Recovered SSH access to the Hetzner box from zero (old key was on the dead SSD) via Cloud Console **Reset root password** + browser Console — chose this over Rescue Mode specifically to avoid rebooting the live box. New key installed, alias `hetzner-footagebrain` works.
- Pulled the box's real backend source down (`/srv/footagebrain/footage-brain-test` — confirmed **not a git repo**) to `C:\Users\Paul\dev\footagebrain\hetzner-live\`. This proved the public **`footage-brain` GitHub repo is stale/wrong** — missing `content_forge.py`, `ig_webhook.py`, `facebook.py`, `dashboard/`, and all of `scout-backend/`.
- Rebuilt `.env.local` from the box's `.env` files + one value the user pulled from the Supabase dashboard. `npm run dev` now boots clean against the live Supabase DB.
- Full recovery checklist (what's still SSD-only vs. recovered elsewhere): `Zi-flow/SECRETS-TO-IMPORT.md`.

## Where we left off
Local dev works (`npm run dev`, port 8000, HTTP 200) — but the background dev-server process from this session is no longer running (stopped when the session ended); just re-run it. No code was written or deployed this session — this was entirely environment/access recovery. Working tree is clean except the auto-generated `api/monitor/migrations.manifest.json` (harmless, regenerated by the `prebuild` script every run).

## Open blockers
- **`D:` drive (old SSD) is unreadable** — `Get-ChildItem D:\` and `manage-bde` both fail "Access is denied" from a non-elevated session. Untried: retry from an **elevated (Run as Administrator)** PowerShell first; if still denied, `takeown`/`icacls` on the old owner SID (old machine's user was `Mi`). Nothing on the SSD was reached this session — see `SECRETS-TO-IMPORT.md` for the prioritized list of what's only there (notably: any live-but-never-committed working-tree code, per `CLAUDE.md` rule 1 about `vercel --prod` shipping uncommitted changes — the Solarin reskin is a confirmed real example of this class of risk).
- **Private repo not yet pulled.** `gh auth login` was never completed (needs an interactive browser flow the user has to run themselves). Repo name/purpose unconfirmed — likely `scout-backend`-adjacent, but that's now moot since scout-backend was recovered directly from Hetzner. Worth doing anyway in case it holds something else.
- **`.gitignore` still has the `data/`→`src/data/` bug.** Fix is known but not applied: change `data/` to `/data/` and add `!src/data/`, then commit `src/data/globe.json`. Until this lands, every future fresh clone will hit the same `npm run build` failure this session hit.
- **Ambiguous GCP service account for the Monitor tab's billing widget.** Two service-account JSONs exist on Hetzner for other purposes (OpenCut integration, Vertex Gemini); the deploy docs reference a *third*, `ssh-to-out-network@footage-brain-database.iam.gserviceaccount.com`, specifically for `roles/billing.viewer`. `.env.local` currently has the OpenCut one as a placeholder for `GOOGLE_SERVICE_ACCOUNT_JSON` — verify the Monitor tab's billing card before trusting it.
- **A handful of secrets exist only in Vercel, not Hetzner or GitHub:** `RESEND_API_KEY`, `VITE_GOOGLE_MAPS_API_KEY`, `SUGGEST_CRON_SECRET`, `PLANABLE_API_TOKEN`, `EPIDEMIC_TOKEN`. App runs without them; those specific features (team emails, Locations-tab map, Planable, Epidemic) won't work until `vercel login && vercel link && vercel env pull` is done.

## Pending (written but not yet live)
- Nothing code-wise. Purely environment: `.env.local` is complete enough to develop locally but has 5 `TODO_vercel_env_pull` placeholders (see above).
- Security hygiene follow-ups from this session, none urgent: rotate the Hetzner root password once more (it was visible in chat during recovery), consider rotating the GitHub PAT and Facebook app secret (both got displayed in full when reading the recovered `.env` files), and consider making the 3 public repos private / rotating the `WHATSAPP_VERIFY_TOKEN` — `backend-handoff/DEPLOY-CHECKLIST.md` has had a few real (low-severity) values sitting in the public repo (WhatsApp verify token, TikTok sec_uid, GCP billing account ID) since it was written.

## Next session — start here
1. **Fix the `.gitignore` bug for real** (`data/` → `/data/` + `!src/data/`, commit `src/data/globe.json`) so this stops being a recurring landmine for every fresh clone (including whenever the original laptop comes back).
2. **Retry `D:` from an elevated PowerShell.** If it opens, work through `SECRETS-TO-IMPORT.md` top-down (working tree/uncommitted code first, then the `.claude` memory folder, then credentials) — the working tree is the highest-value unknown, since live-but-uncommitted code is a known real risk on this project.
3. `vercel login` (browser) then `vercel env pull` to fill the remaining 5 `.env.local` placeholders.
4. Whenever ready to move back to the original laptop: see "Keeping this clean" below.

## Keeping this clean across machines
This machine is explicitly temporary. To avoid the same recovery scramble next time:
- **Before switching back to the original laptop:** commit or stash everything in each of the 3 repos here (`git status` in each of `Zi-flow`, `footage-brain`, `Content-desk-` under `C:\Users\Paul\dev\footagebrain\`), push anything commit-worthy, and copy `.env.local` + `hetzner-live/` back onto the USB/SSD explicitly — don't rely on git alone, given the confirmed history of live-but-uncommitted deploys on this project.
- **Do not delete this machine's copy** until the original laptop has pulled latest and you've confirmed `npm run build` passes there too.
- Prefer fixing gitignore/repo-structure bugs (like the one above) over one-off local workarounds, specifically so they don't need re-discovering on the next machine.

## Verification (read-only checks to confirm current state on resume)
```powershell
# Repos + branch
cd C:\Users\Paul\dev\footagebrain\Zi-flow ; git status --short ; git rev-parse --abbrev-ref HEAD
# Should show: feat/capcut-replica-v2

# .env.local has real values, not placeholders
Select-String -Path .env.local -Pattern 'TODO_' | Measure-Object   # count of still-missing values

# Dev server boots
npm run dev
# then: curl http://localhost:8000  (expect HTTP 200)

# Hetzner SSH still works
ssh hetzner-footagebrain "echo OK && docker ps --format '{{.Names}}' | wc -l"
# expect: OK, then 11 (running containers)
```
