node_modules/
dist/

# Claude Code local state — not for the shared repo
.claude/

# Local secrets — keep these off any shared repo
.env
.env.local
.env.*.local

.vercel

# Obsidian vault junction (live link into C:\Users\Mi\Documents\FootageBrain Obsidian) — read-only context, not repo content
obsidian-vault/

# FootageBrain runtime artifacts that landed loose in this folder — never commit
data/
footage_brain.db
footage_brain.db-shm
footage_brain.db-wal
machine_id.json

# Google Cloud service account keys — never commit
api/footage-brain-sa.json
tools/credentials/
*.json.key
# Service-account JSON dropped in the repo root (project_id-keyid.json pattern)
footage-brain-database-*.json

# Python bytecode cache (e.g. backend-handoff/ webhook drafts)
__pycache__/
*.pyc

# Playwright saved auth session (never commit)
auth.json
screenshots/

# Instagram / yt-dlp cookies — contain live session tokens, never commit
ig_cookies.txt
cookies.txt
*.cookies.txt
