# FootageBrain / Workflow — Feature Change Log

Living inventory of every feature and change built. Grouped by pillar, newest first within each section.

**Legend:** `[LIVE]` = deployed to prod · `[STAGED]` = built, not yet deployed · `[LOCAL]` = uncommitted / in progress · `[DEFERRED]` = parked until revisited  
**Counts:** ~66 commits · 90 DB migration files (**1 pending as of 2026-06-22**: **`0086_perf_samples` written + SHIPPED in code (`2a7cd95`) but NOT yet applied** — human-gated, lights up the WS4 Monitor perf card; **`0087_planable_pushes` written + APPLIED 2026-06-21** (single-file `exec_sql` apply — Push-to-Planable audit/idempotency table); 0078_training_quiz_attempts + 0082_edit_collab + 0083_render_jobs + 0084_daily_tasks_color all **applied**; 0077 / 0079 (backend live) / 0081 (Phase 1 LIVE) applied; **`0080_perf_indexes` written + APPLIED 2026-06-21** (the gap slot — earlier-noted `0080_reel_group_id` never existed); **`0088_planable_media` written + APPLIED 2026-06-21** (single-file `exec_sql` — private `reel-videos` bucket + storage RLS + `planable_pushes` media cols); 0085 reserved for changedetect bridge; 0076 §1/§3/§4 applied; 0049 pending+deferred; **`0089_reel_dna_row_color_favorite` APPLIED 2026-06-22**; **`0090_planable_pushes_grouping` written + APPLIED 2026-06-22** (single-file `exec_sql` — additive grouping cols `campaign_id`/`group_id`/`page_ids`/`batch_id` on `planable_pushes`); **`0092_music_tracks` + `0093_music_library` written 2026-06-23, NOT applied** (Epidemic Music Library — 0092 also EXTENDS the `reel_dna_assets` `asset_type` CHECK to allow `'music'`; 0093 = per-user favorites/playlists); **`0094_edit_collab_v2` written + APPLIED 2026-06-23** (single-file `exec_sql` apply, NOT bulk; OpenCut collab — opens RLS on `edit_projects`/`edit_project_versions`/`render_jobs` owner→authenticated + adds `edit_projects` cols `reel_id` TEXT/`thumbnail_url`/`last_editor`/`locked_*` + new `edit_ai_jobs` + `edit_sessions`→`edit_projects` backfill; fully additive. **Apply-blocking fix:** backfill ordered by `edit_sessions.updated_at` which doesn't exist on the manually-created live table → `last_active`)); **`0095_oc_projects` + `0096_oc_locks` written + APPLIED 2026-06-24** (session i — applied together via a scoped one-off `scripts/apply-oc-collab-migrations.mjs`, NOT migrate:apply; owner-authorized. 0095 = shared `oc_projects` table for the embedded OpenCut fork's timeline docs; 0096 = `oc_locks` single-writer lock table for Phase-2 collab — `project_id` uuid PK with **NO FK** to oc_projects so Take-control works on an unsaved project; both team-wide `auth.role()` RLS + realtime) · 7 feature pillars
**Branches (2026-06-19):** `clean/prod-baseline-2026-06-19` = production (current working branch) · `wip/parking-2026-06-19` = the parked 67-file multi-thread WIP · `main`/`origin/main` = `6fa7204` (baseline-minus-the-6-live-files until owner ff's main forward)

---

## How to Use This File

- Scan by pillar to see what's built in a given area
- Use status badges to know what's actually on prod vs. local
- When starting a new feature, add a `[LOCAL]` entry here first, then update to `[LIVE]` after deploy
- Detailed session narratives + lessons learned live in `CHANGELOG.md`
- Current work state lives in `HANDOFF.md`

---

## 1. Gamification & Training

| Date | Feature | Key Files / Migration | Status |
|------|---------|----------------------|--------|
| 2026-06-19 | **Training — in-module slide carousel ("lesson player")** — de-cluttered the ~3,005px module wall: each pillar's sections are grouped into **5 chapters** (Learn/Standards/Practice/Recall/Track) shown one at a time with a stepper rail, smooth slide + animated height, Back/dots/Next + arrow-keys/swipe (expanded module ~400px). Panels stay mounted so quiz/flashcard/checklist state survives nav. Also fixed the global `+` FAB overlapping the Next button. Verified live via Playwright | `src/components/training/ModuleChapters.jsx` (new), `training-blocks.css`, `src/pages/training.jsx` | `[LOCAL]` |
| 2026-06-19 | **Training — interactive quizzes + flashcards (active-learning MVP)** — every pillar module gets an MCQ+TF **self-check quiz** (per-question reveal + explanation + score, replaces the static self-assessment) and a CSS-flip **flashcard deck**; owner authors both inline via the `training_module_content` `::data` JSON channel (no new table); per-editor best-score badge persisted | `src/components/training/{Quiz,QuizEditor,FlashcardDeck}.jsx` (new), `training.jsx`, `training-curriculum.jsx`, migration `0078_training_quiz_attempts.sql` | `[LOCAL]` |
| 2026-06-18 | **Training — YouTube Embed toggle now PERSISTS** — clicking a YouTube link's **Embed** in the manual used to revert on click-away (local `useState`); the embed-shown state is now saved per (module, field) as a `"<fieldPath>::embed"` JSON-array sibling row in `training_module_content` (no migration), so it survives refresh and shows for every editor. `linkifyText`/`EditableText` gained optional `embeddedUrls`/`onToggleEmbed`; threaded via `getEmbedProps()` | `src/lib/linkify.jsx`, `src/components/EditableText.jsx`, `src/pages/training.jsx` | `[LIVE]` |
| 2026-06-18 | **"Edit the training manual" permission** — new `editManual` action cap in Roles & permissions → Actions (default Off for editor roles = read-only; owner grants per-person). Training `canEdit` now reads `can("editManual")` instead of hard-coded owner check; row auto-renders in the admin matrix | `src/lib/permissions-catalog.js`, `src/pages/training.jsx` | `[LIVE]` |
| 2026-06-18 | **Training — clickable links + YouTube embed EVERYWHERE** — URLs in **all** training module sections (not just the 3 prose blocks) render as clickable links, incl. in the **owner's** editable view; YouTube URLs get an inline **Embed** button → small **260px right-aligned** 16:9 player. Extracted to shared `linkify.jsx` + opt-in `linkify` prop on `EditableText` (applied to read-only AND editable-not-editing branches; owner is always `canEdit`); link/embed clicks `stopPropagation` so they don't trigger edit | `src/lib/linkify.jsx` (new), `src/components/EditableText.jsx`, `src/pages/training.jsx` | `[LIVE]` |
| 2026-06-17 | **Per-reel rubric archive + removed redundant self-assess toggle** — Archiving a rubric sub-skill row is now scoped per-reel (was global across all reels); removed the duplicate `selfAssessRubric` toggle from the Roles matrix (kept in DEMO_ACTIONS), Monitor Gamify card is the single self-assess control | `GamifyRubricSheet.jsx`, `store.jsx`, `permissions-catalog.js` | `[LIVE]` |
| 2026-06-16 | **Gold-standard rubric revamp + sheet UX** — Rebuilt rubric around a canonical quality standard; row-by-row grading sheet with UX fixes (collapse, scroll, submit) | `GamifyRubricSheet.jsx`, migration `0053_rubric_revamp.sql` | `[LIVE]` |
| 2026-06-16 | **Grading authority follows real role** — Rubric grading permission derived from the user's actual DB role, not the previewed perspective | `src/pages/detail.jsx`, `src/components/GamifyRubricSheet.jsx` | `[LIVE]` |
| 2026-06-16 | **Skill XP, spider charts, rubric grading, editor lock** — Full gamify system: XP per skill, radar/spider chart, per-row rubric grading, editor-role lock on grading panel | `SpiderChart.jsx`, `GamifyPanel.jsx`, `GamifyWelcomePopup.jsx`, `MedalBadge.jsx`, `gamify.css`, migrations `0050–0052` | `[LIVE]` |
| 2026-06-16 | **Training pillar modules** — Structured training curriculum with module content, progress tracking, per-user completion state | `training.jsx`, `training.css`, `training-curriculum.jsx`, `TrainingProgressWidget.jsx`, migrations `0047_training_progress.sql`, `0055_training_module_content.sql` | `[LIVE]` |
| 2026-06-08 | **Reel skill tags** — Tag reels with skill categories for XP attribution | migration `0048_reel_skill_tags.sql` | `[LIVE]` |
| 2026-06-08 | **Demo sandbox** — Isolated demo mode for onboarding/testing without affecting live data. **⚠ DB migration `0049` was marked-applied but NEVER actually ran** (discovered 2026-06-19 audit — `is_demo_user()`/demo columns absent); now `[pending]` + guarded `DO-NOT-BULK-APPLY`. **DEFERRED** until owner revisits the project | `src/lib/demo-sandbox.jsx`, migration `0049_demo_sandbox.sql` | `[DEFERRED]` (migration not applied) |

---

## 2. Public Marketing Site

| Date | Feature | Key Files / Migration | Status |
|------|---------|----------------------|--------|
| 2026-06-22 | **Payment Infrastructure Plan — Stripe one-time credits for external customers** — full architecture documented: landing pricing section → `billing-checkout.js` → Stripe Hosted Checkout → `billing-webhook.js` → `0091_customer_credits.sql` (`customers` table + atomic `increment_customer_credits` RPC); Vercel Pro upgrade required (12/12 cap); existing `credits-modal.jsx` Pay button wired in Phase 3 | `obsidian-vault/05 - Roadmap/Payment Infrastructure Plan.md` (new), `BUILD LIST.md` (updated) | `[LOCAL]` |
| 2026-06-22 | **PAW avatar integration research + future product requirements documented** — assessed [programasweights/programasweights-python](https://github.com/programasweights/programasweights-python) (169 KB SDK, llama.cpp, 5–22 MB compiled programs) for controlling the `/space` astronaut avatar. Browser WASM path recommended (zero Hetzner cost, GPT-2 ~5 MB). Documented: pricing plans editable from admin panel (`pricing_plans` Supabase table + `user_plan` join), mandatory sign-up gate before purchases, public create-account `/signup` route. Plan: `.claude/plans/how-much-bandwitdh-does-cryptic-twilight.md` | `.claude/plans/how-much-bandwitdh-does-cryptic-twilight.md` (new) | `[LOCAL]` |
| 2026-06-20 | **/space — astronaut + gravitational-lens warp + gaseous nebula + Scene-Studio accordion** (owner-only) — floating astronaut (visor loads `public/astronaut-face.jpg`, fallback glass), subtle 2D Einstein-lens that forms on the cube→Sun axis + **warps the camera over the Sun** (click/scroll), multi-hue **gaseous** nebula (no dots, 4 depth planes), cube **expand-sooner + orbit/pan unfrozen**, 360° body spread, removed 3 ringed planets; Scene-Studio is now an **accordion** w/ Lens + Astronaut as tunable bodies + **click-a-3D-body-to-open**. Built in worktree `feat/space-enhance`, rebased clean onto `main` (no `styles.css` conflict) | `src/components/space/{GravLens,Astronaut}.jsx` (new), `Galaxy.jsx`, `Nebula.jsx`, `SpaceControls.jsx`, `space-scene-params.jsx`, `space3d.{jsx,css}` | `[LIVE]` (`dpl_3aHZ…`, `390edfa`) |
| 2026-06-19 | **/space Scene Studio + per-body audio** (owner-only) — slide-in customization sidebar: Global (auto-rotate/bloom/master-vol/mute) + a tab per celestial body with live sliders for speed, lighting, **angle/direction (az·el·dist)**, colour, texture-detail, size, visible, **sound+volume**; per-body procedural Web Audio (no asset files, muted by default); persists to `localStorage.s3d_scene`; + more 360° ambient fill | `src/lib/space-scene-params.jsx`, `src/components/space/{SpaceControls.jsx,space-audio.js}`, `space3d.jsx`, `space3d.css`, all `space/*` bodies | `[LIVE]` (shipped 2026-06-20) |
| 2026-06-19 | **/space hyper-real fidelity** (owner-only) — procedural shaders/textures: Sun (granulation+sunspots+limb+prominences+corona), banded/rocky **textured planets** + atmospheres, gaseous-plasma neutron-star beams, doppler-beamed **black-hole accretion-disk shader**, **volumetric fbm nebula**, panelled fleet hull + glowing windows | `src/components/space/celestial-shared.js` (GLSL_NOISE/fresnel/plasma/disk/nebula/textures), `Sun.jsx`, `NeutronStar.jsx`, `BinaryBlackHole.jsx`, `Nebula.jsx`, `SpaceBattle.jsx` | `[LIVE]` (shipped 2026-06-20) |
| 2026-06-19 | **/space hyper-real overhaul** (owner-only) — looping **binary black-hole merger** (inspiral→merge→ringdown→GW ripple), detailed **pulsar**, **battle fleet** (capital ship + fighters + pooled projectiles/explosions), **360° ambient events** (galaxies/comets/supernova/ringed-planet/shooting-stars), real **post-processing bloom** + opaque **skydome** (fixes alpha-canvas bloom artifacts), quality tiers | `src/components/space/{BinaryBlackHole,AmbientEvents,Skydome}.jsx`, `Galaxy.jsx`, `space3d.jsx`, `space-cube-config.jsx`; deps `@react-three/postprocessing@2.19.1`+`postprocessing@6.37.0` | `[LOCAL]` |
| 2026-06-17 | **3D spinning Reel-DNA helix (landing)** — landing breakdown now renders the real R3F `DnaHelix`: continuous tube strands, ACTG base-pair crossbars, slow-on-hover spin, "mitochondria cell" backdrop, + a 3D/Classic toggle; lazy-loaded so three.js stays off the initial bundle | `dna-helix.jsx`, `landing.jsx`, `landing.css` | `[LIVE]` |
| 2026-06-17 | **Leroy title → Co-Founder & CTO** — Team section on the public marketing site now lists Leroy Crosby as "Co-Founder & CTO" (was "Creative Director") | `src/lib/site-content.jsx` | `[LIVE]` |
| 2026-06-17 | **/space cinematic scene expansion** (owner-only) — 3D homepage became an explorable star system: per-face topic names, empty slot boxes, metallic gold/silver/bronze cubes (RoundedBox + baked Environment reflections), continuous-zoom camera (free orbit → drag-rotate → stacked columns), western nebula, sun + planets + real light, neutron star w/ jets, space-battle vignette | `Galaxy.jsx`, `Nebula.jsx`, `Sun.jsx`, `NeutronStar.jsx`, `SpaceBattle.jsx`, `celestial-shared.js`, `RubikCube.jsx`, `space3d.jsx`, `space-cube-config.jsx` | `[LIVE]` |
| 2026-06-17 | **/space 3D galaxy backdrop** (owner-only) — In-Canvas Milky-Way behind the cube: thousands of twinkling distant stars, Sgr A* black hole + accretion disk, drifting asteroids | `src/components/space/Galaxy.jsx` | `[LIVE]` |
| 2026-06-15 | **Landing batch v2** — Added studio showcase, credits modal, repositioned timeline, updated logo, founders section, platform showcase card; fixed analytics chart crash | `content-studio.jsx`, `credits-modal.jsx`, `platform-showcase.jsx`, `landing.jsx`, `landing.css` | `[LIVE]` |
| 2026-06-15 | **Reel DNA landing + Product/About/Team pages** — Full public marketing site at `/`: 3D DNA helix (R3F), co-visible timeline, product page, about page, team section | `landing.jsx`, `dna-helix.jsx`, `helix-flat.jsx`, `timeline-view.jsx`, `asset-fan.jsx`, `about-page.jsx`, `product-page.jsx`, `team-section.jsx`, `reel-dna-view.jsx` | `[LIVE]` |

---

## 3. Monitor & Infrastructure

| Date | Feature | Key Files / Migration | Status |
|------|---------|----------------------|--------|
| 2026-06-23 | **Second-contributor onboarding — in-repo collab docs + repo hygiene** — set up two-person PR-based collaboration (friend on own machine + own Claude Code account; owner stays sole deployer/migrator/Hetzner). Committed `docs/COLLAB-NOTES.md` (rules: no deploy/migrate/SSH, `npm run dev`=LIVE DB, branch-per-feature, hot-file map, migration coordination, RLS trap, PR checklist) + `docs/FRIEND-TASKS.md` (6 known bugs w/ root-cause `file:line` + backlog) to `main`, pushed. Pruned 9 merged local branches (kept `clean/prod-baseline-2026-06-19` + `feat/space-enhance`). Plan: `.claude/plans/i-have-my-friend-validated-coral.md`. Pending owner GitHub UI: invite collaborator + arm `main` ruleset. | `docs/COLLAB-NOTES.md`, `docs/FRIEND-TASKS.md` | `[LIVE]` (`8c45f5a` on `origin/main`) |
| 2026-06-23 | **Exhaustive multi-account bug-audit workflow (built, not run)** — documentation-only diagnostic: real Playwright logins for owner + 4 non-owners on production, walk all 20 tabs per account, diff each non-owner vs the owner baseline, capture console/network errors, static-code root-cause each finding, adversarial-QA verify, write one ranked `BUG-AUDIT-REPORT.md`. Built to reproduce 6 reported cross-account bugs (final-video button `detail.jsx:941`, monitor empty `store.jsx:2003`, Scout Headers TypeError, switcher `app.jsx:498`, one-letter names, role-labels-on-name) + an off-roster `amerejr` account. Key insight: perspective-switching masks per-account data bugs (data layer keys off the real login) → real logins required. All artifacts gitignored. | `.claude/workflows/exhaustive-bug-audit.js`, `diagnostics/accounts.local.json`, `.gitignore` | `[LOCAL]` (gitignored, not executed) |
| 2026-06-23 | **Reel DNA Analyze — all 3 gaps fixed (workflow confirmed, committed `2ad08ee`)** — launched `.claude/workflows/analyze-button-fix.js`; 10-agent workflow QA verdict CONFIRMED. Fixes: (1) 5 new CSS rules in `unified-dna-card.css` (`.udc-reel-deconstruct`/`.udc-reel-failed*`/`.udc-reel-retry`); (2) `analyze_failed` now shows retry button (old `acquireFailed`-only gate removed, both types covered + 120-char error truncation + null fallback); (3) stale-cookie hint in `_acquire_reel_video` fires when cookies ARE configured but download fails. Build green (8.97s). **Needs deploy** — 6 human steps: SCP fresh YT cookies → verify → scp-down+diff+merge `reel_deconstruct.py` → Hetzner build → test → `vercel --prod`. | `src/components/unified-dna-card.css`, `src/components/unified-dna-card.jsx`, `backend-handoff/reel_deconstruct.py` | `[STAGED]` (committed `2ad08ee`, not deployed) |
| 2026-06-21 | **Lean-FootageBrain — one-site lean overhaul (WS1–WS4)** — the dashboard now opens lean on the core workflow without a second site. **WS1** gates secondary/heavy tabs off editable roles' DEFAULT nav (`LEAN_HIDDEN`: editor, lossless, export, analytics, inbox, locations, coverage, generate; Resources stays; owner unaffected, re-enableable per-role/person in admin). **WS2** `React.lazy` code-splits the heavy pages (old single 1.3 MB `index` → many lazy chunks, `monitor-hub` 110 kB) + owner-only "Prefetch heavy tabs" toggle. **WS3** role-gates the store's secondary fetches + 2 realtime channels behind `isOwner`. **WS4** `web-vitals` perf telemetry (one keepalive sample/session, silent-degrade) → owner Monitor perf card. Built via the 4-team `lean-footagebrain` workflow. | `src/lib/permissions-catalog.js`, `src/app.jsx`, `src/components/PreferencesModal.jsx`, `vite.config.js`, `src/store/store.jsx`, `src/lib/perf-tracker.js` (new), `src/main.jsx`, `src/pages/monitor-hub.jsx`, migration `0086_perf_samples.sql` | `[LIVE]` (`2a7cd95` / `dpl_8iNqY7QVDyVxcqx7NC2rx3zzwWH5`; **0086 apply pending**) |
| 2026-06-21 | **Scale for ~15 concurrent users — store perf overhaul** — the dashboard (built for 4) lagged/broke at ~15 simultaneous users. Reworked `store.jsx`: windowed/limited the unbounded `select("*")` boot queries on the big growers, made boot **progressive** (core board renders before secondary tables hydrate, vs the old all-or-nothing gate), **trimmed the chatty unfiltered realtime** subscriptions that fanned every poller write to all clients, and **narrowed the whole-state context memo**. Backward-compatible (no consumer file touched). Built via the 2-team `scale-15-users-perf` workflow; deployed isolated. | `src/store/store.jsx`, migration `0080_perf_indexes.sql` | `[LIVE]` (`5871e05` / `dpl_2ktBndNTNB2fpDZZQHbk2zAxRNRs`; 0080 applied) |
| 2026-06-21 | **Scout — live OpenRouter quota card (Monitor → Infra)** — new **Scout** card answers "how many free pulls/day-month" with LIVE data: auto-detects tier (Free 50/day vs Credited 1,000/day) from OpenRouter `is_free_tier`, shows credits used/balance, dossiers today vs live cap, + DB usage (dossiers/mo, products, new this week, last scrape) + source caps (OpenRouter 20/min·failed-count, HN ~unlimited, GitHub 30/min, PH 6,250 pts/15-min). New secret-gated `GET /quota` on `fb-scout` (→ OpenRouter `/api/v1/key`+`/credits`), owner-gated `suggest.js?action=scout-quota` proxy. **Gotcha:** OpenRouter `rate_limit` field is deprecated (`requests:-1`) — guard `>0`, fall back to documented 20/min. Verified live `is_free_tier:true`→50/day. | `microsaas-scout app/main.py`, `api/ai/suggest.js`, `src/pages/monitor.jsx` | `[LIVE]` (`dpl_…2f6rijml5`) |
| 2026-06-21 | **Scout — `non ISO-8859-1 code point` Headers error closed (all 3 layers)** — fixed across Node (`asciiHeader()` guard on `x-scout-secret`), Python (`ascii_clean()`+`_scrub_settings()` in `config.py`, token scrub in `base.py:_env()` — covers outbound `Authorization` tokens the prior fix never touched), and UI (`scout.jsx` Refresh surfaces the real server error vs a false "started"). Proved browser-side-clean via Playwright (`scripts/scout-inspect.mjs`: Scout loads 388 cards, Supabase 200). Key fact: only header *values* throw this, never JSON bodies. | `api/ai/suggest.js`, `src/pages/scout.jsx`, `microsaas-scout app/{config.py,scrapers/base.py}` | `[LIVE]` (`dpl_7zwE91UX…`) |
| 2026-06-20 | **Reel DNA — yt-dlp cookies activated on Hetzner** — dropped `ig_cookies.txt` (Netscape, from incognito YouTube session) onto `deploy/hetzner/data/cookies/ig_cookies.txt`, chmod 600; `/api/reel/status` → `cookies_set:true`. Cookie auto-acquire now live for all 3 yt-dlp calls (longform + reel audio + reel captions). Re-export + re-SCP when cookies rotate (no restart needed). | `deploy/hetzner/data/cookies/ig_cookies.txt` (file drop only) | `[LIVE]` |
| 2026-06-20 | **Team Chat — Compare panel** — New `ReelComparePanel` component in `team-chat.jsx` (sits above RC iframe): Step 1 pick an inspiration reel (autocomplete), Step 2 attach a local screen recording (blob URL), Step 3 post structured RC message to a named channel with deep-link `?reel=X&compare=1` so teammates auto-open the compare modal | `src/pages/team-chat.jsx` | `[LIVE]` (`4f82931`) |
| 2026-06-21 | **MicroSaaS Scout — integrated as Monitor hub "Scout" tab** — owner-only sub-tab in Monitor hub reads Scout Supabase (rqkzstyvqfmcsxdyogij) via a 2nd anon client (`scout-supabase.js`); browse/filter 97+ PH/HN/GitHub products + AI dossiers (score, source badge, full dossier expand); Refresh button proxies `POST /scout/scrape-all` via `suggest.js?action=scout-scrape` (owner-gated, SCRAPE_SECRET server-side). Scout backend (`fb-scout`) deployed to Hetzner behind Caddy `/scout/*` route; daily 06:00 UTC cron. Committed `79ab93e`, deployed `dpl_GBFPmtjq7afctFBUqE2bGTifXzt3`. | `src/lib/scout-supabase.js` (new), `src/pages/{scout.jsx,scout.css}` (new), `monitor-hub.jsx`, `permissions-catalog.js`, `api/ai/suggest.js` | `[LIVE]` |
| 2026-06-20 | **MicroSaaS Scout — standalone product built** — separate repo + own Supabase (rqkzstyvqfmcsxdyogij), scrapes PH+HN+GitHub → AI "opportunity dossier" (0–100 fork/rebuild score). Python/FastAPI+React(Vite). Built via `.claude/workflows/microsaas-scout-build.js` (4-team generated workflow). 97 products/97 dossiers live. | `C:/microsaas-scout` (separate repo) | `[LIVE]` (standalone) |
| 2026-06-19 | **💬 Reel-share picker auto-defaults to the OPEN Rocket.Chat channel** — the Team-chat "Share a reel into chat" dropdown now defaults to whichever RC channel you currently have open in the embedded chat and follows it live as you switch channels (was always `#pipeline`, manual every time); a manual pick still wins until the next successful send. Chat is a cross-origin iframe → uses RC's **iframe-integration "send" API**: with `Iframe_Integration_send_enable` ON (owner enabled this session), RC postMessages a `room-opened` event; `TeamChat` runs an origin-gated `message` listener (real channels/groups only — DMs/omnichannel ignored), tracks `openRoom`, feeds `ReelSharePicker` (auto-follow effect + `userOverride` flag + `channelOptions` merge). Degrades silently to the old default if the toggle is off. Built via `/qa-verified-plan` (plan `.claude/plans/moonlit-wiggling-leaf.md`). **Deployed in ISOLATION** off branch HEAD (`reorg/tabs-monitor-roles` + only this file) → `dpl_HZKJvncgi2Uj6e9anyiBuSC86GW5` (READY, www.footagebrain.com); the committed nav-reorg rode along live, none of the ~58 dirty WIP files shipped | `src/pages/team-chat.jsx` | `[LIVE]` (uncommitted) |
| 2026-06-19 | **🗂️ Owner Monitor hub — consolidate 3 dashboards into 1 tab (reorg Part A)** — The three separate owner-only tabs (infra **Monitor**, **Pulse**, **AI Brain**) are now ONE **Monitor** tab with **Infra / Pulse / AI Brain** sub-tabs (Pipeline sub-mode-bar pattern). New `monitor-hub.jsx` mounts the existing pages as-is; each sub-tab independently gated by `canView()`, hub visible if ANY of the 3 granted (`canViewView()` helper), lands on first allowed; `wb_view` aliases pulse/ai→monitor. Gating provably unchanged (per-role smoke). **Deployed via isolated worktree → www.footagebrain.com** (`dpl_BHgunVWZsBTAMhdZzMgh57w2v7tm`); on `origin/main` `6fa7204` | `src/pages/monitor-hub.jsx` (new), `src/app.jsx` | `[LIVE]` |
| 2026-06-19 | **🧪 Per-role tab-gating smoke gate (reorg Part 0)** — Extended `scripts/smoke-screenshot.mjs` from an ad-hoc screenshotter into a per-role regression gate: boots as owner, walks each perspective via the switcher, asserts owner sees Monitor+Analytics / non-owner sees neither owner surface but keeps Analytics / no page errors. Gated every reorg part + the prod deploy. On `origin/main` (`80b1330`→`6fa7204`) | `scripts/smoke-screenshot.mjs` | `[LIVE]` |
| 2026-06-19 | **🧹 Dirty-tree cleanup → clean prod-baseline branch (QA-verified)** — Resolved the 67-file multi-session dirty tree with zero loss via `/qa-verified-plan` (3 read-only verifiers + cross-check): parked everything to `wip/parking-2026-06-19`, cut `clean/prod-baseline-2026-06-19` off `main` = `main` + the 6 live-but-uncommitted files (inbox-AI cherry-pick `75c44c6` + reel-share picker + DNA-overlay scroll fix) + applied-live migs 0077/0079 + regen manifest; build green (822 modules). QA caught 2 pre-mutation errors (live-only set was 6 files not 2; junction-delete hazard on comparison worktrees). Removed all extra worktrees. **The tree was never broken — World Monitor/bug-batch/IG-reconcile already committed in `main`; resurrecting a parked feature is a cherry-pick, not a rebuild.** | branches `clean/prod-baseline-2026-06-19` + `wip/parking-2026-06-19` | `[LOCAL]` |
| 2026-06-19 | **🧪 Playwright smoke harness (first browser automation)** — Added `@playwright/test` + Chromium (project had none) + `scripts/smoke-screenshot.mjs`: loads a saved `auth.json` session, opens `/app`, waits out the store-hydrate splash, screenshots a tab + reports console/page errors. Verified the **Reel DNA** tab renders healthy (capture form, Reels/Thumbnails sub-tabs, IG Sync Health, ~28-row spreadsheet, no page errors). Closes the restructure audit's "highest-leverage missing gate." Follow-up: extend to a per-role multi-tab smoke | `scripts/smoke-screenshot.mjs`, `package.json` (`@playwright/test` devDep), `.gitignore` | `[LOCAL]` (uncommitted) |
| 2026-06-19 | **🧪 First Playwright smoke harness + Reel DNA visual verify** — installed `@playwright/test` + Chromium (project had no browser automation) and added `scripts/smoke-screenshot.mjs`: loads a saved Supabase session (`auth.json`, gitignored), opens the authed dashboard at `/app`, waits out the "LOADING WORKFLOW…" hydrate, screenshots Reel DNA, reports console/page errors. **Result: Reel DNA renders healthy — capture form, Reels/Thumbnails sub-tabs, IG Sync Health, ~28-row spreadsheet; zero uncaught page errors.** Seed of the smoke gate the restructure audit wanted. Gotchas: `/app` not `/`, `networkidle` never fires, auth via `codegen --save-storage`. Minor finding: Reel DNA bookmarklet `<a href="javascript:…">` (`reel-dna.jsx:1197`) → React future-block warning, non-breaking. No commit/deploy. | `scripts/smoke-screenshot.mjs`, `package.json`, `.gitignore` | `[LOCAL]` |
| 2026-06-19 | **🧹 Repo hygiene + restructure-readiness audit** — Pruned all 10 stale `.claude/worktrees/agent-*` worktrees (`git worktree remove --force` ×10 + `prune`) + deleted their 10 `worktree-agent-*` branches (old scratch on commits `2c29b06`/`e3a345c`, far behind main; superseded migration edits). Ran a 4-subagent codebase-restructure readiness audit → prioritized "critical things to note" inventory (key flags: `refactor/folder-structure` branch is a TRAP — 61 behind, would delete 0068–0076; zero tests, build ≠ done-signal; add `@/` alias before any moves; `store.jsx` god-module; migration paths coupled in 3 scripts + manifest; Vercel 12/12 fn cap). No code/DB/deploy. | `.claude/worktrees/`, git branches; audit → `obsidian-vault/05 - Roadmap/Session Log.md` | `[LIVE]` (worktrees pruned; audit documented) |
| 2026-06-19 | **🔴→✅ RLS recursion incident — `people` policy took the site down, fixed** — 0076's `"owner manage people"` policy was ON `public.people` yet queried `public.people` in its USING/CHECK → `infinite recursion detected in policy for relation people` on every authenticated read → IG analyze (and all `people` reads) broke. Fixed live via a `SECURITY DEFINER public.auth_is_owner()` helper (runs as table owner → bypasses RLS); policy now calls it. Verified under a real authed session (`7 rows, no recursion`). **An RLS policy must never directly query its own table.** | `supabase/migrations/0076_rls_hardening.sql`, live DB | `[LIVE]` (committed `238759b`) |
| 2026-06-19 | **RLS hardening — back UI permission gating with DB enforcement (§1/§3/§4 applied; §2 deferred)** — Migration `0076` closes the keystone hole: `people` let any authenticated user INSERT `role='owner'` → fixed via a `BEFORE UPDATE` trigger pinning `role`/`id` (an RLS `WITH CHECK` role-guard is a no-op — only sees the NEW row; service_role exempt) + no authenticated INSERT. Plus `attached_footage_items` `using(true)`→authenticated and `reel_dna` writes→owner-or-`captured_by`-self (poller's service_role path preserved). **§2 owner-only DELETE deferred** — it assumed `0049_demo_sandbox` (which was marked-applied but never ran), so it needs rewriting against the live `"auth write"` policies. Applied + verified live | `supabase/migrations/0076_rls_hardening.sql` | `[LIVE]` (committed `5ddb520`/`238759b`; §2 deferred) |
| 2026-06-18 | **Discord notifications on reel assignment** — Moving a reel to **In Progress** (or **Send Back** for revision) fires a Discord webhook ping to the assigned editor + Paul + Leroy. Owner sets per-person webhook URLs + a trigger-mode toggle (**All team** vs **Owner only**) in Roles Admin → Discord notifications. Folded into `suggest.js?action=discord-notify` (no new Vercel fn); fire-and-forget so failures never block. **Owner must enter webhook URLs before pings fire** | `api/ai/suggest.js`, `src/store/store.jsx`, `src/pages/roles-admin.jsx`, `app_settings.discord_config` | `[LIVE]` |
| 2026-06-18 | **Pulse — comprehensive view: 4 layouts + inline tag editing** — Classic feed gained a persisted **layout switcher** (Timeline / Cards magazine / Table sortable / Board Kanban-by-severity). **Click any row → inline-expand** to add/remove **tags** (chip editor) + flip severity/status/star/archive/delete; tags were previously creation-only. Pure UI — `updateMonitorEvent` already carried `tags` | `src/components/pulse-comprehensive.jsx`, `src/pages/pulse.jsx`, `src/pages/pulse.css` | `[LIVE]` |
| 2026-06-18 | **World Monitor hybrid — world-events in Pulse + filming-location linking** — Classic⇆World toggle in Pulse → native world-events feed (USGS earthquakes keyless, NASA FIRMS fires, ACLED conflicts) + an embedded worldmonitor.app map; each event links to a reel / review-card / location. New Monitor "World Monitor" card (free-API usage + off-by-default paid toggles). HYBRID (not self-hosted: AGPL + Edge/Railway/Redis); ingest folded into `suggest.js?action=world-ingest` (engine `_world-feeds.js`, no new Vercel fn) | `api/ai/_world-feeds.js`, `api/ai/suggest.js`, `src/components/pulse-world.jsx`, `pulse-event-link.jsx`, `src/pages/pulse.jsx`, `src/pages/monitor.jsx`, `api/monitor/status.js`, `src/store/store.jsx`, migrations `0064–0066` | `[LIVE]` |
| 2026-06-18 | **Pulse — feed-URL diagnostics on Add-source** — Adding a source now pre-flights the URL: a **Check feed** button + add-time validator say *why* a bad URL won't work (web page not a feed / HTTP error / no-RSS platform) and offer verified one-click **"Use this"** replacement feeds (curated news directory, `<link>` autodiscovery, on-page links, common paths, IG/TikTok/X/FB rules + YouTube channel-id resolution), plus "Add it anyway". Fixes the silent "ok · +0" trap. Folded into `suggest.js?action=validate-feed` (no new Vercel fn) | `api/ai/_rss.js` (`validateFeedUrl`), `api/ai/suggest.js`, `src/store/store.jsx`, `src/components/pulse-sources.jsx`, `src/pages/pulse.css` | `[LIVE]` |
| 2026-06-18 | **Pulse — automated news/RSS ingestion** — Owner curates RSS/Atom feeds (Sources manager); a Hetzner cron (/30m) + "Refresh now" button fetch each feed, classify items (free OpenRouter, source-default fallback), dedup, and write them into the Pulse feed as `poller` rows. Zero-dep parser, 60-day retention prune, News Monitor health card. Folded into `suggest.js?action=news-ingest` (no new Vercel fn) | `api/ai/_rss.js`, `api/ai/suggest.js`, `src/components/pulse-sources.jsx`, `src/store/store.jsx`, `src/pages/monitor.jsx`, migrations `0060_monitor_sources.sql`, `0061_monitor_events_dedup_full_index.sql` | `[LIVE]` |
| 2026-06-18 | **Pulse monitor tab (manual)** — Owner-only live feed for algorithm updates + world/political news: category/platform/severity filters, status lens, manual "Add entry" form, realtime | `src/pages/pulse.jsx`/`.css`, `src/components/pulse-feed.jsx`, `pulse-filters.jsx`, `pulse-entry-modal.jsx`, `src/app.jsx`, `permissions-catalog.js`, migration `0059_monitor_events.sql` | `[LIVE]` |
| 2026-06-17 | **`/workflow` orchestrator skill** — Runs a `/qa-verified-plan` output by spinning up one Senior Architect agent per mission-critical component, each managing implementer subagents + exactly one dedicated QA agent, across parallel waves with inter-wave gates | `.claude/skills/workflow/SKILL.md` | `[LIVE]` |
| 2026-06-17 | **Migration manifest auto-regen (prebuild)** — Monitor "Check migrations" no longer errors on a stale manifest; a `prebuild` npm hook regenerates `migrations.manifest.json` on every build (was stale 54/57) | `package.json`, `api/monitor/status.js`, `api/monitor/migrations.manifest.json`, `supabase/MIGRATIONS.md` | `[LIVE]` |
| 2026-06-17 | **`/senior-architect` skill** — Executes an approved /qa-verified-plan output task-by-task with file ownership registry, per-task sub-agent teams (3–4 agents + QA), sequential layer execution, and cross-task contamination prevention | `.claude/skills/senior-architect/SKILL.md` | `[LIVE]` |
| 2026-06-17 | **`/update-migrations` skill + schema_migrations sync** — Claude skill that auto-applies pending SQL migrations to Supabase without manual dashboard pasting; also synced 10 previously-missed migrations (0045–0053, 0055) that were applied manually | `.claude/skills/update-migrations/SKILL.md`, `scripts/migrate.mjs` | `[LIVE]` |
| 2026-06-14 | **Anthropic (Claude) monitor card + kill switch** — Monitor page card showing Claude status with a sliding toggle that pauses all server-side Claude usage (Generate, FAQ bot, daily suggestions). Gate reads from `app_settings`; fails open so a DB hiccup never breaks AI features | `src/pages/monitor.jsx`, `src/pages/monitor.css`, `api/admin/_auth.js`, `api/generate.js`, `api/ai/ask.js`, `api/ai/suggest.js`, migration `0043_anthropic_killswitch.sql` | `[LIVE]` |
| 2026-06-14 | **Migration tracking + Monitor check button** — `schema_migrations` table tracks applied/pending/changed migrations; Monitor page has a "Check Migrations" button with status summary | `supabase/migrations/`, `src/lib/` migration utils | `[LIVE]` |
| 2026-06-14 | **AI Brain tab** — Searchable FAQ + AI synthesis layer using Supabase vector search (pgvector); owner can add/edit FAQ pairs | `src/pages/ai-brain.jsx`, `src/pages/ai-brain.css`, `api/ai/ask.js`, `api/ai/suggest.js`, migrations `0039–0042` | `[LIVE]` |
| 2026-06-13 | **Rocket.Chat integration** — Self-hosted team chat at `chat.footagebrain.com` (RC 7.13.8 + MongoDB on Hetzner); Team tab iframe-embeds chat; owner Outbox panel for DMs | `src/pages/team-chat.jsx`, `src/pages/inbox.jsx`, `src/lib/social-client.js`, Hetzner `docker-compose.yml` | `[LIVE]` |
| 2026-06-13 | **Workflow insights cron** — Daily AI-generated workflow suggestions stored in Supabase; surfaced in AI Brain | migration `0042_workflow_insights.sql` | `[LIVE]` |
| 2026-05-22 | **start-all.bat** — Single launcher that starts Content Desk, FootageBrain, and Ziflow apps together | `start-all.bat`, `launch.bat` | `[LOCAL]` |

---

## 4. Social & Analytics

| Date | Feature | Key Files / Migration | Status |
|------|---------|----------------------|--------|
| 2026-06-22 | **Push to Planable — grouping + video-attach fix + per-platform titles + posting-time** — one push now creates **one campaign** bundling all reels; **each reel = its own grouped cross-page post** (`pageIds[]`→one `groupId`) fanned across all selected channels (was: separate card per platform, via `POST /campaigns` + `campaignId`). **Video attaches** now — `media` is a **direct public-URL array** (deleted the broken two-step `/media` that used field `url`≠`mediaUrls` + a media id where a URL belongs). **Title** for YT/LinkedIn/Pinterest from the reel-card title (`youtubeTitle`/`linkedinVideoTitle`/`pinterest.title`); caption stays the logline. New **posting-time `<input type="time">`** threaded into `composeSchedule` (each reel keeps its own date). Verified vs the live OpenAPI; built via `/workflow-file-creation` → `planable-grouping-fixes.js`. **⚠ media direct-URL CONTRADICTS the phase-2 two-step finding — calibration push pending** | `api/ai/_planable.js`, `api/ai/suggest.js`, `src/pages/export-view.jsx`, `supabase/migrations/0090_planable_pushes_grouping.sql` (new, **APPLIED**), `.claude/workflows/planable-grouping-fixes.js` | `[LIVE]` (`dpl_Ab5JtD1Wdwofi47opLziga5yxpfn`, uncommitted) |
| 2026-06-22 | **Permissions — remove Leroy ("maya") full-access bypass** — `isOwnerRole()` + `canView`/`canAct` no longer special-case `id === "maya"`; Leroy's access now follows his role + per-person config. Caught at the pre-deploy tree check, owner-confirmed, shipped with the Planable deploy | `src/lib/permissions.jsx` | `[LIVE]` (`dpl_Ab5JtD1Wdwofi47opLziga5yxpfn`, uncommitted) |
| 2026-06-22 | **Reel DNA Analyze — inline progress strip + debug logging** — Animated indeterminate bar under the Analyze button while queued; live fill bar with % while deconstructing (all formats). `[Analyze]` console.log at queuing / DB-persist / Vercel-fire / Vercel-response. Root cause diagnosed during calibration: IG cookies on Hetzner stale → `acquire_failed`; YouTube reels unaffected. Deployed full-tree | `src/components/unified-dna-card.jsx`, `src/components/reel-story-panel.css`, `src/store/store.jsx` | `[LIVE]` (`dpl_SV5dkFDtfWVYbv4uySpuDLM2G1y1`, uncommitted) |
| 2026-06-21 | **Reel DNA spreadsheet — hide/collapse columns + freeze header row & first column (per-user)** — "Columns" menu (grid-only) hides any of the 8 hideable columns, saved per-user (`user_preferences` key `reel_dna_hidden_cols`); header row + first ★/color column stay frozen while scrolling (desktop + mobile). Single-source-of-truth column registry gates all 3 render sites + dynamic colSpan + auto-clears a hidden column's filter. **Freeze fix:** `.rd-table-wrap` made a bounded 2-D scroll box (`overflow:auto` + `max-height:calc(100dvh-200px)`) — sticky only pins vs the nearest scroll container. No migration | `src/lib/reel-dna-filters.jsx`, `src/pages/reel-dna.jsx`/`.css`, `src/components/reel-dna-comprehensive.jsx`, `src/store/store.jsx`, `.claude/workflows/reel-dna-cols-and-multi-editor.js` | `[LIVE]` (`694e0c6`, `dpl_53XjnpzdMdbT9koUgPH7wfVNSXpj`) |
| 2026-06-21 | **Reel DNA — send a captured card to the pipeline for chosen editor(s)** — the **→ Pipeline** row button opens an editor-picker modal: tick one+ editors, each gets the reel in their Not Started (first = linked reel + ▸ badge + ↩ DNA; extras = independent `(FirstName)` copies via `duplicateReel`). FAB "Create new reel" also got a multi-editor dropdown (`createReelForEditors`). An earlier copy of this on the pipeline card ⋯ menu was **reverted** (owner wanted it Reel-DNA-side) | `src/pages/reel-dna.jsx`, `src/store/store.jsx`, `src/components/modals/ReelModal.jsx`, (`components.jsx` reverted) | `[LIVE]` (`694e0c6`) |
| 2026-06-21 | **Reel DNA — in-app reel preview popup (all platforms) + visited-link tracking** — clicking a reel link opens it in an in-app popup (embeds IG/YouTube/TikTok/Facebook; YT+FB autoplay; always "Open original ↗"; ctrl/cmd-click still new-tab) so you keep the sheet to take notes; clicked titles keep a per-user **visited underline** + last-clicked marker (`user_preferences` key `reel_dna_visited`). Fixed 2 bugs from the first build: `.rd-preview-*`/`.rpv-*` class drift + `platformFromUrl` never returning `"fb"` | `src/components/reel-preview-modal.jsx` (rewritten), `src/pages/reel-dna.jsx`/`.css`, `src/store/store.jsx` | `[LIVE]` (`694e0c6`) |
| 2026-06-21 | **Reel DNA — "↩ Back to Reel DNA" (reverse of Send-to-Pipeline)** — a linked card's amber ↩ DNA button pulls the reel back: migrates the pipeline reel's assets (footage/locations/news via `seedAssetsFromPipeline`) + text (logline/brief/VO/script/audio → card Notes) back, unlinks + resets to captured (→ Pipeline returns), and **soft-archives** the reel (never delete — would cascade-drop footage). New `returnReelFromPipeline(reelId)` | `src/store/store.jsx`, `src/pages/reel-dna.jsx`/`.css`, `src/components/reel-dna-comprehensive.jsx` | `[LIVE]` (`694e0c6`) |
| 2026-06-21 | **Push to Planable from the Export tab** — select posted reels → pick a platform → one-click create **scheduled DRAFT** posts via Planable's write API (`api.planable.io/api/v1`), keeping approval inside Planable; CSV download unchanged. **Cross-posting guard** for the SHARED "Nikky Kho" workspace = server-side page-ID allow-list (only the owner's 6 page IDs) + mandatory preview; `publishAtScheduledDate:false` = never auto-publish. Built via plan + 3-team `push-to-planable` workflow, then set up live: token validated, helper patched to the real API, **migration 0087 applied**, `app_settings.planable_config` written, `PLANABLE_API_TOKEN` set (env+Vercel). **Caption/text ready; video attach needs a follow-up two-step `/media` fix; owner browser test pending** | `api/ai/_planable.js` (new), `api/ai/suggest.js`, `src/pages/export-view.jsx`, `supabase/migrations/0087_planable_pushes.sql` (new), `.claude/workflows/push-to-planable.js` | `[LIVE]` (`dpl_Dd9RGGqy5MvpUqoMzpTHSyofYuDX`, uncommitted) |
| 2026-06-20 | **Side-by-side Reel Comparison** — `ReelCompareModal` full-screen split view (inspiration left / current edit right), local file upload (screen recordings), paste-a-URL, auto-fill from linked pipeline reel's `attachUrl`; "📤 Share to channel" posts structured RC message + deep-link `?reel=X&compare=1`. **5 entry points:** detail page inspo section, Reel DNA overlay header, Reel DNA table rows, gallery cards, Team Chat Compare panel (3-step: pick reel → attach recording → post to channel) | `src/components/ReelCompareModal.{jsx,css}` (new), `src/app.jsx`, `src/pages/detail.jsx`, `src/components/reel-dna-view.jsx`, `src/pages/reel-dna.jsx`, `src/components/unified-dna-card.jsx`, `src/pages/team-chat.jsx` | `[LIVE]` (`4f82931`, `dpl_9BE57RF9Vi34ZoTtZstC8jvZzUMG`) |
| 2026-06-20 | **Reel DNA spreadsheet — "Hide Sent" toggle** — Reels that have been sent to the pipeline (`reelId` is set) are now **hidden by default** in the Reel DNA spreadsheet. A `<DPill>` toggle ("Hide Sent" / "Showing Sent") in the filter bar reveals them. `showSent` state (default `false`) added after `showArchived`; `baseList` memo adds `(showSent \|\| !d.reelId)` filter | `src/pages/reel-dna.jsx` | `[LIVE]` (`ec2cf79`) |
| 2026-06-20 | **Reel DNA — Phase 1 ACTIVATION planned (QA-verified) + reachability fix applied** — QA-verified runbook (`/qa-verified-plan`: 4 domain agents + 1 adversarial QA) for turning on the already-built short-reel backend (PySceneDetect cuts + downloadable asset layers + cut-pacing). QA **refuted** the `_claim_one format=eq.long` fear (it's format-agnostic at `:211`) and found **3 blocking gaps** the deploy doc missed → now read-gates: **B1** DATA_DIR must be a persistent volume (else `up -d` wipes media-retained assets → downloads 404), **B2** pre-apply LIVE-catalog audit of 0079 (ledger can lie), **B3** freeze the queue (0 `pending_analyze` + pause `*/2` cron) during the window. Proxy target = `backend:8000` (not the doc's `127.0.0.1:8000`). Applied the **reachability fix** (`unified-dna-card.jsx` Analyze button un-gated from `isLong`). STEP 0 hygiene gate green. Tech locked: PySceneDetect now, **TransNetV2 = Phase-5 fast-follow** behind the `_detect_scenes:764` seam. Decisions: 4-reel calibration · manual-upload-first (no cookies) · keep auth-non-demo download gate. **Nothing deployed/committed/migrated** — STEPS 0.5→9 human-gated next session | `.claude/plans/reel-dna-phase1-activation-pyscene.md` (new), `src/components/unified-dna-card.jsx` | `[LOCAL]` |
| 2026-06-19 | **IG sync was dropping ~80% of DM'd reels — fixed & DEPLOYED (`reel_dna` ig_dm 74 → 393)** — "IG sync checked — seen 46, 0 new + 46 already captured, 34 issue(s) logged": the poller only read the latest **50 messages/thread** and **dropped any thread whose message fetch 500'd** ("reduce the amount of data" — the heavy `attachments{type,payload}` field). Missing reels were deeper in the same 3 threads. Live diagnosis (`scripts/ig-sync-diagnose.mjs`) **refuted the null-link hypothesis** (`skipped_no_link=0`; all 34 were `graph_error`). Fix: slim query to `shares{link}` (attachments/story debug-only), `_fetch_messages()` full pagination + adaptive page-shrink (25→…→5), `_graph_request()` retry on transient 5xx/subcode-99, single-flight `_SYNC_RUNNING` guard (stop overlapping crons racing Graph), `graph_errors` folded into reconciliation, and don't count the non-IG Page's expected 400. **MERGED** with the live file (it was ahead with the 0077 content_type feature — never overwrote). Deployed to Hetzner (build+recreate, in-container sha verified). Latest sweep `reconciled=true` | `backend-handoff/ig_webhook.py`, `scripts/ig-sync-diagnose.mjs` (new) | `[LIVE]` (uncommitted) |
| 2026-06-19 | **Reel DNA — DNA breakdown overlay sized + scrollable, close ✕ always reachable** — the `ReelDnaView` overlay (DNA / View DNA button) used to overflow the viewport with no scrollbar on tall reels, stranding the bottom of the breakdown. Fixed the flex-column scroll trap: `.rdv-stage` got `flex:1 1 auto; min-height:0; overflow-y:auto` (so it actually scrolls) and `.rdv-header` got `flex:0 0 auto` (so the ✕ stays pinned; Esc/backdrop also close). CSS-only. **Deployed in ISOLATION** off `main` (`dpl_6zBjVQN1V8dqA23dX9PNC3QwBJmP`); none of the dirty WIP tree shipped | `src/components/reel-dna-view.css` | `[LIVE]` |
| 2026-06-20 | **Reel DNA — Phase 1 (Reel MVP) + Phase 0 FE carry-forward** — built via the generated `reel-dna-phase1` workflow (~25 agents, ~44 min, zero fix rounds) onto branch `feat/reel-dna-phase1`. **Wave 1** carried the parked Phase 0 longform "Story" FE into the clean baseline (5 whole files + 3 store mappers + `?action=deconstruct` + Story panel; leakage gate 0). **Wave 2** built the reel pipeline: migration `0081` (`asset_manifest`+`pacing` jsonb), HMAC `?action=sign-download` folded into `suggest.js` (fn count still 12/12), `reel_deconstruct.py` reel branch (manual-upload-first → yt-dlp → ffmpeg audio → PySceneDetect → keyframes → pacing math) + serving HMAC validator + `DEPLOY-PHASE1.md`, and `reel-assets-auto.jsx`+`pacing-sparkline.jsx`+card Assets section. Build green (829 mods). **WAVE 1 SHIPPED 2026-06-20** — /qa-verified-plan cleared all 5 complications → committed `8b5eeb4` → `vercel --prod` LIVE (`dpl_5VkNyLPG`, www.footagebrain.com): longform Story FE active vs the live Phase 0 backend; Wave 2 reel FE shipped INERT/read-safe. Reachability fix (short-Analyze) + Phase 1 backend activation DEFERRED to a 2nd human-gated deploy (a UI trigger must not ship before its backend) | `.claude/workflows/reel-dna-phase1.js` (new), `supabase/migrations/0081_reel_dna_assets_pacing.sql`, `api/ai/suggest.js`, `vercel.json`, `src/store/store.jsx`, `src/components/{unified-dna-card,reel-assets-auto,pacing-sparkline,reel-story-panel}.jsx`, `src/lib/reel-narrative.jsx`, `backend-handoff/{reel_deconstruct.py,DEPLOY-PHASE1.md,REEL-DECONSTRUCT-DEPLOY.md}` | `[LIVE]` (Wave 1; Phase 1 backend pending) |
| 2026-06-19 | **Reel DNA — Phase 0: Longform YouTube "Story" deconstruction (flagship reverse-engineering engine)** — flag a capture `format='long'` → owner clicks **Analyze** → a new Hetzner worker pulls audio-only + native captions (faster-whisper fallback) and runs ONE free-LLM **narrative** pass → a **Story panel** renders hook type/strength, story arc, open loops, emotion-curve sparkline, retention-risk flags, payoff/CTA + a 0–100 scorecard, arriving live via realtime. Built via `/workflow` (waves: **T-DB / T-API / T-STORE / T-WORKER** ∥ → **T-UI**) under locked disjoint file ownership + adversarial QA each; integration build + per-role Playwright smoke green; short-format cards proven zero-regression (panel behind `format==='long'`, controls behind `isOwner`). Free LLM now behind a `run_narrative(model)` **provider seam** (Claude swap later). Vercel proxy is a thin fire-and-forget clone of `ig-sync` (no new fn). **NOT deployed** — migration `0079`, `REEL_DECONSTRUCT_SECRET`, Hetzner worker deploy, `vercel --prod` are all human-gated | `supabase/migrations/0079_reel_dna_longform.sql`, `api/ai/suggest.js`, `src/store/store.jsx`, `src/components/reel-story-panel.jsx`/`.css`, `src/lib/reel-narrative.jsx`, `src/components/unified-dna-card.jsx`, `backend-handoff/reel_deconstruct.py` + `REEL-DECONSTRUCT-DEPLOY.md` | `[LOCAL]` |
| 2026-06-19 | **Inbox — AI reply suggestions (✨) + YT/TikTok reply scaffold** — opt-in **✨ Suggest replies** button per **unanswered** thread → 2-3 short on-brand drafts → clicking a pill **seeds the editable compose box** (human always edits + Send; AI never auto-sends). Folded into `api/ai/monitor.js` `suggest_replies` mode (**0 new Vercel fns** — under the 12/12 cap; free OpenRouter, array-only `extractJson`, `classifyCaller` demo gate, always-200 graceful degrade). `replyToThread` gained YT/TikTok branches with a `{pending:true}` "pending business verification" state (drop-in once the Hetzner reply endpoint ships). Planned via `/qa-verified-plan` (QA fixed 3 issues pre-code). **Deployed in ISOLATION** from the dirty 3-thread tree (stash → branch from `main` → 4 files only → `vercel --prod`) — commit `75c44c6` on `deploy/ai-inbox`, `dpl_2efqqQ5C…`. Merge to `main` pending | `api/ai/monitor.js`, `src/lib/social-client.js`, `src/pages/inbox.jsx`/`.css` | `[LIVE]` |
| 2026-06-19 | **Reel DNA — YT/FB DM links auto-tag `content_type='video'`** — DMing a **YouTube or Facebook** link now lands tagged `video` (was `'unknown'`/`NULL`) so the spreadsheet **Type** filter surfaces it. Fixed 3 independent bugs: backend `_classify_content_type` gained a `url_override` fallback to `_content_type_from_url()` for ambiguous native `media_share` shares (webhook + polling paths); frontend `createReelDnaCapture` now derives `contentType` from platform (new `contentTypeFromPlatform` helper); `platformFromUrl()` got its missing **Facebook** branch (had drifted from backend `_detect_platform`). Migration `0077` retro-fixed existing rows (4 YT + 2 FB → video, 0 left). **Deployed all 3 layers:** migrate:apply, Hetzner rebuild (`ingest_enabled:true`), `vercel --prod` (`dpl_86jsjcns5SobFNLeDgmnD3qHXevP`). Completes the `d930896` cross-platform thread | `backend-handoff/ig_webhook.py`, `src/lib/reel-dna.jsx`, `src/store/store.jsx`, migration `0077_fix_yt_fb_content_type.sql` | `[LIVE]` (uncommitted) |
| 2026-06-19 | **Reel DNA — cross-platform IG-DM link ingest + content_type persist** — DMing a **YouTube or Facebook** link to IG now lands in Reel DNA correctly tagged: poller derives `platform` from the URL host (was hardcoded `ig`) and **captures plain-text URLs** (it previously `continue`d on any message with no `shares` edge, so pasted external links were silently dropped). Also fixed: in-app `content_type` corrections now persist (`persistUpdateReelDna` was missing the `contentType`→`content_type` remap). Built via `/qa-verified-plan` → `/workflow`. **Deployed:** C1→Hetzner (`docker compose build/up`, `ingest_enabled:true`), C2→Vercel (`dpl_2YX69tRfyUmx1S7PKHocPSwged2p`). **STILL UNVERIFIED:** whether Meta Graph returns the DM text body — empirical test via `ig_ingest_log` after a YT/FB plain-text DM | `backend-handoff/ig_webhook.py` (`_detect_platform`, `_content_type_from_url`, text-URL capture), `src/store/store.jsx` | `[LIVE]` (committed `d930896`) |
| 2026-06-18 | **IG DM reconciliation + non-reel content-type ingest** — every IG DM the Graph API returns is captured or **logged with the reason**; new **IG Sync Health** strip (seen vs accounted, red Mismatch badge, amber "coverage may be incomplete" when Graph caps/errors hit) + owner Discord alert on mismatch. Poller now captures **carousels / photos / stories** (not just reels), classifies via `content_type` (composite `external_ref={mid}:{i}` for multi-share); new **Type** column + filter. Built `/qa-verified-plan` → `/workflow`; Hetzner poller deployed + verified (first run reconciled, `graph_errors 3`). FB/YT→IG feasibility writeup in `docs/ig-crosspost-feasibility.md` (no build) | `backend-handoff/ig_webhook.py`, `src/store/store.jsx`, `src/pages/reel-dna.jsx`/`.css`, `src/lib/reel-dna.jsx`, `reel-dna-filters.jsx`, `api/ai/suggest.js`, migrations `0073_ig_sync_runs.sql`, `0074_ig_ingest_log.sql`, `0075_reel_dna_content_type_extend.sql` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — 🔎 Check IG Sync button** — on-demand reconciliation report: re-pulls the poller's run history + per-message issue log from Supabase and shows **what landed in the sheet** (new/deduped/seen + total IG-DM rows), coverage this run, and **every error by type** (graph/insert/skipped) with detail text. Robust to a missed realtime socket; separate from ↻ Refresh (which force-polls) | `src/store/store.jsx` (`reloadIgSync`), `src/pages/reel-dna.jsx`/`.css` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — Comprehensive-only overhaul + bug batch** — Removed the **Classic view** entirely (toggle, status/source pills, Cards/Spreadsheet sub-toggle, `unified_cards` flag, legacy `DnaCard`); Comprehensive is now the only view. Removed the **left facet rail** → one **global search** + **per-column filter headers** in the Grid. Grid rows get a **"⤢ Card"** button → rich `UnifiedDnaCard` in a **centered modal** to attach assets; **Gallery uses `UnifiedDnaCard`** too. Bug fixes: dead "Assets →"/count-badge now opens the full-screen Assets page (`onOpenAssets` was never forwarded into Comprehensive); **thumbnails** fall back to stored `thumbnailUrl`; **location pins** → Google Maps links, **footage names** → Drive links; **detach ✕** rolls back on persist failure; `baseList` excludes `deletedAt`. Supersedes the 2026-06-18 "per-column filters + Comprehensive (faceted Grid/Gallery)" row below. No DB changes. Planned via `/qa-verified-plan` (Explore+Plan agents), built inline. **Committed `f24421f`, deployed.** | `src/components/reel-dna-comprehensive.jsx`, `src/pages/reel-dna.jsx`/`.css`, `src/components/reel-assets.jsx`, `src/store/store.jsx`, `src/lib/reel-dna-filters.jsx` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — quick multi-attach + Unified card + font fix** — (1) Fixed the smeared Reel DNA **font** (Google-Fonts faux-bold — `index.html` loaded only JetBrains Mono `400;500` while the CSS uses `600/700`; added the missing weights + Inter 700). (2) NEW **searchable multi-select attach picker** — a `+` per category (Footage/Locations/Thumbnails/News) opens a popover (search + checkboxes) to attach many existing assets at once (attaching had NO UI before, only detach). (3) NEW **UnifiedDnaCard** behind owner `unified_cards` flag (default OFF, toggle in Roles & Permissions → Feature flags): quick-attach pickers + inline create-&-attach new thumbnail/news + "Hide assets" toggle + "↓ Pull from pipeline". (4) Fixed latent bug: detaching a **location/thumbnail silently failed** (plural label vs singular stored `asset_type`). No migration (flag rides `app_settings`). Built inline | `index.html`, `src/components/asset-attach-picker.jsx`/`.css`, `src/components/unified-dna-card.jsx`/`.css`, `src/components/reel-assets.jsx`, `src/pages/reel-dna.jsx`, `src/pages/roles-admin.jsx`, `src/store/store.jsx` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — Facebook Reels platform** — "Facebook Reel" is now selectable in the platform dropdown (track FB reels like IG/TikTok/YouTube). DB `reel_dna.platform` never had a real CHECK constraint, so 0071 is defensive. FB auto-ingest (Graph API) deferred | `src/lib/reel-dna.jsx`, migration `0071_reel_dna_platform_fb.sql` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — IG carousel groundwork (`content_type`)** — Added `content_type` column (reel/carousel/photo/unknown) + `reelDnaToDb()` mapper, prepping the IG DM poller to distinguish carousels/photos from reels. Actual poller parse-message-text change is Hetzner-side (private repo), still pending | `src/store/store.jsx`, migration `0072_reel_dna_content_type.sql` | `[LIVE]` (DB) |
| 2026-06-18 | **Reel DNA — Thumbnails shortcut pill in filter bar** — second entry point for the Thumbnails tab sits in the filter-bar gap between "source" and "view" pills; lights up when active; more discoverable than the header tab-strip | `src/pages/reel-dna.jsx` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — unified Assets column + full-screen Assets page** — every reel card gains an **Assets** column: four collapsible sections (Footage / Locations / Thumbnails / News), collapsed by default with a per-category **count badge**, expand on demand. An **"Assets →"** heading opens an in-app **full-screen page** with all four expanded + a **"Pull from pipeline reel"** seed button (gathers the linked reel's footage/locations/news). Spreadsheet view gets an **Assets count cell** that opens the same page. Backed by a polymorphic `reel_dna_assets` join table (attach any asset to any card on demand; FULL unique index as the upsert arbiter); News opened team-wide via RLS. Built via `/qa-verified-plan` → `/workflow` (T-SCHEMA/T-STORE/T-UI under file-ownership isolation) | `src/components/asset-section.jsx`, `reel-assets.jsx`, `reel-assets-panel.jsx`, `src/lib/reel-dna-assets.jsx`, `src/pages/reel-assets-page.jsx`, `src/pages/reel-dna.jsx`/`.css`, `src/store/store.jsx`, migrations `0068_reel_dna_assets.sql`, `0069_monitor_events_team_read.sql` | `[LIVE]` |
| 2026-06-18 | **YouTube playlist → Thumbnails auto-ingest (RSS poller)** — drop a video into a **public** YouTube playlist → a 15-min poll reads its RSS/Atom feed and auto-catalogs each thumbnail into `thumbnail_dna`, deduped on `video_id`. "↻ Refresh" button + "already captured" 409 guard. Folded into `suggest.js?action=yt-sync` (no new Vercel fn). Endpoint verified live `{items_seen:2,inserted:0}` | `api/ai/_rss.js` (`parseYouTubePlaylistFeed`), `api/ai/suggest.js`, `src/store/store.jsx` (`triggerYtSync`), `src/lib/thumbnail-dna.jsx`, `src/pages/thumbnail-dna.jsx`, migration `0067_thumbnail_dna_video_id_uidx.sql` | `[LIVE]` |
| 2026-06-18 | **Thumbnail DNA — YouTube "Thumbnails" tab in Reel DNA (paste-in)** — "Reels \| Thumbnails" toggle; paste a YouTube link → client-derived thumbnail → tag six manual **design genes** (color/typography/face/layout/mood/subject) → catalog in grid/spreadsheet. `?action=youtube-oembed` enriches title/channel (best-effort, Vercel-only) | `src/lib/thumbnail-dna.jsx`, `src/pages/thumbnail-dna.jsx`/`.css`, `src/pages/reel-dna.jsx`, `src/store/store.jsx`, `api/ai/suggest.js`, migration `0063_thumbnail_dna.sql` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — per-column filters + Comprehensive (faceted Grid/Gallery) view** — Classic spreadsheet gained a **per-column filter row** (text per column + Source/Status selects + clear). New **Classic ⇄ Comprehensive** toggle: Comprehensive = a left **faceted rail** (Platform/Status/Source/Genes multi-select w/ counts + global + location search) and a **Grid ⇄ Gallery** sub-toggle, reusing the existing `DnaTable`/`DnaCard`. Shared filter model reads via `resolveBrief`. No DB changes | `src/lib/reel-dna-filters.jsx`, `src/components/reel-dna-comprehensive.jsx`, `src/pages/reel-dna.jsx`, `src/pages/reel-dna.css` | `[LIVE]` |
| 2026-06-18 | **Fix: deleted Reel DNA cards stay deleted (tombstone)** — Hard delete let the Hetzner IG poller (dedupes on `external_ref` of live rows) re-insert the same DM'd reel on the next sync. `deleteReelDna` now soft-deletes (stamps `deleted_at`, keeps the row so the poller skips it) + hides it from every view (load queries + realtime). Archive unaffected | `src/store/store.jsx`, migration `0062_reel_dna_deleted_at.sql` | `[LIVE]` |
| 2026-06-18 | **Fix: Reel DNA ↻ Refresh "IG sync failed (500)" + stop accidental Claude billing** — the `?action=ig-sync` handler was never deployed, so prod fell through to the daily Claude generator (Hobby ~10s timeout → bare 500 + billed `ANTHROPIC_API_KEY` per click). Deployed the handler + unknown-action 400 guard + 8s `AbortController` fire-and-forget | `api/ai/suggest.js` | `[LIVE]` |
| 2026-06-18 | **Reel DNA — Refresh, hard Delete, Send-to-Pipeline** — ↻ Refresh button force-runs the Hetzner IG poller now (beats the 15-min cron) via `?action=ig-sync` (secret server-side) then reloads; hard **Delete** (no confirm, mass-delete) alongside restorable Archive; card **production-brief** block + **→ Send to Pipeline** that creates a linked `reels` row from the genes (mapped 1:1). Fixed the `reel_dna_reel_id_fkey` race (persist reel before the link) | `src/pages/reel-dna.jsx`/`.css`, `src/lib/reel-dna.jsx` (`reelDnaToPipelineFields`, `resolveBrief`), `src/store/store.jsx`, `api/ai/suggest.js` | `[LIVE]` |
| 2026-06-17 | **Reel Inspiration Library (on Reel DNA)** — Tag-note parser (`location=Bali, music=phonk, font=Aktiv, sfx=…`) auto-fills the gene fields + new `location` column + chips; Cards⇄Spreadsheet toggle with inline-editable columns; clicking a row opens the existing DNA helix/timeline; parse-on-read fills IG-DM/manual rows. The 1-click logger for inspiration reels | `src/lib/reel-dna.jsx` (`parseTagNote`), `src/pages/reel-dna.jsx` (`DnaTable`/`EditableCell`), `reel-dna.css`, `store.jsx`, migration `0058_reel_dna_location.sql` | `[LIVE]` |
| 2026-06-18 | **Instagram DM → Reel DNA spreadsheet (poll)** — DM a reel (+ tag note) to @paulvictortravels → auto-logged to the Reel DNA spreadsheet. Polls the Business-Suite IG inbox (`/api/ig/sync`), pulls each reel permalink from `shares.data[].link`, pairs the adjacent tag note, dedups into `reel_dna`. 15-min Hetzner cron. Webhooks abandoned (need app Published + App Review; dev mode = synthetic Test only) | Hetzner `backend/app/api/ig_webhook.py` (src: `backend-handoff/ig_webhook.py`) | `[LIVE]` |
| 2026-06-11 | **Instagram per-reel analytics grid** — Analytics tab shows 12-reel thumbnail grid; clicking opens detail panel with 8 lifetime metrics (reach, views, likes, comments, shares, saves, interactions, avg watch time) | `src/pages/analytics.jsx`, `src/lib/social-client.js`, Hetzner `backend/app/api/instagram.py` | `[LIVE]` |
| 2026-06-11 | **Fix Instagram Page selection** — Backend now probes all FB Pages for a linked IG account instead of blindly using `pages[0]`; fixed for multi-Page accounts | Hetzner `backend/app/api/instagram.py` | `[LIVE]` |
| 2026-06-11 | **Fix Instagram insights metric format** — Switched to `metric_type=total_value` for `profile_views`; `reach` remains daily time-series. Two-call approach keeps response shape stable | Hetzner `backend/app/api/instagram.py` | `[LIVE]` |
| 2026-06-11 | **Remove deprecated `pages_messaging` FB scope** — Cleared "Invalid Scopes" error blocking all social connects; DMs disabled until Meta App Review restores scope | Hetzner `deploy/hetzner/.env`, `docker-compose.yml` | `[LIVE]` |
| 2026-06-09 | **Social OAuth accounts panel + reconnect cards** — Settings-style panel showing connection status for each platform with one-click reconnect | `src/components/social-status.jsx`, migration `0028_social_connection_health.sql` | `[LIVE]` |
| 2026-06-08 | **YouTube OAuth + social connection health** — YouTube connect flow; connection health table tracks last-checked + error state | migrations `0027_social_connections.sql`, `0028_social_connection_health.sql`, `0030_youtube_oauth_note.sql` | `[LIVE]` |

---

## 5. Pipeline / Workflow Core

| Date | Feature | Key Files / Migration | Status |
|------|---------|----------------------|--------|
| 2026-06-21 | **Push to Planable — phase 2: video attach + upload pipeline + multi-account** — (1) **two-step async video attach** (`POST /media` → poll `GET /media/{id}` until `status:"success"` → `POST /posts` with Planable's media id; replaces the broken single-URL path; never-publish preserved); (2) **final-video upload pipeline** — owner-only MP4 upload on the detail page → private `reel-videos` bucket → push mints a signed url → `?action=planable-cleanup` cron deletes the source ONLY after `planablePostHasMedia` confirms ingest → `?action=planable-upload-target` capacity check (Hetzner seam near ~80%); (3) **multi-account push** — per-account checkbox dropdown + **"Select all accounts"**, fan-out one call per account, per-platform status. Built via `/workflow-file-creation` → `.claude/workflows/planable-media-upload.js`. Pipeline is **lossless up to Planable** (raw-byte upload, no transcode). **Live `/media` calibration pending; Threads excluded (it's Nikky's)** | `api/ai/_planable.js`, `api/ai/suggest.js`, `src/pages/detail.jsx`, `src/pages/export-view.jsx`, migration `0088_planable_media.sql` (**APPLIED**) | `[LIVE]` (`dpl_EfH9ageFLT2jwiFiwAUncEG4iHD4`, uncommitted) |
| 2026-06-21 | **Reel DNA spreadsheet row tagging** — Grid leftmost column gains a ☆ **favorite star** + an **8-tone color dot** (My Work palette) per row; picking a tone **tints the whole row** (`color-mix`). That column's heading carries a **star filter** + a **color filter** (portal popover so it isn't clipped by the table's `overflow-x`/transformed ancestor — `useColorPopover`→`createPortal` to body). **Source & Type columns removed**; new inline-editable **Notes** column (binds `quickNotes`) after Story / Pacing. Store maps `row_color`↔`rowColor` + `favorite`. New mig `0089` (**not applied**) | `src/pages/reel-dna.jsx`, `src/store/store.jsx`, `src/components/reel-dna-comprehensive.jsx`, `src/lib/reel-dna-filters.jsx`, migration `0089_reel_dna_row_color_favorite.sql` | `[LOCAL]` (build-green; 0089 pending + not deployed) |
| 2026-06-21 | **Lean FootageBrain (planned)** — one-site lean refactor (no fork): WS1 gate editor tabs to the core workflow + Team Chat/Training/Resources; WS2 `React.lazy` heavy pages + owner "Prefetch heavy tabs" toggle; WS3 role-gate store secondary fetches + 2 realtime channels behind `isOwner`; WS4 `web-vitals` perf tracking on Monitor (mig `0086_perf_samples`). Plan + generated 4-team workflow `.claude/workflows/lean-footagebrain.js` (gitignored) — NOT launched | `permissions-catalog.js`, `app.jsx`, `PreferencesModal.jsx`, `store.jsx`, `perf-tracker.js`, `monitor-hub.jsx`, mig `0086` | `[LOCAL]` (plan only) |
| 2026-06-21 | **Tasks & Comms — color picker restored** — My Work daily tasks get an 8-tone color picker (colored dot + left-border row tint); checkmark + drag-reorder preserved. New `daily_tasks.color` column (migration `0084`, **not yet applied** — colors persist after apply) | `src/pages/my-work.jsx`, `src/store/store.jsx`, migration `0084_daily_tasks_color.sql` | `[LIVE]` (UI; 0084 pending) |
| 2026-06-20 | **Duplicate reel — editor picker + first-name title + full DNA/asset clone** — `⋯` → "Duplicate →" now opens a sub-menu listing active editors by first name; clone lands in selected editor's Not Started column titled `"<Original> (Judy)"`; fully clones linked `reel_dna` card (IG embed, genes, timeline, assets) + `reel_dna_assets`; Generate/manual reels with `inspo` URL get seeded blank DNA card; XP routes to target editor automatically. Also fixed silent `timeline` drop in `reelDnaToDb` | `src/store/store.jsx` (`duplicateReel`, `reelDnaToDb`), `src/components/components.jsx` | `[LIVE]` (`cf738f8`, `dpl_FW9y5x2e5hgf6ZHHHGjE7gGHfiKC`) |
| 2026-06-20 | **Leroy full owner-level access + pipeline lane + assign-to-editor (disabled)** — Three changes for Leroy (id=`maya`, role=`reviewer`): (1) `isOwnerRole()` now returns true for `id === "maya"` → Monitor hub, Settings, perspective-switcher all unlocked; `canView()`+`can()` bypass all caps when Leroy is the real signed-in user (`!roleOverride` guard keeps Paul's perspective-preview intact). (2) Removed `filter(p => p.role !== "reviewer")` from `personLanes` builder in `pipeline.jsx` (~line 119) — Leroy's reels now appear in his personal board row. (3) Built an "Assign to editor" `<select>` in the reel detail panel (calls `moveStage(id, { lane, stage: "not_started" })`); currently **disabled** with `false /* DISABLED — awaiting owner activation */ &&` guard, awaiting owner re-activation | `src/lib/permissions.jsx`, `src/pages/pipeline.jsx`, `src/pages/detail.jsx` | `[LIVE]` (deployed; not committed) |
| 2026-06-20 | **Reel DNA Phase 1 — short-reel deconstruction ACTIVATED (end-to-end LIVE)** — short-format reels deconstruct into **downloadable asset layers** (base video / audio / per-cut keyframes / scenes.csv) + **cut-pacing metrics** (PySceneDetect ContentDetector), pulled via short-lived **HMAC-signed `/fb/reels/<id>/<file>`** URLs. Activated the already-built backend: merged Phase-0+1 worker on Hetzner (cv2 4.13 + scenedetect 0.7, claim now format-agnostic, longform intact), registered `serve_router`, added a **Caddy `/reels/*` handle → `backend:8000`**, persisted `REELS_DIR=/app/data/reels` volume (B1), set `FB_DOWNLOAD_SIGNING_SECRET` (byte-parity 3 ways), confirmed `0081` columns. Owner `vercel --prod` shipped the short-Analyze reachability fix. Calibrated all 4 reel types incl. a real signed-download **HTTP 200** stream. Ran the runbook `.claude/plans/reel-dna-phase1-activation-pyscene.md` STEP 0→9 | `backend/app/api/reel_deconstruct.py`, `backend/app/main.py`, `backend/Dockerfile.hosting`, `deploy/hetzner/{Caddyfile,docker-compose.yml,.env}`, `src/components/unified-dna-card.jsx`, migration `0081_reel_dna_assets_pacing.sql` | `[LIVE]` (uncommitted) |
| 2026-06-20 | **Pipeline card view toggle — ⊞ 2×2 / ⊟ 3×3** — toolbar toggle (≡ List / ⊞ 2×2 / ⊟ 3×3, persisted to localStorage); grid modes render cards as compact fixed-size tiles (color bar + 2-line-clamped title) packed 2/3-across per cell so you see far more reels at a glance. `ReelCard` gained a `compact` prop (hides id/series/due-date/menu/note/links/foot/status-pill). Fixed unequal-width bug via `/qa-verified-plan` (bare `1fr`=`minmax(auto,1fr)` floored by a `white-space:nowrap` status pill → `minmax(0,1fr)` on board+sub-grids + `min-width:0` + pill removal; the board template is an INLINE style at `pipeline.jsx:388` that shadows the stylesheet). **Deployed → www.footagebrain.com** (`dpl_AsWJvdpdC1hdPBURJee3AAG9YFDL`) | `src/pages/pipeline.jsx`, `src/components/components.jsx`, `src/styles.css` | `[LIVE]` (uncommitted) |
| 2026-06-19 | **🧭 Nav regroup 11→7 + centralized owner checks (reorg Parts B+C)** — Nav drawer regrouped from **11 groups → 7 workflow domains** (My Work · Produce · Library · Edit & Ship · Learn · Engage · Monitor; Activity under Monitor), killing 5 of 7 singletons (tab keys unchanged; rename-safe via the existing `nav_group_order` merge). Plus centralized the scattered `me.role === "owner"` literals behind one `useIsOwner()`/`isOwnerRole()`/`ownsReviewQueue()` source of truth in `permissions.jsx` (swapped in app/analytics/archived-view/pipeline/pulse/resources/my-work; deferred inbox/reel-dna/training — parallel-session WIP). **Deployed → www.footagebrain.com**; on `origin/main` `6fa7204` (`0c2a096`+`8efce38` cherry-picked) | `src/app.jsx`, `src/lib/permissions.jsx`, `src/pages/{analytics,archived-view,pipeline,pulse,resources,my-work}.jsx` | `[LIVE]` |
| 2026-06-18 | **Reel DNA bug-batch (7 fixes)** — (1) column-header filters keep focus while typing a whole word (`TextCell`/`SelectCell` hoisted out of `ColumnFilterRow`); (2) attaching one asset no longer duplicates (`UPSERT_REEL_DNA_ASSET` dedupes by composite key, not the `gen_random_uuid()` id — optimistic vs realtime-echo key mismatch); (3) footage rows render a thumbnail (`footageBrainThumbnailUrl`) + Drive link; (4) Thumbnail DNA spreadsheet trimmed to match the Reels grid (no image/"Playlist"/timestamp); (5) filter headers persist at 0 results (grid always renders the table; empty-row notice in tbody); (6) **Send-to-Pipeline migrates assets** (footage→`attached_footage_items`, locations→`linked_reel_ids`, news→`monitor_event_links`) + new read-only Thumbnails/News/DNA-notes boxes in the pipeline detail; (7) gallery "Hide all assets" toggle. Built via `/qa-verified-plan` → `/workflow-file-creation` → generated workflow | `src/pages/reel-dna.jsx`, `src/components/reel-dna-comprehensive.jsx`, `src/components/unified-dna-card.jsx`, `src/store/store.jsx`, `src/components/reel-assets.jsx`, `src/pages/thumbnail-dna.jsx`, `src/pages/detail.jsx`, `src/components/pipeline-dna-assets.jsx` (new), `.claude/workflows/reel-dna-bug-batch.js` (new) | `[LIVE]` |
| 2026-06-18 | **Pipeline reel collapse — persists per-user + more compact** — Collapsing a reel card now survives logout/reload (was ephemeral `useState`), per-team-member, via new `user_preferences` table; collapsed cards are tighter (~32px chip); per-lane hide/show also DB-persisted (was localStorage-only). Built via `/qa-verified-plan` | `src/store/store.jsx`, `src/components/components.jsx`, `src/pages/pipeline.jsx`, `src/styles.css`, migration `0070_user_preferences.sql` | `[LIVE]` |
| 2026-06-18 | **Fix: Tasks & Comms notes — drag-select text** — Could not drag-select note text to copy/paste (the row's drag-reorder handler hijacked the gesture); `onMouseDown stopPropagation` on both inline + expanded-modal note textareas | `src/pages/my-work.jsx` | `[LIVE]` |
| 2026-06-18 | **Display & accessibility — text size now a real display zoom + wider size/font ranges** — Fixed the "Text size" buttons that "sometimes" did nothing (they were gated behind Comfortable mode AND only nudged root `font-size`, which the px-everywhere app ignores). Now a CSS `zoom` on `<body>`, independent of theme, 8 steps **80/90/100/110/125/150/175/200%**; Font widened 3→6 (**Inter/System/Serif/Rounded/Mono/Readable**, Rounded=lazy Nunito CDN). | `src/theme-accessible.css`, `src/lib/theme.jsx`, `src/components/PreferencesModal.jsx` | `[LIVE]` |
| 2026-06-18 | **Opt-in "Comfortable" display / accessibility toggle** (owner-only) — avatar-menu **Display & accessibility** panel: legibility mode (text 13→15px, line-height 1.6, WCAG-AA brighter text, solid card borders, +padding, responsive auto-fit Analytics/Monitor grids) + Text size + Font. Per-browser localStorage → `data-*` attrs on `<html>`; OFF removes the attrs → byte-for-byte current default (`styles.css`/`:root` untouched). | `src/lib/theme.jsx`, `src/theme-accessible.css`, `src/components/PreferencesModal.jsx`, `src/app.jsx`, `src/main.jsx`, `index.html` | `[LIVE]` |
| 2026-06-17 | **Series / playlist grouping** — Tag reels with a `series` (via "+ Series" in detail); Pipeline gets a "Group by series" toggle that clusters same-series reels per cell under a label + shows a card chip | `pipeline.jsx`, `detail.jsx`, `store.jsx`, `components.jsx`, `styles.css`, migration `0057_reel_series.sql` | `[LIVE]` |
| 2026-06-17 | **Duplicate reel** — `⋯` card-menu option clones a reel (title/owner/detail-blob/footages/pins) into a fresh `REEL-NNN` for reassigning to another editor | `store.jsx` (`duplicateReel`), `components.jsx` | `[LIVE]` |
| 2026-06-17 | **Reel card readability + collapse-to-title + removed Discuss icon** — Fixed long titles spilling into adjacent cards (`min-width:0` + `overflow-wrap`); collapsed cards show only the title + Expand; removed the always-on white 💬 "Discuss in team chat" button + popover (kept the chat-ref count badge) | `components.jsx`, `styles.css` | `[LIVE]` |
| 2026-06-17 | **Permission enforcement + My Work task drag-reorder** — New `moveReel` capability actually gates reel-card moves on Pipeline / My Work / List view (completed needs `moveReel && moveToCompleted`); My Work daily tasks are now drag-reorderable via a new `daily_tasks.sort_order` column + `reorderDailyTasks()` action | `permissions-catalog.js`, `pipeline.jsx`, `list-view.jsx`, `my-work.jsx`, `store.jsx`, migration `0056` | `[LIVE]` |
| 2026-06-17 | **3D "Space" alternate homepage (`/space`)** — Owner-only interactive R3F Rubik's-cube map of the app: 6 category faces (drag-to-orbit, gold frame) → labelled column grid → per-box detail (summary + stat cards + bar graph + deep-link). ⚙ panel for cube color/style + background presets; nebula starfield + shooting stars. Fully isolated + lazy-loaded; reached via ▦ pill on owner My Work | `src/pages/space3d.jsx`, `src/pages/space3d.css`, `src/components/space/*` (RubikCube, StarWeb, SpaceMenu, DetailPanel, SpaceFallback, SpaceSettings, widgets), `src/lib/space-cube-config.jsx`, `src/app.jsx`, `src/pages/my-work.jsx`, `vite.config.js` | `[LIVE]` |
| 2026-06-14 | **Nav groups + back nav + coverage pills + footage status sheet** — Sidebar nav reorganized into logical groups; back navigation; coverage pills on reel cards; footage status overlay sheet; pipeline lane hide; resources row hide; mobile CSS | `src/app.jsx`, `src/pages/coverage.jsx`, `src/pages/footage-status.jsx`, `src/pages/training.css` | `[LIVE]` |
| 2026-06-13 | **Owner My Work command-center** — Dedicated `/my-work` page for owner: task queue, pending reviews, pipeline health summary, quick-action buttons | `src/pages/my-work.jsx` | `[LIVE]` |
| 2026-06-13 | **Remember last active tab** — `localStorage` persists the last-viewed tab so page reloads return to the same view | `src/app.jsx` | `[LIVE]` |
| 2026-06-11 | **Wrap-up / continue session handoff system** — `/wrap-up` skill refreshes HANDOFF.md + appends CHANGELOG; `/continue` skill bootstraps full context on session resume | `HANDOFF.md`, `CHANGELOG.md`, `CLAUDE.md`, `~/.claude/skills/` | `[LIVE]` |
| 2026-06-07 | **Major batch: roster-driven perspectives + per-person permissions + CapCut tracker widget** — Every UI view respects the live roster; granular per-person permission flags; CapCut tracker visible in sidebar | `src/store/store.jsx`, `src/lib/permissions.jsx`, `src/lib/permissions-catalog.js`, `src/lib/roster.jsx` | `[LIVE]` |
| 2026-06-07 | **Review queue: group by submitter + in-review timer** — Review tab groups cards by who submitted; shows elapsed time since submission; Variant role uses editor layout | `src/pages/detail.jsx` | `[LIVE]` |
| 2026-06-07 | **Stop auto-reassigning card owner on stage drag** — Stage drag no longer silently overwrites the assigned owner | `src/store/store.jsx` | `[LIVE]` |
| 2026-06-07 | **Activity tab on prod** — Live per-day activity log, previously local-only, deployed | `src/pages/activity.jsx` | `[LIVE]` |
| 2026-05-22 | **Pipeline card collapse** + search modal close-on-drag fix | `src/components/fab.jsx` | `[LIVE]` |
| 2026-05-15 | **App-wide refactor: pages/components/store/lib** — Reorganized flat file structure into proper subdirectories; extracted TaskModal/ReelModal/Modal from fab.jsx; hoisted PIPELINE_LANES; deduped PIPELINE_STAGES | All `src/` files | `[LIVE]` |
| 2026-05-14 | **Initial Workflow / Ziflow app** — Pipeline kanban, reel cards, team roles, stage drag, Supabase auth+RLS, admin panel | `src/app.jsx`, `src/store/store.jsx`, `src/auth.jsx`, `api/admin/` | `[LIVE]` |

---

## 6. Footage & CapCut

| Date | Feature | Key Files / Migration | Status |
|------|---------|----------------------|--------|
| 2026-06-24 | **OpenCut playback freeze FIXED + Replicate/Pexels wired (session j)** — the session-i blocker is resolved: "won't play/scrub" was a **speed-0 bug** in the fork `PlaybackManager` (normal `play()` ran at `speed = shuttleSpeed = 0` — only the shuttle controls ever set it), NOT a decode hang. Fix: play at 1× when `shuttleDirection === null`. Diagnosed via a reusable Playwright probe (canvas-pixel-hash + `playback-update`/`playback-seek` event counters over the `:8000→iframe :3000` embed) — verified live (`lastTime` 4.9→10.5 at 1×, canvas animates). Also wired the owner's **Replicate** token (`NEXT_PUBLIC_` → baked into `.next/static`) and brought **Pexels** image search live (restart loaded the saved key; Freesound no regression). **FORK committed + pushed** (`69842d6`); FB build green. | FORK: `apps/web/src/core/managers/playback-manager.ts`, `apps/web/.env.local` (Replicate token, gitignored). FB: `scripts/oc-embed-diag.mjs` (new reusable diagnostic) | `[LOCAL]` (working on localhost; fork pushed `69842d6`; NOT deployed — prod editor needs the Hetzner stand-up) |
| 2026-06-24 | **OpenCut Phase 2 — real-time collaboration INSIDE the OpenCut editor (BUILT + WORKING LIVE ON LOCALHOST)** — ported the single-writer lock + presence + live-doc-sync model into the self-hosted fork via the `oc-collab-phase2` workflow (4 disjoint teams, both builds green), then brought it working end-to-end on localhost: the **full OpenCut-AI editor renders embedded in FootageBrain, SSO-authenticated, saving to `oc_projects`, with a live collab bar**. Applied migs **0095+0096** (scoped one-off). Fixed Phase-1's never-run SSO (`initExternalAuth` was never called → auto-init + retry + adapter token-wait), made the embed **full-bleed** (+ Maximize/Fullscreen), `loadProject` **open-or-create**, **rate-limit fail-open** (was 500ing all AI routes), hid the OpenCut VC bar, wired **Freesound** (LIVE). Fork on a **prod build** `:3000` + FB dev `:8000`. **✅ playback freeze FIXED in session j (speed-0 bug); fork committed+pushed `69842d6`.** | FB: `supabase/migrations/0096_oc_locks.sql` (new), `src/app.jsx` (hostname-gated flag), `src/pages/editor.jsx` (full-bleed+maximize), `scripts/apply-oc-collab-migrations.mjs` (new). FORK: `lib/collab/oc-collab.ts` (new), `components/collab/{collab-bar,read-only-shield}.tsx` (new), `lib/external-auth/client.ts`, `components/providers/editor-provider.tsx`, `core/managers/project-manager.ts`, `lib/rate-limit.ts`, `components/editor/editor-header.tsx`, `services/storage/supabase-project-adapter.ts`. Plan `.claude/plans/clever-leaping-clover.md`, workflow `.claude/workflows/oc-collab-phase2.js` | `[LOCAL]` (working on localhost; NOT committed/pushed/deployed; migs 0095+0096 applied) |
| 2026-06-24 | **OpenCut-AI Phase 1 — SSO embed + shared timeline storage (A1+B1+C1)** — built via the `opencut-phase1-sso-storage` workflow across BOTH repos: FootageBrain can embed the self-hosted editor via iframe, SSO it with the user's Supabase JWT (parent-side **postMessage** `EDITOR_READY`→`FB_AUTH`, no OpenCut login, JWT never in URL, origin-pinned, 9s frame-blocked fallback), and persist projects to a shared **`oc_projects`** table so any teammate opens the same project. Media import deferred to **Phase 1B**. Whole-project build gate green (FB `npm run build` 11.1s · fork `bun run build:web` 1m8s, sequential). Gated behind `EDITOR_EMBED_ENABLED=false` (default OFF → native editor unchanged). Committed + pushed BOTH repos. **NOT deployed; mig 0095 unapplied; `editor.footagebrain.com` not stood up.** Full human-gated path in `docs/opencut-phase1-deploy.md`. | FB: `supabase/migrations/0095_oc_projects.sql` (new), `src/app.jsx` (flag+origin), `src/pages/editor.jsx` (`EmbeddedEditor`), `docs/opencut-phase1-deploy.md` (new) — branch `feat/opencut-collab-multitrack` `24e79fb`. FORK `opencut-ai-fb` `a797026`: `apps/web/next.config.ts` (CSP frame-ancestors), `packages/env/src/web.ts` (4 env vars), `lib/external-auth/{client,server}.ts` (new), `lib/auth/{client,server}.ts`, `services/storage/supabase-{client,project-adapter}.ts` (new)+`service.ts`/`types.ts`, `+@supabase/supabase-js` | `[STAGED]` (committed+pushed both repos, build-green, NOT deployed, flag OFF) |
| 2026-06-24 | **OpenCut-AI self-host fork — plan + Phase-0 spike (GREEN)** — investigated bringing the *real* OpenCut back: the old feature-rich editor was a real `opencut.app` **iframe**; the native collab timeline (`1fe26e0`/`2ecd68b`) replaced it. Owner chose to **self-host OpenCut-AI on Hetzner (~$0) + build collab INTO it** (AI deferred, separate fork repo, runtime-only integration via iframe+JWT+shared Supabase). Spike (in `C:/Users/Mi/Downloads/opencut-ai`, FootageBrain untouched): `bun install`+`bun run build:web` exit 0; `next start` → `/editor`=200 with no GPU/AI-backend/DB; iframe-able; client-side export. Next: formalize fork + Phase-1 workflow (SSO + Supabase storage adapter). | `.claude/plans/is-it-a-simple-goofy-kahan.md` (plan); spike in separate dir `C:/Users/Mi/Downloads/opencut-ai` | `[LOCAL]` (planning/spike — no repo code) |
| 2026-06-23 | **OpenCut — collaborative multi-track editor (SHIPPED LIVE end-to-end)** — project-level single-writer lock (`editor_locks.track_id='__project__'`) + live multi-viewer + presence ("Take control"/"Release control"; viewers read-only), a **Projects browser** (per-reel + freestanding), a **multi-track timeline** (video/audio/text-caption), **Whisper auto-captions**, **music/audio mixing**, **silence/filler trim**; editors self-serve draft renders. Wires up the dormant `0082_edit_collab` scaffolding. Built via the 7-team workflow `opencut-collab-multitrack.js`; `/code-review high` found+fixed 1 release-blocking camel/snake trim bug. **Shipped in 3 human-gated prod steps:** mig `0094` applied (scoped `exec_sql`, NOT bulk `migrate:apply`; fixed an apply-blocking `edit_sessions.updated_at`→`last_active` live-vs-repo drift); Hetzner `fb-backend` rebuilt (live `render.py` was still the Phase-0 stub → real filtergraph shipped first-time + new `edit_ai.py` `/api/edit/*`); `vercel --prod` of the full main+collab tree. ⚠️ Deployed from the `feat` working tree → `main` lacks the collab commits; `0094` `last_active` fix uncommitted. Follow-ups: ffmpeg calibration render + 2-session collab smoke. | `src/lib/editor-collab.jsx` (new), `src/pages/editor-projects.{jsx,css}` (new), `backend-handoff/edit_ai.py` (new, deployed), `backend-handoff/render.py` (deployed), `api/ai/suggest.js`, `src/store/store.jsx`, `src/components/editor/Timeline.jsx`+`timeline.css`, `src/pages/editor.jsx`+`.css`, `src/app.jsx`, `src/lib/permissions-catalog.js`, `supabase/migrations/0094_edit_collab_v2.sql` (**APPLIED**) | `[LIVE]` (`dpl_8VuqB1fqckEy5BtoGYR2bLqvD1MG`, www=200; branch `feat/opencut-collab-multitrack` `3dc625c`) |
| 2026-06-23 | **Music Library — Browse (genre/mood) · Favorites · Playlists** — grew the Music Library tab into a 4-view library: Browse the curated Epidemic genre(28)/mood(26) taxonomy, per-user ♥ Favorites, and create/rename/delete Playlists with a ＋Playlist menu on every track. Per-user (RLS mirrors `0070`). **NOT deployed; inert until calibration + 0093 applied** | `src/pages/music-library.jsx` (rewrite), `src/pages/music-library.css`, `src/store/store.jsx`, `supabase/migrations/0093_music_library.sql` (new, **NOT applied**) | `[LOCAL]` |
| 2026-06-23 | **Music Library — Epidemic Sound integration (proxy + tab + per-reel attach)** — server-side swap-ready proxy so editors use the owner's Epidemic subscription: Music Library tab (search/preview/licensed-download) + Attach Music card on reel detail (music = `reel_dna_assets` `asset_type='music'`). Built via workflow `epidemic-sound-music-library.js`; `/code-review high` found+fixed 3 bugs (reel_dna_assets CHECK, useReelDnaAssets hook, optimistic upsert). **🔴 BLOCKED: guessed host `api.epidemicsound.com` doesn't resolve → needs owner DevTools calibration. NOT deployed** | `api/ai/_epidemic.js` (new), `api/ai/suggest.js`, `src/store/store.jsx`, `src/app.jsx`, `src/lib/permissions-catalog.js`, `src/pages/detail.jsx`, `src/components/MusicPickerModal.jsx` (new) + `reel-assets.jsx` + `reel-dna-assets.jsx`, `supabase/migrations/0092_music_tracks.sql` (new, **NOT applied**) | `[LOCAL]` |
| 2026-06-21 | **CapCut editor — Phase 1: in-app visual timeline → real render (trim/cut/crossfade)** — new visual drag `Timeline.jsx` (reorder clips, edge-trim, toggle cut↔crossfade + duration, zoom, live total) inside a **Timeline⇄OpenCut** toggle; **Render draft** builds a `project_json`, submits to the worker, polls progress, and "Use as export link" feeds the review-queue. `render.py` `_build_filtergraph` now does real Phase 1 ffmpeg (per-clip trim + concat for cuts + xfade/acrossfade for crossfades + ffprobe clamp + silence for audio-less clips). `verifyOwner` added to the render proxies. Fixed a TDZ crash (`view` read in a hook dep array before its `useState`). Migration 0084 applied. Frontend deployed `dpl_7KpcQmXgYZAMdynKzz8qa94qppzc`; **worker NOT yet deployed → Render button 502s until the Hetzner build** | `src/components/editor/Timeline.jsx`/`timeline.css` (new), `src/pages/editor.jsx`/`.css`, `backend-handoff/render.py`, `api/ai/suggest.js` | `[LIVE]` (frontend, `1fe26e0`) / `[LOCAL]` (worker) |
| 2026-06-21 | **Attached footage — removed Analyze + Preview; 📁 country chip links to the folder** — on a reel's attached clips, the Analyze + Preview buttons were removed; the `📁 country` chip is now a link that opens Footage search pre-loaded with all of that country's clips (new `footageFolderPath()` derives the country-folder abs path) | `src/components/AttachedFootageList.jsx`, `src/lib/footage-brain-client.js`, `src/pages/detail.jsx` | `[LIVE]` (`5d7a9a7`) |
| 2026-06-21 | **CapCut editor — Google Drive SA wired, render worker fully live** — `_download_drive_file()` stub replaced with real Drive API v3 via `google-api-python-client` (`run_in_executor`); `GOOGLE_SERVICE_ACCOUNT_JSON` set in Hetzner `.env`; `/api/render/health` → `{"ok":true,"drive_configured":true}` | `backend-handoff/render.py`, `backend-handoff/requirements-hosting.txt` | `[LIVE]` |
| 2026-06-21 | **CapCut-style editor — Phase 0: render pipeline + collab foundation (Hetzner live, editor.jsx still dirty)** — migrations `0082`/`0083` applied to prod Supabase; render worker deployed at `api.footagebrain.com/api/render/*`; `editor-presence.jsx` committed (dead code, activate in Phase 3); ffmpeg filtergraph is passthrough stub; `editor.jsx` dirty pending OpenCut fork | `src/lib/editor-presence.jsx` (dead/unwired), `backend-handoff/render.py`, `api/ai/suggest.js`, migrations `0082_edit_collab.sql` / `0083_render_jobs.sql` | `[LIVE]` (Hetzner) / `[LOCAL]` (editor.jsx) |
| 2026-06-08 | **Vision footage tagging** — Auto-tags footage via free OpenRouter vision models; tags stored in `vision_tags` jsonb column; pipeline lane-desync fix | `src/pages/footage-library.jsx`, migration `0026_vision_tags.sql` | `[LIVE]` |
| 2026-06-08 | **OpenCut editor tab + footage transcripts + beat-titles** — Embedded OpenCut editor; transcript storage per footage item; beat-title annotations; editor read-only permissions | `src/pages/editor.jsx`, `src/pages/editor.css`, migrations `0024_footage_transcripts.sql`, `0025_edit_sessions.sql` | `[LIVE]` |
| 2026-06-08 | **Edit sessions table** — Tracks who is editing what and when for the editor-lock feature | migration `0025_edit_sessions.sql` | `[LIVE]` |
| 2026-06-04 | **CapCut near-real-time tracking + project name from disk** — Agent reads active CapCut project name from OS; splits sessions by project; near-real-time heartbeat | `tools/capcut-agent/capcut_agent.py`, `public/capcut-agent/capcut_agent.exe` | `[LIVE]` |
| 2026-06-04 | **CapCut activity monitor (private, localhost-only)** — Background Python agent polls CapCut process and POSTs heartbeats to Supabase; install.bat for team | `tools/capcut-agent/`, `public/capcut-agent/`, migration `0019_capcut_activity.sql` | `[LIVE]` |
| 2026-06-04 | **Footage card: Drive link + folder/country label** — Footage cards show Google Drive link + country/folder label; filename persists without a migration | `src/pages/footage-library.jsx`, `src/lib/footage-brain-client.js` | `[LIVE]` |
| 2026-06-04 | **Footage attach: filename typeahead + folder browser** — Attaching footage uses a searchable typeahead; folder browser available; fixed card thumbnails | `src/components/AttachedFootageList.jsx`, `src/components/FootageBrainSearch.jsx` | `[LIVE]` |
| 2026-06-04 | **Activity tab: clickable per-day drill-down** — Each day row in Activity expands to show time log, projects worked, and hourly breakdown | `src/pages/activity.jsx` | `[LIVE]` |
| 2026-06-04 | **Reel title editing + create-time persistence fix** | `src/pages/detail.jsx`, `src/store/store.jsx` | `[LIVE]` |
| 2026-06-04 | **FootageBrain search URL: environment-aware** — Search URLs resolve at runtime (not build time) so prod and dev hit the right endpoint | `src/components/FootageBrainSearch.jsx` | `[LIVE]` |
| 2026-06-04 | **CORS fix: drop Content-Type on GET calls** — GET requests to FootageBrain API no longer send Content-Type, avoiding CORS preflight failures | `src/lib/footage-brain-client.js` | `[LIVE]` |
| 2026-05-15 | **Footage library: visual/caption/multimodal search** — Three search modes against the footage index; preview link fix | `src/pages/footage-library.jsx`, `src/components/FootageBrainSearch.jsx` | `[LIVE]` |

---

## 7. AI Generate & Search

| Date | Feature | Key Files / Migration | Status |
|------|---------|----------------------|--------|
| 2026-06-21 | **Footage search — type-to-filter Folders combobox + removed Multimodal/Caption/Visual modes** — the Folders dropdown is now a searchable combobox (type a country to filter, click to browse); the Multimodal/Caption/Visual search modes were removed (left: Semantic/Keyword/Hybrid/Filename/Folders). Part of the reverted-fixes restore | `src/components/FootageBrainSearch.jsx` | `[LIVE]` (`5d7a9a7`, `dpl_CJ2okt…`) |
| 2026-06-03 | **Generate: country scoping (parallel, cached)** — Country-scoped footage retrieval runs search+folder lookup in parallel; file listing cached; auto-detects country from footage tags | `src/pages/idea-generator.jsx`, `api/generate.js` | `[LIVE]` |
| 2026-06-03 | **Generate: multi-model (Puter + OpenRouter) + Quick mode** — Model picker (Claude/Puter/OpenRouter); Quick mode generates faster with reduced detail; dedup prevents duplicate footage picks; Drive links on output cards | `src/pages/idea-generator.jsx`, `api/generate.js` | `[LIVE]` |
| 2026-06-03 | **Generate: full publish pack output** — AI output expanded to full production pack (script, hashtags, description, music cue, hook) instead of just a draft | `api/generate.js` | `[LIVE]` |
| 2026-06-03 | **Generate: Supabase cross-device history sync** — Generation history persisted in `generated_drafts` table; syncs across devices | migration `0013_generated_drafts.sql` | `[LIVE]` |
| 2026-06-03 | **Coverage tab** — Shows which topics/territories are covered vs. gaps in the footage library | `src/pages/coverage.jsx` | `[LIVE]` |
| 2026-06-03 | **AI Generate tab** — First version of the idea generator tab with Claude-backed script generation | `src/pages/idea-generator.jsx` | `[LIVE]` |
| 2026-06-03 | **Mobile responsive layout** — Global mobile CSS pass; key pages usable on phone | `src/pages/` CSS files | `[LIVE]` |
| 2026-05-15 | **Visual/Caption/Multimodal footage search** — Three search modes (visual similarity, caption text, combined) via FootageBrain API | `src/components/FootageBrainSearch.jsx`, `src/lib/footage-brain-client.js` | `[LIVE]` |

---

## Database Migrations (through 0079)

Schema history in order. All applied to the Supabase `kjruhbaahqkuajseoojn` project.

| # | Migration | Area |
|---|-----------|------|
| 0001 | `init.sql` | Core schema |
| 0002 | `auth_and_people.sql` | Auth + people table |
| 0003 | `realtime.sql` | Supabase realtime subscriptions |
| 0004 | `reel_blueprint.sql` | Reel cards core schema |
| 0005 | `reel_detail_blob.sql` | Reel detail JSONB blob |
| 0006 | `sla_timestamps.sql` | SLA tracking timestamps |
| 0007 | `rename_people.sql` | People table rename |
| 0008 | `archive.sql` | Archive flag on reels |
| 0009 | `attached_footage.sql` | Footage attachments to reels |
| 0010 | `stage_canonicalize.sql` | Canonical stage values |
| 0011 | `reset_reels.sql` | Reset/seed reels |
| 0012 | `delete_stuck_seed_reels.sql` | Cleanup stuck seeds |
| 0013 | `generated_drafts.sql` | AI Generate history |
| 0014 | `app_settings.sql` | App-wide settings table |
| 0015 | `update_jay_email.sql` + `fix_jay_email.sql` | Team member email fix |
| 0016 | `clear_stale_user_ids.sql` | Auth cleanup |
| 0017 | `sync_canonical_people.sql` | People sync |
| 0018 | `service_role_people_access.sql` | RLS workaround for service role |
| 0019 | `capcut_activity.sql` | CapCut session tracking |
| 0020 | `reel_extensions.sql` | Extra reel fields |
| 0021 | `daily_tasks.sql` | Daily task checklist |
| 0022 | `resources.sql` | Resources rows (links/docs) |
| 0023 | `footage_framerate.sql` | Framerate field on footage |
| 0024 | `footage_transcripts.sql` | Transcript storage |
| 0025 | `edit_sessions.sql` | Editor lock sessions |
| 0026 | `vision_tags.sql` | AI vision tags jsonb |
| 0027 | `social_connections.sql` | OAuth social accounts |
| 0028 | `social_connection_health.sql` | Connection health checks |
| 0029 | `locations.sql` | Filming locations table |
| 0030 | `youtube_oauth_note.sql` | YouTube OAuth config note |
| 0031 | `whatsapp_messages.sql` | WhatsApp message log |
| 0032 | `whatsapp_social_connection.sql` | WhatsApp connection row |
| 0033 | `locations_row_color.sql` | Color coding for locations |
| 0034 | `resource_row_color.sql` | Color coding for resources |
| 0035 | `location_reel_links.sql` | Location ↔ reel linkage |
| 0036 | `location_photos.sql` + `daily_tasks_notes.sql` | Location photos + task notes |
| 0037 | `processing_jobs.sql` | Background job queue |
| 0038 | `resource_row_hidden.sql` | Hide/show resource rows |
| 0039 | `ai_brain.sql` | AI Brain FAQ pairs |
| 0040 | `match_faq_pairs_rpc.sql` | Vector similarity RPC |
| 0041 | `faq_vector_1024.sql` | 1024-dim vector column |
| 0042 | `workflow_insights.sql` | Daily AI suggestions |
| 0043 | `anthropic_killswitch.sql` | Claude enable/disable flag |
| 0044 | `reel_dna.sql` | Reel DNA gene schema |
| 0045 | `reel_dna_timeline.sql` | Reel DNA timeline markers |
| 0046 | `reel_chat_refs.sql` | Chat message ↔ reel links |
| 0047 | `training_progress.sql` | Per-user training progress |
| 0048 | `reel_skill_tags.sql` | Skill tags on reels |
| 0049 | `demo_sandbox.sql` | Demo sandbox isolation |
| 0050 | `gamify.sql` | XP + gamify core |
| 0051 | `gamify_rubric_per_row.sql` | Per-row rubric scores |
| 0052 | `reel_gamify_difficulty.sql` | Difficulty rating on reels |
| 0053 | `rubric_revamp.sql` | Gold-standard rubric schema |
| 0054 | `attached_footage_realtime.sql` | Realtime on footage attachments |
| 0055 | `training_module_content.sql` | Training module content blobs |
| 0056 | `daily_tasks_sort_order.sql` | My Work task drag-reorder ordinal (APPLIED 2026-06-17) |
| 0057 | `reel_series.sql` | Series/playlist tag on reels (APPLIED 2026-06-17) |
| 0058 | `reel_dna_location.sql` | Location tag column on reel_dna for the inspiration spreadsheet (APPLIED 2026-06-17) |
| 0059 | `monitor_events.sql` | Pulse monitor events (algo/news feed) — owner+service RLS, realtime (APPLIED 2026-06-17) |
| 0060 | `monitor_sources.sql` | Pulse owner-curated RSS/Atom feed list for auto-ingest (APPLIED 2026-06-18) |
| 0061 | `monitor_events_dedup_full_index.sql` | Swap partial dedup index → full unique index so ingest upsert ON CONFLICT resolves (APPLIED 2026-06-18) |
| 0062 | `reel_dna_deleted_at.sql` | Reel DNA permanent-delete tombstone (`deleted_at` + index) so the IG poller stops resurrecting deleted reels (APPLIED 2026-06-18) |
| 0063 | `thumbnail_dna.sql` | YouTube thumbnail capture/catalog (separate `thumbnail-dna` feature) (APPLIED) |
| 0064 | `monitor_events_geo.sql` | World Monitor: geo cols (lat/lng/event_type/metric) + `source_type` 'geo'; reuses the 0061 FULL dedup index (APPLIED 2026-06-18) |
| 0065 | `monitor_event_links.sql` | World Monitor: polymorphic event→reel/review_card/location link table + FULL unique index + owner/service RLS (APPLIED 2026-06-18) |
| 0066 | `world_monitor_settings.sql` | World Monitor: seeds `app_settings.world_monitor` flags + `world_monitor_usage` counters (APPLIED 2026-06-18) |
| 0067 | `thumbnail_dna_video_id_uidx.sql` | FULL unique index on `thumbnail_dna.video_id` — the `ON CONFLICT` arbiter + anti-resurrection guard for the YouTube-playlist auto-ingest poller (APPLIED 2026-06-18) |
| 0068 | `reel_dna_assets.sql` | Reel DNA unified Assets: polymorphic `reel_dna_assets(reel_dna_id, asset_type, asset_id, label)` join table + FULL unique index (upsert arbiter) + team-wide RLS + realtime (APPLIED 2026-06-18) |
| 0069 | `monitor_events_team_read.sql` | Adds an authenticated SELECT policy to `monitor_events` so attached News is readable team-wide (writes stay owner-only) (APPLIED 2026-06-18) |
| 0070 | `user_preferences.sql` | First **per-user** prefs table `user_preferences(person_id, key, value)` (vs owner-only `app_settings`); self-RLS via `people.user_id` + owner-all; composite-PK upsert arbiter. Keys: `pipeline_collapsed`, `pipeline_hidden_lanes` (APPLIED 2026-06-18) |
| 0071 | `reel_dna_platform_fb.sql` | Defensive CHECK `platform IN ('ig','tiktok','yt','fb')` — adds Facebook Reels; the column never had a real constraint before (APPLIED 2026-06-18) |
| 0072 | `reel_dna_content_type.sql` | `reel_dna.content_type` (reel/carousel/photo/unknown) for IG-carousel poller groundwork (APPLIED 2026-06-18) |
| 0073 | `ig_sync_runs.sql` | IG DM reconciliation: one row per poller run (seen/inserted/dedupe/graph_errors/reconciled counters); team-read RLS + service-write + realtime, no unique index (append-only) (APPLIED 2026-06-18) |
| 0074 | `ig_ingest_log.sql` | IG DM reconciliation: per-message NON-happy-path reason log (issue_type + detail), FK→ig_sync_runs ON DELETE CASCADE; same RLS/realtime template (APPLIED 2026-06-18) |
| 0075 | `reel_dna_content_type_extend.sql` | Extend `content_type` CHECK to `reel/carousel/photo/story/video/unknown` so the poller can capture carousels/photos/stories (APPLIED 2026-06-18) |
| 0076 | `rls_hardening.sql` | Back UI permission gating with RLS: `people` priv-esc fix (no authenticated INSERT + BEFORE UPDATE trigger pinning role/id, service_role exempt), owner-only reel/card/task DELETE (keeps 0049 demo predicate), `attached_footage_items` `using(true)`→authenticated, `reel_dna` writes→owner-or-`captured_by`-self (service_insert/owner_all kept) — APPLIED 2026-06-19 (§1/§3/§4; §2 deferred) |
| 0077 | `fix_yt_fb_content_type.sql` | One-time `UPDATE reel_dna SET content_type='video' WHERE platform IN (yt,fb,tiktok) AND content_type IS NULL/unknown AND deleted_at IS NULL` — retro-fix so YT/FB DM rows filter under "Video"; leaves `platform='ig'` rows alone; idempotent (APPLIED 2026-06-19; 4 YT + 2 FB rows fixed). ⚠ Collides with untracked `0077_training_quiz_attempts.sql` — renumber the training one to 0078 |
| 0078 | `training_quiz_attempts.sql` | Training active-learning: per-editor quiz best-score table backing the inline `Quiz`/`QuizEditor` MVP (parallel-session work) (WRITTEN, NOT applied) |
| 0079 | `reel_dna_longform.sql` | Reel DNA Phase 0 longform deconstruction: idempotent `ADD COLUMN IF NOT EXISTS` for `format`('short'/'long'), `media_status`(idle→pending_analyze→analyzing→analyzed/analyze_failed), `source_url_resolved`, `narrative`(jsonb), `progress`(jsonb), `media_error`, `analyzed_at` + `reel_dna_media_status_idx`. `narrative` is MACHINE-ONLY — never the human `timeline`. No RLS change (existing reel_dna policies cover new cols) (APPLIED — catalog-confirmed `format`/`media_status` present 2026-06-20) |
| 0081 | `reel_dna_assets_pacing.sql` | Reel DNA Phase 1: idempotent `ADD COLUMN IF NOT EXISTS asset_manifest jsonb, pacing jsonb` on `reel_dna` — the downloadable layer index (H3) + cut-pacing metrics (H4) the worker writes. Additive/nullable, no RLS/index change (reuses 0079's `reel_dna_media_status_idx`). (APPLIED — catalog-confirmed both cols present 2026-06-20; STEP 1.5 "absent" was a stale PostgREST schema cache, fixed via `NOTIFY pgrst`) |
| 0082 | `edit_collab.sql` | CapCut editor collaboration tables: `edit_projects` (live timeline JSONB, draft\|template), `edit_project_versions` (manual checkpoints), `editor_locks` (track-level soft locks, PK on `(project_id, track_id)`, 30s TTL). Full unique index on `edit_projects(reel_dna_id)`. RLS uses `auth_is_owner()` SECURITY DEFINER from 0076. ALTER PUBLICATION for realtime on all 3 tables. (APPLIED 2026-06-21) |
| 0083 | `render_jobs.sql` | CapCut editor render pipeline: `render_jobs(id, reel_dna_id, project_id, project_json, render_mode, status queued\|rendering\|done\|failed, progress, output_url, output_bytes, error, submitted_by, created_at, completed_at, updated_at)`. Python does single-flight check; DB has regular index on `reel_dna_id` as safety net only. RLS: owner manages, authenticated views. ALTER PUBLICATION for realtime. (APPLIED 2026-06-21) |
| 0084 | `daily_tasks_color.sql` | `ALTER TABLE daily_tasks ADD COLUMN IF NOT EXISTS color TEXT` — one of the 8 app tone names or NULL, backs the My Work → Tasks color picker. (APPLIED 2026-06-21 via `/update-migrations`; 0 pending after) |
| 0080 | `perf_indexes.sql` | Pure-additive perf indexes backing the store's windowed boot queries (15-concurrent-user scaling): partial `(created_at DESC) WHERE deleted_at IS NULL` on `reel_dna` + `thumbnail_dna`; bare `(created_at DESC)` on `reel_dna_assets`, `monitor_event_links`, `attached_footage_items` (`monitor_events` already indexed by 0059). All `CREATE INDEX IF NOT EXISTS` (idempotent, no schema/RLS change). **APPLIED 2026-06-21** via a single-file one-off (NOT `migrate:apply`, which would have swept the unready 0084). Fills the `0080` gap slot. |
| 0087 | `planable_pushes.sql` | Push-to-Planable audit/idempotency table (render_jobs-style RLS). **APPLIED 2026-06-21** (single-file `exec_sql`). |
| 0088 | `planable_media.sql` | Planable media phase: private `reel-videos` bucket + storage RLS + `planable_pushes.media_path`/`media_deleted_at` + cleanup index. **APPLIED 2026-06-21** (single-file `exec_sql`). |
| 0089 | `reel_dna_row_color_favorite.sql` | Reel DNA spreadsheet row tagging: `ADD COLUMN IF NOT EXISTS row_color TEXT` (8-tone name or NULL) + `favorite BOOLEAN NOT NULL DEFAULT false`. Additive, no RLS change (existing `reel_dna` policies cover the new cols). **APPLIED** (live ledger shows `[ applied ]` as of 2026-06-22). |
| 0090 | `planable_pushes_grouping.sql` | Push-to-Planable grouping phase: additive nullable cols on `planable_pushes` — `campaign_id text`, `group_id text`, `page_ids text[]`, `batch_id uuid` + indexes `(reel_id,batch_id)`,`(campaign_id)`. RLS INHERITED from 0087 (no policy DDL). Recording grain → one row per (reel, batch). **APPLIED 2026-06-22** (single-file `exec_sql` one-off; left the unrelated `0086_perf_samples` pending; columns verified present). |

---

## Deployed & Committed (as of 2026-06-24)

**2026-06-24 — OpenCut-AI Phase 1 (SSO + shared timeline storage) COMMITTED + PUSHED both repos, build-green, NOT deployed.** Executed the `opencut-phase1-sso-storage` workflow (decisions A1+B1+C1): embed the self-hosted OpenCut-AI editor via iframe, SSO via the user's Supabase JWT (parent-side postMessage `EDITOR_READY`→`FB_AUTH`, no OpenCut login), persist projects to a shared `oc_projects` table. Media import deferred to Phase 1B. Verified the whole-project integration build gate SEQUENTIALLY (never parallel → no `.next` clobber): FootageBrain `npm run build` ✅ 11.1s, then fork `bun run build:web` ✅ 1m8s (`/editor/[project_id]`+`/projects` built, exit 0). The workflow shipped code + env but **no deploy doc** → wrote `docs/opencut-phase1-deploy.md` (grounded in the fork's real `Dockerfile`/`docker-compose.yml`/`next.config.ts` CSP). Committed each repo with explicitly-staged files — FB tree is dirty with unrelated WIP, so staged ONLY the 4 Phase-1 files (never `git add -A`); the fork tree is 100% Phase-1 so `git add -A` was safe there. **FB** `feat/opencut-collab-multitrack` `24e79fb` → `origin/Zi-flow` (new remote branch). **Fork** `opencut-ai-fb` `main` `a797026` → `origin/opencut-ai-fb`. **Gotchas:** fork `upstream` push is hard-disabled (`DISABLE_PUSH_TO_UPSTREAM`) and `origin`=owner's private fork → push safe (always `git remote -v` first); the storage adapter leaves `oc_projects.owner` NULL on write (injected JWT `sub` is the auth-user UUID but `owner` FKs to the TEXT `people.id` slug — writing the UUID hard-fails the FK; RLS is team-wide so `owner` is attribution, not a boundary); framing lives in code not Caddy (emit `frame-ancestors` CSP, NEVER `X-Frame-Options`). **Flag `EDITOR_EMBED_ENABLED=false` (default OFF → native editor unchanged).** Human-gated path (in `docs/opencut-phase1-deploy.md`): DNS → apply 0095 (scoped one-off) → fork on Hetzner (throwaway PG + web + 4 env vars) → Caddy site block → direct smoke → flip flag + `vercel --prod` LAST → embedded SSO smoke. Memory: `project_opencut-selfhost-fork`, `current-state`.

**2026-06-23 — OpenCut collaborative multi-track editor SHIPPED LIVE end-to-end (`dpl_8VuqB1fqckEy5BtoGYR2bLqvD1MG`).** Took the branch-only collab editor fully live in three human-gated prod steps the owner authorized in turn ("0094 + Hetzner worker", then "ship to the live site"). **(1) Migration 0094 — applied scoped, not bulk.** `npm run migrate:apply` would have also fired the held-back `0086`/`0092`/`0093`, so applied ONLY 0094 via a one-off mirroring `migrate.mjs applyOne()` (`exec_sql` RPC + `schema_migrations` upsert). First attempt hard-failed: the DB5 backfill ordered by `edit_sessions.updated_at`, but the live `edit_sessions` was created MANUALLY (0025 header says so) and has **no `updated_at`** — its real cols include `last_active` (live-vs-repo catalog drift, rule #8b; Postgres validates the column ref at plan time even on an empty table). `exec_sql` is atomic (one plpgsql txn) so it rolled back fully — fixed `updated_at`→`last_active`, re-applied, verified new cols/`edit_ai_jobs` exist + recorded. **(2) Hetzner `fb-backend` rebuilt.** Discovered the live `render.py` was still the **Phase-0 passthrough stub** (editor-render-phase1's real `_build_filtergraph` was never deployed) → this shipped the real filtergraph + audio amix + caption burn-in for the first time, plus new `edit_ai.py` (Whisper captions/silence `/api/edit/*`). Verified the snapshot was a true superset of live (shared helpers byte-identical) before replacing; container already had ffmpeg + faster-whisper + 8 caption fonts (no image-dep change); `/api/edit/*` needed no Caddyfile handle (rides the existing nginx `/api/` proxy). Built from the correct compose (`deploy/hetzner/docker-compose.yml`, project `footagebrain` — NOT the stale test-root compose), swapped container, verified clean boot + health 200 + in-container sha == source. **(3) `vercel --prod`** of the full main+collab tree — flagged that the whole-tree deploy also ships the accumulated `2ad08ee` work (Planable grouping [0090 applied], Reel-DNA tweaks, analyze-button fixes; Music Library co-ships **hidden/inert**); owner chose "ship full main + collab". Live: www.footagebrain.com=200, fresh bundle, backend health 200. ⚠️ Deployed from the `feat` working tree → **`main` lacks the collab commits** and the `0094` `last_active` fix is uncommitted. Follow-ups: ffmpeg calibration render + 2-session collab smoke; re-commit `0094` + merge `feat`→`main`. Updated memory `reference_migration-tracking-drift` (scoped-apply + live-shape-drift), `project_opencut-collab-multitrack`, `current-state`; CLAUDE.md rule #8(d).

**2026-06-21 — Reel DNA spreadsheet batch SHIPPED LIVE (`dpl_53XjnpzdMdbT9koUgPH7wfVNSXpj`, full-tree deploy, commit `694e0c6`).** Four Reel-DNA enhancements (hide-columns+freeze, → Pipeline editor-picker, all-platform in-app link preview + visited tracking, ↩ DNA send-back) planned via `/qa-verified-plan`, built via generated workflow `.claude/workflows/reel-dna-cols-and-multi-editor.js`, hardened against live testing, verified on `npm run dev`. **No migration** (live `user_preferences` 0070 + existing cols). Committed only the 7 reel-dna files + docs (explicit staging — never `git add -A`, pushed to origin/main). **Deploy:** the tree was shared/dirty (a parallel Planable-grouping effort created `0090_planable_pushes_grouping.sql` + edited `suggest.js`/`export-view.jsx`/`_planable.js` during the session); surfaced both traps to the owner (full-tree ships that unverified WIP; isolated reverts live Planable phase-2) → **owner chose the full-tree deploy**, so the Planable-grouping WIP also shipped to prod (now deployed-but-uncommitted — needs the Planable owner to commit + verify). New memories `project_reel-dna-spreadsheet-pipeline-batch`, `reference_sticky-freeze-needs-bounded-scrollbox`.

**2026-06-19 — Reel-share picker auto-defaults to the OPEN Rocket.Chat channel LIVE (isolated deploy).** The Team-chat "Share a reel into chat" picker's target-channel dropdown now defaults to whichever RC channel the owner currently has open in the embedded chat and follows it live as channels are switched (was always `#pipeline`, manual every time); a manual pick still wins until the next successful send, then it resumes following. Planned via `/qa-verified-plan` (Explore agents mapped the chat UI + reel-attach flow; a Plan agent confirmed the cross-origin mechanism) → plan `.claude/plans/moonlit-wiggling-leaf.md`. **Mechanism:** the chat is a **cross-origin iframe** (`https://chat.footagebrain.com`), so the dashboard can't read its open channel from the DOM — used Rocket.Chat's **iframe-integration "send" API**: with `Iframe_Integration_send_enable` ON (owner enabled it this session), RC `postMessage`s a `room-opened` event. `TeamChat` adds a `window` `message` listener (origin-gated, tolerates object-or-JSON-string payloads, filters `event === "room-opened"`, accepts only real channels/groups `t ∈ {c,p}` — ignores DMs `d`/omnichannel `l`) → `openRoom` state → `ReelSharePicker` auto-follows via an effect guarded by a `userOverride` flag; a `channelOptions` memo merges the open/manual channel so `<select>` never points at a missing option. Degrades silently to the old `#pipeline` default if the toggle is ever off. **Single file:** `src/pages/team-chat.jsx` (+ module const `RC_ORIGIN`). **Deploy:** owner chose base = branch HEAD (the committed nav-reorg Parts A–C) + only this file. Isolated per `reference_isolated-deploy-dirty-tree`: `git stash -u` → `git checkout stash -- src/pages/team-chat.jsx` → build green (822 mods, 9.62s) → `vercel --prod` (`dpl_HZKJvncgi2Uj6e9anyiBuSC86GW5`, READY, aliased www.footagebrain.com) → reset file to HEAD + `git stash pop` → drop redundant stash. **The committed nav-reorg shipped live with this deploy**; none of the ~58 dirty WIP files shipped. **Friction:** (a) a **parallel session is actively editing this tree** — `team-chat.jsx` was externally reverted twice mid-session (new files `pipeline.jsx`/`reel-dna-view.css`/`0080_reel_group_id.sql` appeared), so edits had to be re-applied and re-grepped before build; (b) the build's prebuild regenerated `migrations.manifest.json` (blocked `stash pop` → reset to HEAD) and the stash's untracked set "already existed" on disk (parallel session kept them) so only tracked WIP restored — net result a fully-restored tree + a redundant stash safe to drop. New memory `reference_rc-iframe-room-opened`. **Change is LIVE but uncommitted** in the restored working tree.

**2026-06-19 — Reel DNA Phase 0 "Longform Story" deconstruction engine BUILT (gate-green), NOT deployed.** The flagship reverse-engineering engine's lowest-risk slice, sourced from the owner's `obsidian-vault/REEL DNA/` spec (Master Plan / Automation Blueprints / Workflows / DB Migrations / Build-Difficulty ranking) and planned at `.claude/plans/luminous-frolicking-pumpkin.md`. **What it does:** flag a capture `format='long'` → owner clicks **Analyze** → `store.analyzeReelDna(id)` optimistically sets `media_status='pending_analyze'` + fires a thin fire-and-forget Vercel proxy (`api/ai/suggest.js?action=deconstruct`, a byte-for-byte `ig-sync` clone, no new fn) → a new Hetzner FastAPI worker (`backend-handoff/reel_deconstruct.py`) atomically claims the row (status-guarded PATCH, concurrency=1), runs `yt-dlp` audio-only + `--write-auto-sub` native captions (faster-whisper fallback), then ONE free-OpenRouter **narrative** pass behind a `run_narrative(segments, *, model)` provider seam (Claude swap later), and service-role-writes `narrative`+`progress`+`media_status` back → the **Story panel** (`reel-story-panel.jsx` + pure `reel-narrative.jsx` helpers) renders hook/arc/open-loops/emotion-sparkline/retention-flags/payoff-CTA/0–100 scorecard live via realtime. **Built via `/workflow`:** Wave 1 = T-DB (migration `0079`) ∥ T-API ∥ T-STORE (additive on the shared-dirty `store.jsx`) ∥ T-WORKER; Wave 2 = T-UI — each a Senior Architect + adversarial QA under locked disjoint file ownership. **Gates:** centralized `npm run build` green (822 mods → 12.3s), per-role Playwright smoke PASS (zero uncaught page errors; only the pre-existing `reel-dna.jsx:1197` bookmarklet warning), short-format proven zero-regression. **Decisions:** Phase 0 only (Phase 1 reel/asset track deferred); free LLM now + seam for Claude; "I write all code incl. the worker, owner deploys." **Human-gated, NOT run:** apply `0079` to live DB, set `REEL_DECONSTRUCT_SECRET` (Vercel + Hetzner), deploy the worker per `backend-handoff/REEL-DECONSTRUCT-DEPLOY.md` (yt-dlp/ffmpeg/faster-whisper + `*/2` cron + router register), `vercel --prod`. **Tree is a shared multi-thread WIP** (parallel session's inbox/training/ingest + 0077/0078) — touched ONLY the 6 owned files; do NOT full-tree deploy until that work is verified. New memory `feedback_shared-tree-keep-building`.

**2026-06-19 — Inbox AI reply suggestions (✨) + YT/TikTok reply scaffold DEPLOYED (isolated).** Picked from the Obsidian roadmap as the highest functionality-per-effort item ("biggest time-saver not yet built"); planned via `/qa-verified-plan` (Architect/Backend/Frontend + adversarial QA fixed 3 issues pre-code: incompatible single-thread payload vs `monitor.js`'s array-only `extractJson`; a "no demo gate" rec that contradicted `generate.js`; auto-fetch-on-load that would double the shared free-OpenRouter burn). **Feature:** opt-in **✨ Suggest replies** per unanswered thread → 2-3 on-brand drafts → click seeds the **editable** compose box (human always edits + Send; AI never auto-sends). Folded into `api/ai/monitor.js` `suggest_replies` mode (**0 new Vercel fns**; demo-gated via `classifyCaller`; always-200 degrade). `replyToThread` got YT/TikTok branches (`{pending:true}` "pending business verification" until the Hetzner endpoint ships). **Deploy was the hard part:** `git status` showed a 3-thread WIP tree (inbox + an already-committed unapproved nav reorg on `reorg/tabs-monitor-roles` + an uncommitted training rebuild + migrations 0077/0078) — and `vercel --prod` ships the ENTIRE tree. Isolated it: `git stash -u` everything → `git switch -c deploy/ai-inbox main` → `git checkout stash@{0} -- <only the 4 inbox files>` → restored manifest/`change-log.md` drift back to `main` → build green → `vercel --prod` (`dpl_2efqqQ5C2HKi75nFg9chgVD1EXUm`, aliased www) → `git switch reorg/…` + `git stash pop` → **verified `git diff <dropped-stash>` empty = zero data loss**. Commit `75c44c6` lives on `deploy/ai-inbox`. **Held back (NOT deployed):** the nav reorg, training rebuild, migrations 0077/0078, backend `ig_webhook.py`. Recipe captured in memory `reference_isolated-deploy-dirty-tree`. **Follow-ups:** merge `deploy/ai-inbox`→`main`; not visually verified in prod (needs a non-demo session — demo gets `{}`).

**2026-06-19 — YT/FB DM links → `content_type='video'` DEPLOYED (all 3 layers), NOT committed.** Planned via `/qa-verified-plan`. Three independent bugs all produced the same symptom — YT/FB DM links stored as `content_type='unknown'`/`NULL`, invisible to the spreadsheet **Type** filter: (1) backend webhook `_classify_content_type(attach_type)` had no URL fallback (native `media_share` of a YT/FB link → `'unknown'`); (2) backend polling share-loop, same gap; (3) frontend `createReelDnaCapture` never set `contentType`. Secondary: `platformFromUrl()` had no Facebook branch (drifted from backend `_detect_platform`). Fix: `_classify_content_type` gained `url_override` → `_content_type_from_url()` (also +tiktok); `contentTypeFromPlatform()` helper in `store.jsx`; FB branch added client-side; migration `0077_fix_yt_fb_content_type.sql`. **Deployed in safe order:** `npm run migrate:apply` (verified live via one-off service-role script: 4 YT + 2 FB rows → video, 0 remaining) → Hetzner `scp`+`docker compose build/up` (diffed remote-vs-local first; `/api/ig/status` `ingest_enabled:true`) → `vercel --prod` (`dpl_86jsjcns5SobFNLeDgmnD3qHXevP`). Completes the `d930896` cross-platform thread (that fixed **platform**; this fixes **content_type** for native shares + retro data + manual form). **Working tree dirty + UNCOMMITTED** — and shares the tree with unrelated carried-over restructure/training work (`app.jsx`, `monitor-hub.jsx`, `training/`, a colliding `0077_training_quiz_attempts.sql`); commit selectively + renumber the training migration to 0078.

**2026-06-19 — Cleanup batch COMMITTED (`d930896`), NOT deployed.** Housekeeping pass via `/qa-verified-plan` → `/workflow` (3 disjoint components, one wave, adversarial QA each; plan `.claude/plans/i-have-a-big-logical-pixel.md`). **C1** `ig_webhook.py`: cross-platform IG-DM ingest — `_detect_platform(url)` (yt/fb/tiktok/ig from host, was hardcoded `ig`) on all 3 inserts + **plain-text URL capture** in the no-`shares` branch (a pasted YouTube/Facebook link was silently dropped because the poller only acted on `shares` edges) + webhook `content_type` parity. **C2** `store.jsx`: `persistUpdateReelDna` now maps `contentType`→`content_type` (in-app content_type edits were lost on reload). **C3** new `0076_rls_hardening.sql`: closes the `people` privilege-escalation hole (BEFORE UPDATE trigger pins role/id; an RLS `WITH CHECK` role-guard is a no-op since it only sees NEW), owner-only DELETE on reels/cards/tasks (keeps 0049 demo predicate), `attached_footage` `using(true)`→authenticated, `reel_dna`→owner-or-`captured_by`-self. Integration build green (8.71s). Local `main` fast-forwarded to `d930896`. **Deferred by owner:** Discord→RocketChat alert. **BLOCKED this session:** `npm run migrate:apply` (live DB) AND `git push origin main` (default branch) were both refused by the safety classifier despite a verbal "do these steps" — they need explicit per-action authorization or a `Bash` allow rule (surfaced, not routed around). **Pending human-gated:** apply 0076 (`/update-migrations`), deploy C1 to Hetzner + run the empirical YT/FB DM test, `vercel --prod` for C2 (verify locally first), push `main` (15 commits unpushed).

**2026-06-18 — IG DM reconciliation + non-reel content-type ingest + Check IG Sync button LIVE.** Three IG asks via `/qa-verified-plan` → `/workflow` on branch `ig-ingest-reconcile-contenttype`. (A) Reconciliation: new `ig_sync_runs` (0073) + `ig_ingest_log` (0074) tables, **IG Sync Health** panel (seen vs accounted, mismatch badge, amber coverage caveat) + owner Discord alert (`?action=ig-sync-alert`). (C) The Hetzner poller now captures **carousels/photos/stories** (composite `external_ref={mid}:{i}`), classifies `content_type` (0075 += story/video); new **Type** column/filter. (B) `docs/ig-crosspost-feasibility.md` writeup. Plus a **🔎 Check IG Sync** button (`reloadIgSync()`) showing what landed + errors by type. **Migrations applied to live DB; frontend deployed (`vercel --prod`); Hetzner poller deployed + verified** (scp + `docker compose build/up`; `/api/ig/status` ingest_enabled:true; first run reconciled, surfaced `graph_errors 3` = IG's flaky conversations edge, subcode 99). Committed `e4e7823` + `0b244d9`, pushed; prior local Reel DNA batch committed `f24421f` on `main` as the clean baseline. **Tree clean + in sync with origin.** Follow-ups: Discord notify is broken (owner fixing) → then set `APP_BASE_URL` on Hetzner for the mismatch alert; branch not yet merged to `main`. Lesson: prod-backend SSH writes + live DB migrates are human-gated by the safety system — they required explicit owner authorization this session (a blanket `Bash(*)` allow doesn't cover hard-to-reverse production actions).

**2026-06-18 — Training embed-persistence fix + "Edit the training manual" permission LIVE.** Two changes built inline (modest scope), `npm run build` green (815 modules), **user-verified on localhost**, then `vercel --prod` → `dpl_GGuS8FEC5azMBNf7twphPuGkv2Sm` (816 modules, aliased www). (1) The Training-manual YouTube **Embed** toggle now **persists** — was ephemeral `useState` in `YoutubeEmbedLink`; now saved per (module, field) as a `"<fieldPath>::embed"` JSON-array sibling row in `training_module_content` (no migration), shows for every editor. `linkifyText`/`EditableText` gained optional `embeddedUrls`/`onToggleEmbed`; threaded via `getEmbedProps()`. (2) New **`editManual`** action capability — Roles & permissions → Actions row, default Off for editor roles (owner always allowed); `training.jsx` `canEdit = can("editManual")` (was `me?.role === "owner"`). Full-tree deploy → any other uncommitted working-tree edits also shipped. **Tree uncommitted** — nothing committed this session.

**2026-06-18 — Daily-use batch 2 LIVE (6 items via /qa-verified-plan).** Organized a backlog screenshot of hand-written notes, ran `/qa-verified-plan` (3 domain agents + 1 adversarial QA agent, 1 QA loop), implemented the approved layered plan, applied migrations 0070–0072 to the live DB, and `vercel --prod` (815 modules, green, aliased www). Six changes: **(1) Pipeline reel collapse persists per-user** (new `user_preferences` table 0070; ReelCard reads store not local `useState`; tighter `.reel.collapsed` chip; per-lane hide also DB-persisted) — QA forced a per-user table since `app_settings` is owner-write-only, and hydration into a **separate auth-keyed effect** so a missing table can't brick boot. **(2) Discord notifications** on move-to-`in_progress` + `sendBack` — `suggest.js?action=discord-notify` (no new Vercel fn), fire-and-forget with Bearer JWT, owner sets webhooks + All/Owner-only mode in Roles Admin → `app_settings.discord_config`. **(3) Training clickable links + YouTube click-to-embed** — `linkifyText()`/`YoutubeEmbedLink` scoped to `training.jsx` `ProseBlock` read-only path (NOT the shared `EditableText`). **(4) Facebook Reels platform** in the Reel DNA dropdown (0071 defensive — no real CHECK existed). **(5) Tasks notes drag-select fix** — `onMouseDown stopPropagation` on both my-work.jsx textareas. **(6) IG-carousel groundwork** — `content_type` column (0072) + `reelDnaToDb` mapper; the actual Hetzner poller change is still pending. **Owner action:** paste Discord webhook URLs in Roles Admin before pings fire (silent no-op until then). The whole working tree was committed (3 commits on `bugfix-daily-use-batch`) and fast-forward-merged into `main` (now 9 ahead of origin, unpushed), then re-deployed `vercel --prod` → current prod `dpl_7JRsTwTMDGPEMfqSTD7mn1Dy8X6a` (811 modules), aliased www.footagebrain.com READY.

**2026-06-18 — Reel DNA unified Assets system LIVE.** Every reel card now has an **Assets** column — four collapsible sections (**Footage / Locations / Thumbnails / News**), collapsed by default with a per-category **count badge**, expandable on demand — plus an **"Assets →"** heading that opens an **in-app full-screen page** with all four expanded and a **"Pull from pipeline reel"** seed button; the spreadsheet view gets an **Assets count cell** opening the same page. Backed by a new **polymorphic `reel_dna_assets`** join table (attach *any* asset to *any* card on demand — chosen over a pipeline-bridge because QA verified almost no `reel_dna` cards carry a `reel_id`, which would have shown 0/0/0/0 everywhere) with a **FULL unique index** as the upsert arbiter, team-wide RLS, and realtime; News opened team-wide via a new authenticated SELECT policy on `monitor_events` (0069). Built via **`/qa-verified-plan` → `/workflow`**: three components under locked file ownership — **T-SCHEMA** (migrations), **T-STORE** (`store.jsx`: hydrate/realtime + `attachAsset`/`detachAsset`/`seedAssetsFromPipeline` + pure `resolveReelDnaAssets`/`assetCountsForReelDna`), **T-UI** (4 new components + hook + page + `reel-dna.jsx`/`.css` wiring) — each QA-signed. A session-limit interruption truncated T-UI mid-wiring; resumed and completed the `reel-dna.jsx`/`.css` wiring. Migrations 0068/0069 applied (`migrate:apply`); `reel_dna_assets` verified live (rows=0); `npm run build` green (811 modules) → `vercel --prod` (deploy `dpl_G5rkbTJFjcFnWJP9Yu4GpUSY6aJt`, READY, aliased www.footagebrain.com). **Code-review not run.** Tree uncommitted on `bugfix-daily-use-batch`.

**2026-06-18 — Comprehensive views for Pulse + Reel DNA LIVE.** Two daily-use view upgrades, both **pure UI** (no DB/schema changes), deployed together (`dpl_6Wh8G87Je8PDgE2ovXQM2k4BgJ5E`). **Pulse:** the Classic feed now has a persisted **layout switcher** — Timeline / Cards (magazine) / Table (sortable) / Board (Kanban by severity) — and **clicking any row inline-expands** into a detail editor to add/remove **tags** + flip severity/status/star/archive/delete (tags were creation-only before). New `src/components/pulse-comprehensive.jsx`; `updateMonitorEvent` already carried `tags`, so zero store work. **Reel DNA:** the Classic spreadsheet gained a **per-column filter row** (text per column + Source/Status selects + clear), and a new **Classic ⇄ Comprehensive** toggle adds a **faceted rail** (Platform/Status/Source/Genes multi-select w/ counts + global + location search) with a **Grid ⇄ Gallery** sub-toggle that **reuses** the existing `DnaTable`/`DnaCard`. New `src/lib/reel-dna-filters.jsx` + `src/components/reel-dna-comprehensive.jsx`. Layout direction was confirmed via AskUserQuestion before coding; the page↔component **circular import** resolves via hoisted function declarations. `npm run build` green (806 modules) → `vercel --prod`, aliased www. **Code-review skipped** (owner's call); tree uncommitted on `bugfix-daily-use-batch`.

**2026-06-18 — World Monitor hybrid LIVE.** Pulse gained a **Classic⇆World toggle** (world-events feed: USGS/FIRMS/ACLED + an embedded worldmonitor.app map; events link to reels/review-cards/locations) + a Monitor **"World Monitor"** limits card; paid APIs (Finnhub/FRED/IMF/NASDAQ/flights) ship OFF as `app_settings` flags. **HYBRID — not self-hosted** (worldmonitor is AGPL-3.0 + built for Vercel Edge/Railway/Upstash Redis, infeasible on the 4 GB Hetzner box; the full dashboard blocks framing, only `embed.html` allows it). Ingest engine `api/ai/_world-feeds.js` folded into `suggest.js?action=world-ingest` (no new Vercel fn). Migrations 0064/0065/0066 applied; `npm run build` green; `vercel --prod` (deploy `dpl_5jtsJt7WK3gtW6BZJo8PENnFmUpN`, READY, aliased www). USGS works keyless; FIRMS/ACLED await env keys + a Hetzner cron. **Code-review skipped** (owner's call). Tree uncommitted on `bugfix-daily-use-batch`.

**The full working tree is now LIVE on www.footagebrain.com.** This session ran `vercel --prod` from branch `bugfix-daily-use-batch`, which deploys the entire tree at once — clearing the long-standing "build green but not deployed" backlog. Shipped live together: the **3D DNA helix landing**, the **Reel Inspiration Library** (Reel DNA tag-note + Cards⇄Spreadsheet), the **daily-use batch** (series/playlist grouping, duplicate reel, card readability/collapse, Leroy→CTO), the **`/space`** cinematic expansion (owner-only), and the **training pillar**. Migrations 0056/0057/0058 applied. The tree was then committed + pushed to `bugfix-daily-use-batch`.

**2026-06-18 — Pulse automated news monitor shipped.** The owner-only Pulse tab (manual feed, migration 0059) plus **automated RSS ingestion** (Sources manager, `api/ai/_rss.js` ingester via `suggest.js?action=news-ingest`, Hetzner cron /30m, 60-day prune, Monitor health card) are LIVE. Migrations 0059/0060/0061 applied; committed `4455424`; deployed `vercel --prod`. Both Hetzner cron lines moved to `www` (apex 308-redirects API routes). Commit is on `bugfix-daily-use-batch`, not yet pushed/merged.

**2026-06-18 — Instagram-DM-to-spreadsheet ingest LIVE (via poll).** "DM a reel (+ tag note) to @paulvictortravels → it auto-logs to the Reel DNA spreadsheet" is working in prod. **Webhooks were abandoned** (Instagram only delivers real-DM webhooks once the app is *Published + App-Review'd*; dev mode emits only the synthetic Test). Pivoted to **polling the Business-Suite IG inbox**: `GET/POST /api/ig/sync` on Hetzner reads the Page's IG conversations, pulls each shared reel's permalink from `shares.data[].link`, pairs the adjacent tag-note text, and dedups into `reel_dna` (`source='ig_dm'`). 15-min Hetzner cron; dedup pre-check → ~0 steady-state Supabase writes; 33 reels captured. Committed `01046fd`; `vercel --prod` (frontend unchanged). Handler `ig_webhook.py` on Hetzner; src copy in `backend-handoff/`.

**2026-06-18 — Training-manual linkify + YouTube embed everywhere LIVE.** URLs pasted into **any** training module section (not just the 3 prose blocks) now render as clickable cyan links, and YouTube URLs get an inline **Embed** toggle → a small **260px right-aligned** 16:9 player. Extracted `linkifyText`/`YoutubeEmbedLink` into a new shared `src/lib/linkify.jsx`; added an opt-in `linkify` prop to `EditableText` applied to **both** its read-only and **editable-not-editing** branches (the key fix — owner is always `canEdit`, so a read-only-only treatment was invisible to them, which is why the first pass "showed nothing"); link/embed clicks `stopPropagation` so they don't trigger edit mode. `npm run build` green → `vercel --prod` (`dpl_3kuHzejE9WtFg3eTceZK2LEpqG6h`, READY). ⚠ This was a **full-tree** deploy, so the previously-LOCAL Reel DNA comprehensive overhaul shipped to prod alongside it. **Edits uncommitted** (16 M + `linkify.jsx`).

**2026-06-18 — Pulse feed-URL diagnostics LIVE.** The Pulse → Sources Add-source form now validates a pasted URL server-side and shows *why* it isn't a usable feed plus verified one-click "Use this" replacements (`validateFeedUrl()` in `api/ai/_rss.js`, exposed via `suggest.js?action=validate-feed` — no new Vercel fn). Built after diagnosing "no new articles after refresh" → root cause was 3 of 5 sources being non-feed URLs (CNN homepage, IG/TikTok profiles), not an ingester bug. Build passed; deployed `vercel --prod` (aliased www.footagebrain.com); endpoint confirmed live. **Edits uncommitted** on `bugfix-daily-use-batch`. The owner's 3 broken sources are still in the live DB — replace them via the new flow.

**2026-06-18 — Reel DNA Refresh / hard Delete / Send-to-Pipeline LIVE.** The Reel DNA page gained a **↻ Refresh** button that force-runs the Hetzner IG-DM poller immediately (beats the 15-min cron) — store action `triggerIgSync()` → `POST /api/ai/suggest?action=ig-sync`, which calls Hetzner `/api/ig/sync` server-side with `IG_SYNC_SECRET` kept off the browser (folded into the suggest route for the 12-fn cap) — then `reloadReelDna()` + two delayed reloads. Plus a **hard Delete** (no confirm, for mass-delete; Archive stays as the restorable option) and a card **production-brief** block + **→ Send to Pipeline** button that creates a linked `reels` row from the card (`reelDnaToPipelineFields`). Fixed a **`reel_dna_reel_id_fkey` FK race** by persisting the reel before the link (sequential). Added Vercel env `IG_SYNC_SECRET` (Production + Development) + `.env.local`. Deployed `vercel --prod` (READY). **Edits uncommitted** on `bugfix-daily-use-batch`. ⚠ Check the Pipeline for an **orphan reel** left by the user's pre-fix Send attempt.

**2026-06-18 — Two Reel DNA bug fixes LIVE (committed).** (1) The ↻ Refresh **"IG sync failed (500)"**: the `?action=ig-sync` handler in `api/ai/suggest.js` was only in the working tree and **never deployed**, so prod fell through to the daily Claude generator → Vercel Hobby ~10s timeout → bare non-JSON 500, **and it billed `ANTHROPIC_API_KEY` on every click**. Fixed by deploying the handler + an **unknown-action 400 guard** (so no future action can silently hit the Claude path) + an **8s `AbortController`** making the Hetzner call truly fire-and-forget. Key gotcha: **adding a Vercel env var does NOT redeploy** (the `IG_SYNC_SECRET` had been added 25m earlier but the code reading it wasn't live). (2) **Deleted reels resurrected**: the Hetzner poller dedupes on `external_ref` of *live* rows, so a hard `DELETE` let the next poll re-insert the same DM. `deleteReelDna` is now a **soft-delete tombstone** (`deleted_at`, migration `0062`) — keeps the row so the poller skips it, hides it from every view (load queries + a realtime `deleted_at`→remove rule). "Still coming back after the fix" turned out to be a **stale browser bundle** (deployed bundle == local build, all DB rows `deleted_at: null`). Two `vercel --prod` deploys; committed on `bugfix-daily-use-batch`.

**2026-06-18 — Display & accessibility toggle LIVE + text-size buttons fixed.** The owner-only **Display & accessibility** panel (avatar menu) is now deployed on footagebrain.com. This session fixed its **"Text size" buttons that "sometimes" did nothing**: they were gated behind Comfortable mode AND only changed root `font-size`, which the px-everywhere app ignores. Rebuilt as a CSS **`zoom`** on `<body>` — a true percentage that scales every pixel, **independent of theme** — with 8 steps (**80/90/100/110/125/150/175/200%**), and widened the Font picker 3→6 (**Inter/System/Serif/Rounded/Mono/Readable**; Rounded=lazy Nunito CDN @font-face). `src/theme-accessible.css`, `src/lib/theme.jsx` (default scale `"100"`, still emits no attr at default), `src/components/PreferencesModal.jsx`. `npm run build` green → `vercel --prod` (deploy `dpl_2Jat5Ajzv18qUeEPEq6gAUd6aD2E`, READY). This deploy also ships the originally-built Comfortable-mode layer. The 3 `theme*` files remain git-untracked + tree uncommitted on `bugfix-daily-use-batch`.

**Still pending (does not block prod):**

- **Push to Planable phase 3 (grouping/media/title/posting-time) — DEPLOYED LIVE (`dpl_Ab5JtD1Wdwofi47opLziga5yxpfn`, 2026-06-22) but UNCOMMITTED.** Working tree (Planable + permissions): `M api/ai/suggest.js`, `M src/pages/detail.jsx`, `M src/pages/export-view.jsx`, `M src/lib/permissions.jsx`, `M api/monitor/migrations.manifest.json`, `?? api/ai/_planable.js`, `?? supabase/migrations/0087+0088+0090`. Live but no git ref == live → **commit when ready**. Mig **0090 APPLIED**. **⚠ Calibration push is the open verification:** phase 3 swapped the two-step `/media` for **direct `media:[url]`** per the OpenAPI (contradicts the phase-2 empirical two-step finding) — push 1 reel and confirm video attaches; if it goes up text-only, restore a corrected two-step (`mediaUrls` array). The `planable-cleanup` Hetzner cron is still **not wired** (human-gated).
- **Reel DNA spreadsheet row tagging — COMMITTED + LIVE 2026-06-21; `0089` now APPLIED** (live ledger `[ applied ]` as of 2026-06-22). Star/color persist.
- **MicroSaaS Scout → FootageBrain integration — PLAN NEXT (2026-06-20).** Scout is built + live as a standalone product (separate repo `C:/Users/Mi/Downloads/microsaas-scout`, own Supabase, 97 products). Next session: plan an owner-gated **"Scout" view + a button next to Pulse** in `src/pages/monitor-hub.jsx` (read the Scout Supabase via a 2nd supabase-js client), **daily auto-refresh** via a Hetzner cron hitting the Scout backend `POST /scrape-all`, and **deploy the Scout Python backend to Hetzner behind a Caddy route** (human-gated, same `/reels/*` pattern). The Scout repo itself is **uncommitted** (offer an initial commit). New untracked-in-FootageBrain files: `.claude/workflows/microsaas-scout-build.js`, `.claude/plans/i-want-to-make-bubbly-squid.md`. See memory `project_microsaas-scout`.
- **Pipeline grid card-view — LIVE (2026-06-20), commit pending.** The 2×2/3×3 toggle is deployed (`dpl_AsWJvdpdC1hdPBURJee3AAG9YFDL`) but `src/pages/pipeline.jsx` + `src/components/components.jsx` + `src/styles.css` are modified + **uncommitted** on `clean/prod-baseline-2026-06-19`; `git add` + commit when ready. **Deferred follow-up (user-flagged):** the **connecting-lines** feature (lines between cards that should follow on drag; owner wants Canva-style elbow connectors) is **NOT in this branch** — first locate the branch it lives on (`git grep` across branches for leader-line/connector/SVG-path), then plan the drag-follow fix. A pre-existing quirk: grid drag-reorder insertion uses cursor-Y only (`pipeline.jsx:~490`), loose in a horizontal row.

> **2026-06-19 — the dirty tree is now PARKED.** The items below that were "uncommitted/local-only" are now preserved on branch **`wip/parking-2026-06-19`** (recoverable via `git checkout wip/parking-2026-06-19 -- <path>`), and the working tree is clean on `clean/prod-baseline-2026-06-19`. Bring each parked feature forward per-feature when ready. Human-gated: owner runs `/update-migrations` to reconcile the ledger, and may fast-forward `main` → the baseline + push.

- **`/space` hyper-real overhaul + Scene Studio (parked on `wip/parking-2026-06-19`)** — all `src/components/space/*` (new: `BinaryBlackHole.jsx`, `AmbientEvents.jsx`, `Skydome.jsx`, `SpaceControls.jsx`, `space-audio.js`; reworked: `Sun.jsx`, `NeutronStar.jsx`, `SpaceBattle.jsx`, `Nebula.jsx`, `Galaxy.jsx`, `celestial-shared.js`, `RubikCube.jsx`), `src/lib/{space-cube-config,space-scene-params}.jsx`, `src/pages/space3d.{jsx,css}`, `vite.config.js`, plus new deps `@react-three/postprocessing@2.19.1`+`postprocessing@6.37.0` (`package.json`/`-lock`) + capture scripts `scripts/shot-space*.mjs`/`shot-studio.mjs`. Build green, zero page errors. **Next (user-flagged):** fix cube interaction (scroll lag, columns expand too late, time freezes on entry, no in-column movement/drag); realism+flow polish; gate heavy fbm shaders into `mid`/`low` QUALITY tiers. Ship via an isolated branch off `main` only.
- **Playwright smoke harness uncommitted (2026-06-19)** — `scripts/smoke-screenshot.mjs` + `@playwright/test` devDep (`package.json`/`package-lock.json`) + `.gitignore` edits are local only. Commit (coherent unit) or stash before any full-tree `vercel --prod`. Optional follow-ups: extend to a per-role multi-tab smoke test; fix the `javascript:` bookmarklet warning at `src/pages/reel-dna.jsx:1197`.
- **✅ DONE 2026-06-19:** migration `0076 §1/§3/§4` applied + verified; C1 deployed to Hetzner; C2 deployed to Vercel; `main` + `ig-ingest-reconcile-contenttype` pushed. (An RLS recursion incident from 0076 was found + fixed live — `238759b`.)
- **0076 §2 (owner-only DELETE on reels/cards/tasks) — DEFERRED.** It assumed `0049_demo_sandbox`'s schema (which never ran). Rewrite against the live `"auth write reels/cards/tasks"` policies (split → team INSERT/UPDATE + owner-only DELETE, no `is_demo_user`), apply, verify from a non-owner session. **Next-session item.**
- **Demo sandbox (`0049`) — DEFERRED until owner revisits.** Now `[pending]` + `DO-NOT-BULK-APPLY` guard header; do NOT let `migrate:apply`/`/update-migrations` build it blindly.
- **Empirical YT/FB DM test** (still pending) — now that C1 is live: DM yourself a YouTube + Facebook link as plain text → 🔎 Check IG Sync → inspect `ig_ingest_log` (insert vs `skipped_no_link` reveals whether Meta Graph returns the DM text body — the one unverified assumption).
- **Local curl-TLS quirk** — the Bash env's `curl` can't TLS to `*.footagebrain.com` (exit 35) though node HTTPS works; verify prod health from the Hetzner box or a node client. **Next-session item.**
- **Prune ~12 `.claude/worktrees/agent-*` worktrees + stale branches** from prior workflow runs. **Next-session item.**
- **(Deferred) Discord→RocketChat alert** — owner chose to defer. Backend already has `chat.postMessage` via admin token in `backend-handoff/reel_chat.py` to reuse when revisited.
- **Reel DNA bug-batch (7 fixes) — LIVE** (this session, `dpl_7yr9At3svKX7V74N7XKz41DkejwB`, aliased www.footagebrain.com). Owner: re-verify on prod, especially #6 (Send-to-Pipeline migration with all 4 asset types) and #2 (no duplicate assets after reload).
- **Re-verify the Reel DNA Comprehensive-only overhaul ON PROD** (owner action) — it went **LIVE incidentally** via the 2026-06-18 full-tree training deploy, without its own separate sign-off. Walk the checklist on the live site (column filters narrow, ⤢ Card modal attaches assets, thumbnails/map/footage links render, detach ✕ sticks, delete stays gone). The `unified_cards` flag + Classic view are gone — Comprehensive is the only Reel DNA view. **Next phase (user-flagged):** merge the Unified card with the Pipeline `ReelCard` into one shared format.
- **Commit the working tree** — 16 modified files + new `src/lib/linkify.jsx` are uncommitted on `main`. Includes this session's training linkify work AND the Reel DNA overhaul (both now live). Commit + push when ready.
- **Enter Discord webhook URLs** (owner action) — the Discord-notify feature is LIVE but silent until the owner pastes each member's webhook URL into **Roles Admin → Discord notifications**. No URLs = graceful no-op.
- **Push `main` to origin** — the working tree is now **committed on `main`** (9 commits ahead of origin/main, tree clean) but **not pushed**. `git push` when ready.
- **Reel DNA — WAVE 1 LIVE (2026-06-20), Phase 1 activation pending.** Wave 1 longform Story FE committed (`8b5eeb4`) + deployed (`dpl_5VkNyLPG`, www.footagebrain.com); Wave 2 reel FE shipped inert/read-safe. **Phase 1 activation (human-gated, per `backend-handoff/DEPLOY-PHASE1.md`):** apply the reachability fix (`unified-dna-card.jsx:259` un-gate Analyze) → MERGE-deploy the live Hetzner worker (scp→diff→merge reel branch) → apply `0081` → set `FB_DOWNLOAD_SIGNING_SECRET` (byte-identical Vercel⇄Hetzner) → `vercel --prod` → HMAC parity vector → calibrate one reel. Branch not pushed.
- **IG sync missing-reels fix — DONE + LIVE (2026-06-19), commit pending.** Poller fixed so it paginates each thread's full history (was capped at 50 msgs) and no longer drops a whole conversation on a heavy-payload 500 → `reel_dna` ig_dm **74 → 393**. `backend-handoff/ig_webhook.py` is **modified locally (matches deployed) but uncommitted**, and `scripts/ig-sync-diagnose.mjs` is new/untracked — `git add` + commit when ready; consider syncing the change into the private `footagebrain-backend` repo. **Follow-up (offered, not built):** a Page-token health pre-check (token expiry is the most likely future cause of missing reels). Server backup at `…/ig_webhook.py.bak.20260620`.
- **IG carousel/photo/story poller — DONE (2026-06-18).** The Hetzner poller now iterates all `shares.data[]`, classifies `content_type` (0075 += story/video), and logs reconciliation to `ig_sync_runs`/`ig_ingest_log`. Deployed + verified. **Follow-up:** live-verify non-reel capture by DMing a NEW carousel/story/photo from a 2nd account; the IG mismatch Discord alert needs `APP_BASE_URL` on Hetzner once the broken Discord notify is fixed; consider merging `ig-ingest-reconcile-contenttype` → `main`.
- **FB Reels auto-ingest** — only the manual `'fb'` platform option shipped; Graph-API polling (like the IG inbox) is a future project.
- **World Monitor — activate fires + conflicts:** set `FIRMS_MAP_KEY`, `ACLED_KEY`, `ACLED_EMAIL` (Vercel + `.env.local`) and add the Hetzner cron `*/30 * * * * curl -s "https://www.footagebrain.com/api/ai/suggest?action=world-ingest&secret=…" > /dev/null` (USGS already works keyless). Confirm **ACLED commercial-use terms** (free for non-commercial only).
- **Rotate IG secrets pasted in chat (prior session):** `IG_APP_SECRET` + the 2 Instagram access tokens — regenerate in the Meta dashboard, update Hetzner `deploy/hetzner/.env`, restart backend.
- **Delete the one leftover `(debug — no reel url)` row** from the Reel DNA spreadsheet (a Test-event artifact).
- **Rocket.Chat config (owner action, no code):** set Leroy's role to reflect **Owner** (currently `admin`); disable **self-assignment** of the Owner role — both in `chat.footagebrain.com` admin.

**Next step:** owner enters Discord webhook URLs → `git push` origin/main → rotate the IG secrets/tokens.

---

*Last updated: 2026-06-24 (wrap-up #67 — **OpenCut playback freeze FIXED + Replicate/Pexels wired + FORK committed/pushed**. The session-i blocker is resolved: "won't play/scrub" was a **speed-0 bug** in the fork `PlaybackManager` (normal `play()` ran the playhead at `speed = shuttleSpeed = 0` — only shuttleForward/Reverse ever set it), NOT a decode/render hang. Fix: play at 1× when `shuttleDirection === null`. Diagnosed empirically with a reusable Playwright probe (`scripts/oc-embed-diag.mjs`) — a 64×36 canvas-pixel-hash sampler + counters on the `playback-update`/`playback-seek` window events over the real `:8000→iframe :3000` embed; an in-range frame-step scrub test ruled out a decode hang. Verified live (`lastTime` 4.9→10.5 at 1×, canvas animates, "NO FREEZE"). Wired the owner's **Replicate** token (`NEXT_PUBLIC_` build-time var → confirmed inlined in `.next/static`) and brought **Pexels** image search live (the prod-server restart loaded the saved key; `/api/images/search` returns photos; Freesound no regression). **FORK committed + pushed** (`69842d6` → `origin/opencut-ai-fb main`: playback fix + all session-i Phase-2 runtime fixes + collab dirs). FootageBrain repo committed in the wrap-up doc batch. **NOT deployed to prod** — `vercel --prod` would not surface the fix (the fork isn't on prod; prod `EDITOR_EMBED_ENABLED` is OFF); the prod editor cutover remains the human-gated Hetzner stand-up (`docs/opencut-phase1-deploy.md`), deferred to the pro-features session. New memory `reference_opencut-playback-speed0-freeze`.)*

*Previously (#66 — **OpenCut Phase 2 collaboration BUILT + WORKING LIVE ON LOCALHOST**. Ran the `oc-collab-phase2` workflow (single-writer lock + presence + live-doc-sync ported into the self-hosted fork, wired to `oc_projects` over Supabase Realtime; 19 agents, both builds green; first run interrupted mid-Reconcile → resumed via `resumeFromRunId`), then brought it working end-to-end on localhost: applied migs **0095+0096** (scoped one-off `scripts/apply-oc-collab-migrations.mjs`, owner-authorized) and fixed the cascade of Phase-1 runtime bugs that were never reachability-tested — **`initExternalAuth` was defined-but-never-called** (SSO dead) → auto-init+retry+adapter-token-wait; native-editor fallback → **full-bleed embed** (+Maximize/Fullscreen); **`Project not found`** → `loadProject` open-or-create (native `edit_projects` vs OpenCut `oc_projects` are different tables); **Freesound 500** → `checkRateLimit`/Upstash fail-open (was breaking ALL AI routes); switched fork to a **prod build** (`next start` works with `output:standalone`). Wired Freesound (LIVE), saved Pexels key, hid the OpenCut VC bar. **NOT committed/pushed/deployed.** OPEN BLOCKER: playback won't scrub → next session Playwright diagnose+fix. New memory `reference_opencut-sso-init-wiring` + `reference_opencut-localhost-embed-runtime`.)*

*Previously (#65 — **OpenCut collaborative multi-track editor SHIPPED LIVE end-to-end** (`dpl_8VuqB1fqckEy5BtoGYR2bLqvD1MG`, www.footagebrain.com=200). Three human-gated prod steps: **mig 0094 applied scoped** (`exec_sql` one-off, NOT bulk `migrate:apply` which would fire held-back 0086/0092/0093; fixed an apply-blocking `edit_sessions.updated_at`→`last_active` live-vs-repo catalog drift — `exec_sql` is atomic so the failed first try rolled back fully); **Hetzner `fb-backend` rebuilt** (live `render.py` was still the Phase-0 stub → real filtergraph + audio amix + caption burn-in shipped first-time + new `edit_ai.py` `/api/edit/*`; container already had ffmpeg/faster-whisper/fonts; `/api/edit/*` needs no Caddyfile handle — rides nginx `/api/`; built from the correct `deploy/hetzner/docker-compose.yml`, sha-verified); **`vercel --prod`** of the full main+collab tree (owner chose "ship full main + collab" — also took live the accumulated `2ad08ee` Planable/Reel-DNA/analyze-button work; Music Library co-shipped HIDDEN/inert). ⚠️ Deployed from the `feat` working tree → `main` lacks the collab commits + the `0094` `last_active` fix is uncommitted. Follow-ups: ffmpeg calibration render + 2-session collab smoke + re-commit/merge. Updated memory `reference_migration-tracking-drift` + CLAUDE.md rule #8(d).)*

*Previously (#64 — **Second-contributor onboarding — collaboration setup, no code/DB change.** Set the repo up to be co-developed by a friend on his own machine + own Claude Code account, **GitHub PR-based**, owner staying the sole deployer/migrator/Hetzner. Wrote a full handoff package + two-account collaboration plan (`.claude/plans/i-have-my-friend-validated-coral.md`) and committed two in-repo briefing docs so the friend's Claude gets them on clone (it has `CLAUDE.md` but NOT the owner's local memory folder/skills): **`docs/COLLAB-NOTES.md`** (rules — no deploy/migrate/SSH, `npm run dev`=LIVE prod DB, branch-per-feature into `main`, hot-file collision map `store.jsx`/`app.jsx`/`suggest.js`, migration-number coordination, RLS recursion trap, 12/12 fn cap, PR checklist) + **`docs/FRIEND-TASKS.md`** (the 6 known cross-account bugs with root-cause `file:line` — final-video `detail.jsx:941`, monitor `store.jsx:2003`, Scout client Headers TypeError, switcher `app.jsx:498`, role labels, name truncation — + feature backlog + in-flight context). Pushed to **`main` `8c45f5a`** via stash→checkout main→bring file→commit→push→checkout back→pop (kept the dirty wrap-up tree intact on the feature branch). Pruned **9 merged local branches**, keeping `clean/prod-baseline-2026-06-19` (rollback) + `feat/space-enhance` (worktree). Learned: GitHub Rulesets show "does not target any resources and will not be applied" until a **Target branch** is added; the friend's Claude needs durable gotchas **in-repo**, not in the owner's memory folder; merge-conflict model = same-exact-lines only, resolved on the incoming branch, never on `main`, with owner-only deploy as the safety net. **PENDING (owner GitHub UI):** invite the collaborator + arm the `main` ruleset. New memory `project_second-contributor-onboarding`.)*

*Previously (wrap-up #63 — **OpenCut → collaborative multi-track editor BUILT + committed to branch, NOT deployed.** Built the collaborative upgrade to the in-app editor — **project-level single-writer lock** (sentinel `editor_locks.track_id='__project__'`) + live multi-viewer + presence ("Take control"/"Release control"; viewers read-only, watch live), a **Projects browser** (per-reel + freestanding), a **multi-track timeline** (video/audio/text-caption), **Whisper auto-captions**, **music/audio mixing**, and **silence/filler trim**; editors self-serve draft renders. Wires up the dormant `0082_edit_collab` scaffolding (`edit_projects`/`editor_locks` + the never-imported `useEditorPresence`). **Plan-mode** (3 Explore + 1 Plan agent + 3 AskUserQuestion rounds → collab=project-lock, all 4 features, both project types, editors render drafts) → generated a **7-team disjoint-ownership workflow** via `/workflow-file-creation` (`.claude/workflows/opencut-collab-multitrack.js`, 9 frozen contracts, wave-ordered, adversarial QA per team) → ran ~1h → **independently re-verified** (npm build + node --check + py_compile + all 9 contracts wired) → `/code-review high` **found + fixed 1 release-blocking bug**: `render.py` dual-reads `start_at`/`startAt` but reads trim ONLY as snake `trim_in`/`trim_out`, while `buildProjectJsonV2` emitted camelCase `trimIn`/`trimOut` → every multi-track render silently dropped ALL clip trims (fixed `Timeline.jsx` to emit snake, matching sibling `buildProjectJson`). New migration **`0094_edit_collab_v2`** (RLS open owner→authenticated on `edit_projects`/`edit_project_versions`/`render_jobs` + `edit_projects` cols `reel_id` **TEXT-not-UUID** (reels.id is TEXT)/`thumbnail_url`/`last_editor`/`locked_*` + new `edit_ai_jobs` + `edit_sessions`→`edit_projects` backfill) — **NOT applied**. **Committed to a fresh branch `feat/opencut-collab-multitrack` (`2ecd68b`)** (15 files, 4847 ins; staged EXPLICITLY, left the prior session's uncommitted analyze-button-fix wrap-up docs untouched). Explained 0094's reversibility (fully additive → app & DB decoupled → app always revertible via `git checkout 7a95176`; "irreversible" overstated — it's a live prod action reversed via a compensating down-migration; peripheral/dormant tables bound the blast radius). Owner chose **commit-to-branch only**; mig 0094 + Hetzner (`render.py` MERGE + new `edit_ai.py` + caption fonts + Caddyfile `/api/edit/*`) + `vercel --prod` next session. New memories `project_opencut-collab-multitrack` + `feedback_stage-build-deploy-separately`.)*  
*Previously (#62 — **Epidemic Sound Music Library built, NOT deployed**. Whole session on the Music Library: a swap-ready server proxy (`api/ai/_epidemic.js` + 3 `?action=epidemic-*` branches; underscore helper keeps the 12/12 fn cap) + a Music Library tab (Search/Browse/Favorites/Playlists) + per-reel Attach Music (music reuses `reel_dna_assets` `asset_type='music'`). Built via the generated workflow `.claude/workflows/epidemic-sound-music-library.js` (~36 min), then `/code-review high` found+fixed 3 feature-breaking bugs (the `reel_dna_assets` CHECK didn't allow `'music'` → `0092` extends it; the shared `useReelDnaAssets` hook didn't pass `musicTracks`; `upsertMusicTrack` had no optimistic update), then expanded to Browse/Favorites/Playlists (mig `0093`). `npm run build` green throughout. **🔴 THE BLOCKER: the guessed private host `api.epidemicsound.com` does NOT resolve** (token is a private Keycloak user JWT, not the Partner key) → needs the owner's logged-in DevTools capture of the real search/download URLs to patch the `// CALIBRATION-REQUIRED` consts in `_epidemic.js`; partner host is the swap-ready fallback. `EPIDEMIC_TOKEN` set in `.env.local` + Vercel dev/prod env (exp ~2026-07-20). Local dev = `vercel dev --listen 3001` + `npm run dev` (:8000); `vercel dev` uses CLOUD env not `.env.local` (rule #5). Migrations `0092`+`0093` written NOT applied. Nothing committed/deployed. Owner pausing this topic.)*  
*Previously (#61 — **Push to Planable phase 3 SHIPPED LIVE** (`dpl_Ab5JtD1Wdwofi47opLziga5yxpfn`, www.footagebrain.com; mig `0090` APPLIED; files deployed-but-uncommitted). Owner's 4 asks fixed: (1) **grouping** — one push = one Planable **campaign** bundling all reels, each reel its **own grouped cross-page post** (`pageIds[]`→one `groupId`) across selected channels (was: separate card per platform; via `POST /campaigns` + `campaignId`); (2) **video attach fixed** — `media` is a **direct public-URL array** (deleted the broken two-step `/media` that used field `url`≠`mediaUrls` + a media id where a URL belongs); (3) **per-platform title** from the reel card title (`youtubeTitle`/`linkedinVideoTitle`/`pinterest.title`; caption stays logline); (4) **posting-time `<input type="time">`** threaded into `composeSchedule` (each reel keeps its own date). All verified vs the **live Planable OpenAPI**, built via `/qa-verified-plan` → `/workflow-file-creation` (`.claude/workflows/planable-grouping-fixes.js`, 3 disjoint teams + adversarial QA), build-gated green. Applied **only** 0090 via one-off `exec_sql` (left `0086_perf_samples` pending). Pre-deploy tree check caught an unrelated **`permissions.jsx`** change (removed Leroy "maya" full-access bypass) → owner-confirmed → shipped full-tree. **⚠ Calibration push pending** — the media direct-URL approach CONTRADICTS the phase-2 two-step finding; restore a corrected two-step if attach fails. Memory `project_push-to-planable` (phase 3 block) updated.)*

*Previously (#60 — **Reel DNA spreadsheet batch SHIPPED LIVE** (`dpl_53XjnpzdMdbT9koUgPH7wfVNSXpj`, commit `694e0c6`, full-tree deploy). Four Reel-DNA enhancements: (1) per-user **hide-columns + freeze header row & first column** (Columns menu; `.rd-table-wrap` made a bounded 2-D scrollbox so sticky actually pins — `overflow:auto` + `max-height:calc(100dvh-200px)`); (2) **→ Pipeline editor-picker** (send a captured card to chosen editors; first=linked reel, extras=`(FirstName)` copies; a mis-placed pipeline-card version was reverted); (3) **in-app reel preview popup** embedding IG/YouTube/TikTok/Facebook (YT+FB autoplay; fixed `.rd-preview`/`.rpv` class drift + `platformFromUrl` never returning `fb`) + per-user **visited-link underline**; (4) **↩ DNA "Back to Reel DNA"** reverse-of-send-to-pipeline (assets+text migrate back via `seedAssetsFromPipeline`+new `returnReelFromPipeline`, unlink/reset, soft-archive). Planned `/qa-verified-plan` → built via `.claude/workflows/reel-dna-cols-and-multi-editor.js`. No migration. **Deploy held** — parallel Planable-grouping WIP (`0090` + suggest/export-view/_planable edits) dirtied the tree; owner chose the **full-tree deploy**, so that WIP also shipped (deployed-but-uncommitted — Planable owner to commit/verify). New memories `project_reel-dna-spreadsheet-pipeline-batch`, `reference_sticky-freeze-needs-bounded-scrollbox`.)*

*Previously (#59 — **Push to Planable phase 2 SHIPPED LIVE** (`dpl_EfH9ageFLT2jwiFiwAUncEG4iHD4`). Completed the three follow-ups from #57: (1) **two-step async video attach** in `api/ai/_planable.js` (`POST /media` → bounded-poll `GET /media/{id}` until `status:"success"` → `POST /posts` with Planable's media id; poll-timeout → text-only fallback; never-publish preserved) + new `planablePostHasMedia` ingest-check export; (2) **final-video upload pipeline** — owner-only "Final video" MP4 upload on `src/pages/detail.jsx` → **private `reel-videos` bucket** (mig **`0088` APPLIED** single-file `exec_sql`: bucket + storage RLS + `media_path`/`media_deleted_at` + cleanup index) → persists `reel.mediaPath` → `?action=planable-push` mints a fresh signed url → new `?action=planable-cleanup` cron deletes the source ONLY after ingest is confirmed (safe-default keep-file) → new `?action=planable-upload-target` capacity check (Hetzner seam near ~80%); (3) **multi-account push** in `src/pages/export-view.jsx` — per-account checkbox dropdown + **"Select all accounts"**, fan-out one server call per ticked account (allow-list guard per-platform), per-reel × per-platform status. Built via `/workflow-file-creation` → `.claude/workflows/planable-media-upload.js` (3 disjoint-ownership teams + adversarial QA). Findings: **`vercel dev` reads platform env not `.env.local`** (rule #5) → `planable_config not set` locally, fine on prod; **Threads is unsafe** (only Threads channel is Nikky's — allow-list correctly excludes it); the method is **lossless up to Planable** (raw-byte upload, no transcode). Deployed-but-uncommitted; live `/media` calibration + Hetzner cleanup-cron wiring pending. New memory `reference_planable-media-upload-phase2`.).*  
*Previously (#58 — **Reel DNA spreadsheet row tagging BUILT (build-green), NOT deployed.** Added a leftmost Grid column with a ☆ **favorite star** + an **8-tone color dot** (My-Work `TaskColorDot` palette) per row; picking a tone **tints the whole `<tr>`** via `color-mix(in srgb, var(--c-TONE) 14%, transparent)` — store the tone NAME in `reel_dna.row_color`. That column's heading carries a **star filter** + **color filter**; **removed the Source & Type columns**; added an inline-editable **Notes** column (binds `quickNotes`) after Story / Pacing. Store maps `row_color`↔`rowColor` + pass-through `favorite`; comprehensive holds `favOnly`/`colorFilter` state. New migration **`0089_reel_dna_row_color_favorite.sql`** (`row_color TEXT` + `favorite BOOLEAN`) — **NOT applied**. **Two UX bugs fixed:** (1) the header color filter was gated on "colors in use" so it never appeared before any row was colored (and persistence is blocked pre-0089) → made always-on; (2) the popover was clipped first by `.rd-table-wrap`'s `overflow-x:auto` and then — even at `position:fixed` — by a transformed/sticky ancestor's containing block → fixed with a **React portal to `document.body`** (`useColorPopover` hook, outside-click checks BOTH trigger + portaled panel, closes on scroll/resize). Mid-session the IDE **reverted `reel-dna.jsx` + `store.jsx`** (companion files kept edits → nothing rendered) — re-applied from baseline. Verified on the dev server (localhost:8003); committing ONLY this session's 5 files. **Deploy decision pending** — the tree also carries unrelated Planable WIP and `vercel --prod` ships the whole tree. New memories `project_reel-dna-row-tagging` + `reference_portal-escape-overflow-clip`.).*  
*Previously (#57 — **Push to Planable SHIPPED LIVE.** Built the Export-tab → Planable scheduled-DRAFT push via plan + 3-team `push-to-planable` workflow, then set it up live: validated the owner's token against the real API (`api.planable.io/api/v1`), patched `_planable.js` to the confirmed spec, applied **migration 0087** (single-file `exec_sql`, not the all-or-nothing sweep), wrote `app_settings.planable_config` (owner's 6 page IDs only — the cross-posting allow-list for the SHARED "Nikky Kho" workspace that also holds another person's accounts), set `PLANABLE_API_TOKEN` (`.env.local`+Vercel), `vercel --prod` **`dpl_Dd9RGGqy5MvpUqoMzpTHSyofYuDX`**. A create+delete calibration draft proved `publishAtScheduledDate:false` never auto-publishes. **Discovered media attach is two-step + async** (`POST /media` → poll library `success` → `POST /posts` with the cloudfront url; a raw external url never attaches) → the shipped single-URL video path needs a follow-up fix; caption/text posting is live + owner browser test pending. Also designed (NOT built) a final-video upload pipeline: external MP4 → Supabase `reel-videos` bucket → two-step push → auto-delete after Planable ingests → Hetzner fallback near ~80% capacity. Owner also opted OUT of migration-drift/manifest flagging.).*  
*Previously (#56 — **Lean-FootageBrain (WS1–WS4) EXECUTED + SHIPPED LIVE.** Owner reported the prior session's `lean-footagebrain` workflow had finished; this session verified + shipped it. The 4-team workflow (disjoint file ownership + adversarial QA + whole-project build gate) had edited 12 files in place: **WS1** `permissions-catalog.js` `LEAN_HIDDEN` array gates [editor, lossless, export, analytics, inbox, locations, coverage, generate] off editable roles' DEFAULT nav (Resources stays, owner god-mode unaffected, re-enableable per-role/person in admin); **WS2** `app.jsx`/`PreferencesModal.jsx`/`vite.config.js` `React.lazy` + `manualChunks` code-split + owner-only "Prefetch heavy tabs" toggle; **WS3** `store.jsx` gated secondary fetches + 2 realtime channels behind `isOwner`; **WS4** new `perf-tracker.js` (one keepalive web-vitals sample/session, all failures swallowed) + `main.jsx` + owner Monitor perf card (`monitor-hub.jsx`) + `package.json` (`web-vitals ^4.2.4`) + migration `0086_perf_samples` (RLS per 0069 team-read pattern). **Verified before shipping:** `npm run build` green 8.08s — code-split CONFIRMED (old single 1.3 MB `index` → many lazy chunks: `editor`/`inbox`/`locations`/`analytics`/`monitor-hub` 110 kB/`space3d`…); read `perf-tracker.js` to confirm silent-degrade (so safe to ship before 0086 — missing table = dormant telemetry, not errors); per-role Playwright smoke (owner sees Monitor+Analytics, **HTTP 200, ZERO boot page errors**); WS1 gating confirmed STATICALLY from the diff. **Gotcha:** the smoke's reviewer-perspective leg crashed on the **pre-existing `GamifyWelcomePopup` `gf-overlay`** intercepting the perspective-switcher click — NOT a lean regression; static diff verification is the fallback when the popup defeats the harness. Pre-deploy tree check showed exactly the WS1–WS4 footprint (clean/intentional) → committed all 12 files **`2a7cd95`** → `git push origin main` → `vercel --prod` → **`dpl_8iNqY7QVDyVxcqx7NC2rx3zzwWH5`**, www.footagebrain.com, prod **200-verified**. **Pending (human-gated):** apply migration `0086_perf_samples` to prod Supabase (via `/update-migrations` or the SQL editor) — until then the WS4 Monitor perf card has no data. Lessons: (1) a perf-collector that swallows all write failures is safe to ship ahead of its backing table; (2) when a full-screen welcome-popup overlay blocks browser-automation clicks, verify gating via the static diff instead of fighting the harness; (3) WS1 only moves the DEFAULT nav, so the lean pass is fully reversible in-admin with no code change.)*  
*Previously (#55 — **Scaled the dashboard for ~15 concurrent users — store perf overhaul LIVE + migration 0080 applied.** Owner reported lag/breakage at ~15 simultaneous users (the app was built for a 4-person team) and asked how to monitor traffic. **Diagnosed** via 2 Explore agents → root cause is `src/store/store.jsx`: unbounded `select("*")` boot on ~14 tables behind an all-or-nothing `loaded` gate, 4 realtime channels subscribing to ~20 tables **unfiltered** (every Hetzner-poller write fans a WebSocket message to all clients), and a `useMemo([state])` that re-renders every consumer on any patch. Wrote a Part-3 fix roadmap to the plan file (Parts 1–2 = traffic-monitoring guidance: the in-app Monitor → Infra tab already shows row counts / MAU / Hetzner CPU-disk; Vercel Functions/Usage + Supabase Reports cover the rest). **Generated + ran the `scale-15-users-perf` workflow** (`.claude/workflows/scale-15-users-perf.js`, 2 disjoint-ownership teams — TEAM_STORE=store.jsx, TEAM_DB=migration — each with a senior-architect lead + adversarial QA + a whole-project build gate; the honest decomposition is just 2 teams because nearly all the work was in ONE file). It rewrote `store.jsx` (windowed boot, progressive hydrate, trimmed realtime, narrowed memo — 565 ins/198 del, backward-compatible) and wrote `supabase/migrations/0080_perf_indexes.sql` (5 indexes). Build green. **Committed `5871e05`** (staged ONLY the 2 owned files), **deployed isolated** (`git stash -u` the concurrent Scout/Monitor WIP → `vercel --prod` clean main+perf → `stash pop` to restore) → `dpl_2ktBndNTNB2fpDZZQHbk2zAxRNRs` (LIVE www.footagebrain.com), then **applied ONLY 0080** to the live DB via a throwaway one-off mirroring `migrate.mjs` `applyOne()` (avoided `migrate:apply`, which would have swept the unready 0084). **⚠ Likely prod regression:** the prior session left `monitor.jsx`/`scout.jsx` (Scout quota work) *live but uncommitted* — and an isolated deploy builds from clean `main` (WITHOUT those dirty files), so this deploy probably reverted that Scout work on prod → **verify Monitor → Scout on prod and re-ship if regressed** (the no-clean-ref==live trap; new memory `reference_isolated-deploy-reverts-live-uncommitted`, CLAUDE.md rule #1 amended). The migration manifest still doesn't list 0080 (left untouched to avoid the concurrent session's manifest edits); 0080 IS in `schema_migrations`. Lessons: (1) a single-file perf problem collapses the many-parallel-teams workflow shape to one owning team; (2) `migrate:apply` is all-or-nothing — to apply one authorized file, reuse the `exec_sql` RPC + `schema_migrations` upsert; (3) isolated deploy silently reverts live-but-uncommitted files.)*  
*Previously (#54 — **CapCut editor Phase 1 — in-app visual timeline → real render — FRONTEND LIVE; worker deploy pending.** From the product-health audit the owner picked two threads: apply pending migrations + build the editor's next phase. (1) Applied migration **0084** (`daily_tasks.color`) via `/update-migrations` — dry-run revealed the changelog was stale (0078 already applied, 0080 has no file) so only 0084 was pending; **0 pending after**. (2) Built **Editor Phase 1**: new visual drag `Timeline.jsx` (reorder/edge-trim/cut↔crossfade/zoom/live total) inside a **Timeline⇄OpenCut** toggle; **Render draft** builds a `project_json`, submits to the worker, polls progress, and "Use as export link" feeds the existing review-queue export flow; timeline persists in `edit_sessions.edit_plan.timeline`. (3) Implemented real **ffmpeg Phase 1** in `render.py` `_build_filtergraph` (per-clip trim + concat for cuts + xfade/acrossfade for crossfades + `ffprobe` duration-clamp + `anullsrc` silence for audio-less clips + single-clip fast path). (4) Added `verifyOwner` to the `render-submit`/`render-status` proxies (were secret-only). (5) Fixed a runtime **TDZ crash** the owner hit ("Cannot access '$' before initialization") — a hook dep array read `view` before its `useState`; moved the declaration up. Build green; committed `1fe26e0`; deployed `vercel --prod` `dpl_7KpcQmXgYZAMdynKzz8qa94qppzc` (tree check: only my phase + already-live Scout work rode along). **The render worker (`backend-handoff/render.py`) is NOT deployed** → the Render button 502s until the owner does the Hetzner build (scp-merge + `docker compose build backend && up -d backend`). Lesson: a hook's dep array is evaluated during render, so any `const` it references must be declared earlier or it's a runtime-only TDZ the build never catches.)*  
*Previously (#53 — **Scout Headers error CLOSED across all 3 layers + live OpenRouter quota card shipped.** (1) The recurring `non ISO-8859-1 code point` error is fixed in Node (`asciiHeader()` guard on `x-scout-secret` in `suggest.js`), Python (`ascii_clean()`+`_scrub_settings()` in the Scout `config.py` + token scrub in `base.py:_env()` — covers outbound `Authorization` tokens the prior fix never touched), and UI (`scout.jsx` Refresh now shows the real server error). **Key fact: only header VALUES throw this, never JSON bodies** — that bounds the whole search space. Proved browser-side-clean via Playwright (`scripts/scout-inspect.mjs`: Scout loads 388 cards). The owner's console noise (puter.js socket, CSP `unsafe-eval`, Clarity, Rocket.Chat) was all benign third-party-embed chatter — NO CSP exists in the repo. (2) New **Scout card on Monitor → Infra** answers "how many free pulls/day-mo" with LIVE data: new secret-gated `GET /quota` on `fb-scout` (→ OpenRouter `/api/v1/key`+`/credits`) + owner-gated `suggest.js?action=scout-quota` proxy + `ScoutSection` in `monitor.jsx`; auto-detects tier (Free 50/day vs Credited 1,000/day) from `is_free_tier`, shows credits + DB usage + source caps. **Gotcha:** OpenRouter's `rate_limit` field is deprecated (`requests:-1`) → guard `>0`, fall back to documented 20/min. Verified live `is_free_tier:true`→**50/day (~1,500/mo)**. Deploys: Vercel `dpl_7zwE91UX…` then `dpl_…2f6rijml5`; Hetzner `fb-scout` rebuilt twice (followed rule #7: scp-diff-merge, build context `../../scout-backend`, service `scout-backend`). All LIVE. `editor.jsx` shelved during both deploys, untouched.)*  
*Previously (#52 — **Restored 4 reverted UI fixes, shipped LIVE alongside a concurrent Scout session.** Planned via `/qa-verified-plan` (4 fixes scoped against actual code). (1) Footage search Folders → **type-to-filter combobox**; (2) removed **Multimodal/Caption/Visual** search modes; (3) attached footage: removed **Analyze + Preview**, made the **📁 country chip a link** to all that country's clips (new `footageFolderPath()`); (4) restored the **Tasks & Comms 8-tone color picker** (dot + left-border tint; checkmark + drag intact). **Key QA catch:** the Reel DNA "✕ unreachable" overlay needed NO code — the scroll/pin fix was already in the tree and just stale on live (owner had only checked the live site). Committed ONLY my 7 files to `main` (`5d7a9a7`); deployed from an **isolated detached `git worktree` at HEAD** (`vercel --prod`, cloud build → no node_modules needed) so the concurrent Scout session's dirty WIP (`suggest.js`/`editor.jsx`/`scout.jsx`/`monitor.jsx`) never shipped and its working dir was untouched. Live `dpl_CJ2oktPBqLLDrWbk8mEXfUHzwUjB`. New migration `0084_daily_tasks_color.sql` **written, NOT applied** (coordinate the number with the Scout session). Memory `reference_isolated-deploy-dirty-tree` updated with the concurrent-session worktree variant.)*  
*Previously (#51 — **Scout "non ISO-8859-1" Headers error — root cause confirmed SERVER-SIDE, no browser culprit; fix already live.** Diagnostic pass via `/qa-verified-plan` (3 domain + 1 QA agent). Grepped every browser `fetch(` in `src/`+`api/` — all use ASCII-safe headers only (`Authorization: Bearer ${JWT}` / static `Content-Type`); `editor.jsx` has no fetch; `editor-presence.jsx` is dead code; `VITE_SCOUT_SUPABASE_ANON_KEY` + `.env.local` value lines are all-ASCII. **Key insight:** Node.js 18+ `fetch` (undici) throws the IDENTICAL error string as the browser — so the text does NOT prove a browser origin. The throw was in `suggest.js` setting the non-ASCII `SCOUT_SCRAPE_SECRET` header, caught + returned as a 502 body that the Scout tab rendered (looked browser-side). The live `encodeURIComponent` (`suggest.js:164`) + `unquote` (Scout backend `main.py:44`) pair already fixes it. No new code. New memory `reference_undici-iso8859-header-error`. CapCut editor Phase 0 committed (`feat(editor)`+render `/api` prefix fix), `editor.jsx` still in-progress.)*  
*Previously (#50 — **MicroSaaS Scout integrated as FootageBrain Monitor "Scout" tab.** `scout-supabase.js` (2nd Supabase client) + `scout.jsx`/`scout.css` (97 products/dossiers, filter/expand UI) + `monitor-hub.jsx` Scout SUBVIEWS entry + `permissions-catalog.js` scout VIEW_CAP (owner-only) + `suggest.js?action=scout-scrape` (owner-gated server proxy to Hetzner Scout backend). `fb-scout` Docker service on Hetzner port 8787, Caddyfile `/scout/*` handle block, daily 06:00 UTC cron, SCRAPE_SECRET auth + single-flight guard. Scout Supabase RLS migration 0004 applied. Committed `79ab93e`, deployed `dpl_GBFPmtjq7afctFBUqE2bGTifXzt3`. changedetection.io bridge still approved-not-built.)*  
*Previously (#47 — **/space 3D enhancements SHIPPED LIVE** (`dpl_3aHZDUcabmF6ug6Av2jm19zFJbbW`, www.footagebrain.com, `main` `390edfa`). Built the owner `/space` upgrade in an **isolated git worktree** (`feat/space-enhance` off `clean/prod-baseline-2026-06-19`) so the parallel Reel-DNA/Monitor tree was never touched: floating **astronaut** (visor loads `public/astronaut-face.jpg`), subtle **2D gravitational-lens** forming on the cube→Sun axis that **warps the camera over the Sun**, **gaseous multi-hue nebula** (no dots), cube expand-sooner + orbit/pan unfrozen, 360° body spread, **Scene-Studio accordion** w/ Lens+Astronaut tunable bodies + click-a-3D-body-to-open. Coordinated a clean multi-session ship: waited for the parallel Monitor deploy to land `main` == live → **rebased onto `main` CONFLICT-FREE** (because /space kept all CSS in `space3d.css`, never `styles.css`) → build-gated (908 modules) → owner-verified → **copied `.vercel/` from the main dir into the worktree** (a worktree has no `.vercel` link) → `vercel --prod` → verified live by grepping the deployed `space3d-CfhIZE8M.js` chunk (CLI deploys carry no git metadata) → ff-merged `main` + pushed. Pending: drop `public/astronaut-face.jpg` + redeploy for the visor face. Memories [[project_space-ship-plan]] · [[feedback_space-enhance-guardrails]])*  
*Previously (#46 — **MicroSaaS Scout — NEW standalone product built + live, via `/workflow-file-creation`**. Built a brand-new separate product (own repo `C:/Users/Mi/Downloads/microsaas-scout`, own Supabase `rqkz…`) that scrapes Product Hunt + Hacker News + GitHub → free-OpenRouter AI "opportunity dossier" per product → React power-filter UI + pipeline kanban. Generated `.claude/workflows/microsaas-scout-build.js` (4 disjoint-ownership teams, Opus leads/QA) and ran it autonomously: schema **auto-applied via the Supabase Management-API PAT** (service_role can't do DDL — the driverless reliable path on Py3.14), then live Phase-1 ingest. **Live: 97 products + 97 dossiers** (HN 40, GitHub 37, PH 20). Then fixed **3 dedup bugs + dossier dedup** (PH 1→20, GitHub 16→37, dossiers 75-for-38 → 1:1 via migration 0003 `unique(product_id,tier)`): a domain dedup key must be the product's REAL external domain, never the aggregator's (PH `/r/` redirect + GitHub `github.com` fallback collapsed distinct products; `dedup.py` re-derived domain from URL via `or` and mutated rows). Phase 2/3 sources coded but `enabled=false`; Anthropic deep-dive coded but off. Repo **uncommitted**; NOT integrated into FootageBrain or deployed. Next: PLAN the Scout→FootageBrain Monitor "Scout" tab + Hetzner deploy. New memory `project_microsaas-scout`)*  
*Previously (#45 — **Reel DNA Phase 1 — ACTIVATED & LIVE end-to-end**. Executed the activation runbook `.claude/plans/reel-dna-phase1-activation-pyscene.md` STEP 0→9 with a verify-and-pause gate each step. **STEP 0.5** froze the queue (reset 1 stray `pending_analyze` short row `66e8aaff` to idle + paused the `*/2` cron). **STEP 1** live-file inspection corrected 3 plan assumptions: the edge is a **TWO-LAYER proxy Caddy→frontend-nginx→backend** (added a `/reels/*` handle to the bind-mounted Caddyfile → `backend:8000`, reload-only); the LIVE `_claim_one` was **long-only** (`format=eq.long`) — why the stray short row never drained — merge generalizes it; **B1** real → added `REELS_DIR=/app/data/reels` + a `./data/reels` volume. **STEP 2** merged worker = clean replace (LIVE had no hotfix; CR-stripped, py_compile OK). **STEP 5** rebuild verified cv2 4.13 + scenedetect 0.7 import, sha host==container, ffmpeg/yt-dlp survived, `/status` `download_signing_set:true`. **STEP 6** 0081 columns present — **B2 was a STALE PostgREST cache, not a ledger lie** (`NOTIFY pgrst` fixed it). **STEP 7** HMAC parity Python==JS==`34010c33…`. **STEP 8** owner ran `vercel --prod` (blocked-for-me by deny rule `Bash(npx vercel:*)`); secret byte-parity 3 ways. **STEP 9** re-enabled cron + **calibrated all 4 reel types** on a real YT reel (restored to idle after): type-1 auto-acquire→`acquire_failed`→upload CTA (YouTube now needs JS-runtime+cookies, same as IG → manual upload is the real backbone); type-2 manual base.mp4 upload→full pipeline (3 shots, audio, keyframes, exact H3/H4 contracts) → **signed-download streamed HTTP 200** (`Via: 1.1 Caddy`, `Server: uvicorn`, 148901 bytes); type-3 silent→`audio:null`; type-4 single-shot→`shot_count:1, cuts_per_sec:0` guarded. **LIVE**; branch `feat/reel-dna-phase1` proven but uncommitted (clean ff to main). Updated `current-state` + CLAUDE.md rule 7 (Caddy topology). Next: tidy grouped commit → ff-merge to main (owner-gated, no push))*  
*Previously (#44 — **Reel DNA Phase 1 ACTIVATION — QA-PLANNED + STEP-0-CLEARED, execution next session (human-gated)**. Ran a dedicated `/qa-verified-plan` (4 domain agents — Infra/Backend/DB/Security — + 1 adversarial QA, every claim confirmed against source) over the activation of the already-built short-reel backend → runbook `.claude/plans/reel-dna-phase1-activation-pyscene.md` hardening `DEPLOY-PHASE1.md`. **QA refuted** the backend agent's worst fear (it thought `_claim_one` filtered `format=eq.long` and would starve reels — source `reel_deconstruct.py:211` proves it's format-agnostic). **3 BLOCKING gaps the deploy doc missed, now read-gates:** B1 — `DATA_DIR` defaults to `/tmp` + reels are media-RETAINED, so a non-volume path means every `up -d` rebuild wipes assets → downloads 404; B2 — `migrate.mjs` trusts the ledger (lied before, 0049) → mandatory pre-apply LIVE-catalog audit of 0079; B3 — live `*/2` cron + now-reachable Analyze can claim a row mid-activation → freeze the queue (0 `pending_analyze` + pause cron). Proxy target resolved to `backend:8000` (doc's `127.0.0.1:8000` is wrong — port unpublished). **Applied the reachability fix** (`unified-dna-card.jsx` — removed the `{isLong && …}` wrapper on Analyze; only `title` format-aware; Story-panel gate intact; build-green, uncommitted). **Settled cut-detection tech:** PySceneDetect now, **TransNetV2 = Phase-5 fast-follow** behind the `_detect_scenes:764` seam (~20–40% better F1 on reel-type content; box has 5.2GB RAM / 37GB headroom). Owner picked all 3 recommended: 4-reel calibration · manual-upload-first (no cookies) · keep auth-non-demo download gate (doc-wording fix only). **STEP 0 hygiene gate green** (fn 12, build OK, source contaminant-clean — the `CONTAMINATED` print was a false self-match on the doc that quotes the grep). **Nothing deployed/committed/migrated**; STEPS 0.5→9 human-gated next session)*  
*Previously (#43 — **Reel DNA WAVE 1 SHIPPED LIVE**. Ran `/qa-verified-plan` over the 5 Phase-1 complications (3 Explore + 1 adversarial QA agent; every finding re-verified by direct `git diff`/file read — which caught a false "contamination" alarm: the grid-view `compact` prop is the separate already-live grid thread, not Phase-1). **Decisive QA catch:** the short-Analyze reachability fix is a **Phase-1 change, not a Wave-1 change** — a UI trigger must not ship before its backend (else a short reel gets mis-claimed by the live Phase-0 longform worker or hangs in `pending_analyze`) → owner chose a **two-deploy** structure. Cleared all 5 (reachability deferred; Phase-1 vs grid-view file sets disjoint + grid-view already live → full-tree `vercel --prod` safe; Wave 2 FE read-safe pre-0081; backend MERGE documented; HMAC byte-identical, vector `34010c33…`). `git reset` → staged ONLY the 15 Phase-1 files → verified zero grid-view/contaminant leakage in the staged diff + `isLong` gate retained → committed **`8b5eeb4`** → build green (829 mods) → owner visual-verified longform Story → `vercel --prod` → **LIVE `dpl_5VkNyLPG3DhpSaxb9pHj7Ryytb3c`, www.footagebrain.com**. Wave 2 reel FE rode along inert/read-safe. OpenRouter clarified: the free model (`gemini-2.0-flash-exp:free`) runs only on the longform narrative pass (short-reel Phase 1 = ffmpeg+PySceneDetect+pacing, NO LLM). Updated memories `current-state` + `feedback_contract-qa-misses-reachability`. Phase 1 activation pending + human-gated; branch unpushed)*  
*Previously (#42 — **Reel DNA Phase 1 — BUILT + LANDED on `feat/reel-dna-phase1`, build-green, UNDEPLOYED**. Generated `.claude/workflows/reel-dna-phase1.js` via `/workflow-file-creation` then **launched it** — ~25 agents, ~44 min, **zero fix rounds** both waves, onto a new branch off `clean/prod-baseline-2026-06-19`. **Wave 1** carried the parked Phase 0 longform "Story" FE into the clean baseline (5 whole files `reel-story-panel.jsx`+`.css`/`reel-narrative.jsx`/`reel_deconstruct.py`/`REEL-DECONSTRUCT-DEPLOY.md` + 3 store mappers + `?action=deconstruct` + Story panel; **leakage gate 0** — no `group_id`/`contentTypeFromPlatform`). **Wave 2** built Phase 1 Reel MVP: migration `0081` (`asset_manifest`+`pacing` jsonb), HMAC `?action=sign-download` folded into `suggest.js` (**fn count still 12/12**), `reel_deconstruct.py` reel branch (manual-upload-first → yt-dlp → ffmpeg audio → PySceneDetect → keyframes → pacing math) + serving HMAC validator + `DEPLOY-PHASE1.md`, and `reel-assets-auto.jsx`+`pacing-sparkline.jsx`+card Assets section. **Independently verified:** `npm run build` green (829 mods), leakage 0, fn 12, `node --check` OK, HMAC parity + three-mapper round-trip. **Visual-verified Wave 1** (owner) — Short/Long toggle + Analyze + Story panel render for `format='long'`. **REACHABILITY GAP found in the visual pass:** the Analyze button is `{isLong && …}`-gated at `unified-dna-card.jsx:259` and there is NO short-reel trigger → **Phase 1 unreachable from the UI** (contract-QA passed everything; cross-cutting "can a user start the flow" slipped through → new memory `feedback_contract-qa-misses-reachability`). **Owner's order: fix complications FIRST (short-reel trigger + full-tree dirty-tree handling) → ship Wave 1 → activate Phase 1.** NOTHING committed/pushed/deployed/migrated. Updated memories `current-state` + `project_reel-dna-phase1-plan`)*  
*Previously (#41 — **Reel DNA Phase 1 — PLAN + pre-flight diagnostics, NO code/deploy/migration**. Continued the flagship "Reel DNA" reverse-engineering engine. **Discovery:** Phase 0 (longform Story deconstruction) **backend is LIVE** (Hetzner worker + `0079` applied) but its **frontend is parked** on `wip/parking-2026-06-19`, never merged into prod `clean/prod-baseline-2026-06-19` → the live deconstruct backend **has no UI**. **Owner locked:** (1) carry Phase 0 FE forward first; (2) then build **Phase 1 Reel MVP** (reel → downloadable base video/audio/keyframe layers + cut-pacing sparkline). Wrote the full plan (`plans/i-want-to-continue-rippling-peacock.md`): Wave 1 carry-forward (surgical-additive, exclude the `group_id` grouping + `contentTypeFromPlatform` threads) + Wave 2 four disjoint teams (DB+Store/API/Worker/UI). **Pre-flight diagnostics GREEN, no repair:** three-mapper trap confirmed (store.jsx 138/158/1249), 12/12 Vercel fn cap (sign-download MUST fold into suggest.js), `classifyCaller` ready @ `_auth.js:78`, parked new files import clean + zero stale refs, dirty tree has NO overlap with Phase-1 files, migration `0081`. Next: `/workflow-file-creation` with the ready prompt → `reel-dna-phase1.js`. New memory `project_reel-dna-phase1-plan`. **Prior #40 — IG sync "new DM'd reel not showing" — DIAGNOSIS, no code/deploy/migration**. Owner DM'd a fresh YouTube + Facebook reel, hit sync, saw "seen 374, 0 new + 374 already captured, 34 issue(s) logged", reels not showing. Ran as senior-architect + 3 subagents (live DB evidence + adversarial H1/H2). **Nothing lost** — forced a real poll, `reel_dna` ig_dm **395 → 398** (+3), owner confirmed the reels appeared. **Root cause = buttons + cadence:** `🔎 Check IG Sync` → `reloadIgSync()` is a **Supabase SELECT only — it never triggers a poll**; it re-shows the *last cron run* (pre-DM) → "0 new". Only `↻ Refresh` → `triggerIgSync()` polls, and that's fire-and-forget with reel_dna re-reads at only 7s/16s vs a ~5-min sweep → fresh reels land via realtime/cron, not instantly. **H2 (Type-filter hiding) refuted** (default "all", YT/FB classify `content_type='video'`). **Heavy-thread 500 drop intermittent, NOT eliminated** — 34 `graph_error` on 2 threads across 30 runs, 0 added this run. Clarified the two different "500s" (per-thread msg cap vs payload error) + that neither IG DMs nor the spreadsheet has a row ceiling. Hardening (per-thread fallback fetch + token/sync health alerting) **offered + deferred** to a future `/workflow-file-creation`. Updated memory `project_ig-sync-graph-error-drop`)*  
*Previously (#39 — **Pipeline 2×2/3×3 card-view toggle BUILT + DEPLOYED** (`dpl_AsWJvdpdC1hdPBURJee3AAG9YFDL`, www.footagebrain.com). Toolbar toggle ≡ List / ⊞ 2×2 / ⊟ 3×3 (localStorage); grid modes = compact fixed-size tiles, `ReelCard` `compact` prop hides id/series/due-date/menu/note/links/foot/status-pill. Real defect — **unequal card WIDTHS** — fixed via `/qa-verified-plan`: bare CSS `1fr` = `minmax(auto,1fr)` floored by a `white-space:nowrap` status pill → blew out the column. Fix = `minmax(0,1fr)` on board template **and** sub-grids + `min-width:0` + removing the pill. QA's catch: the board template is an **INLINE style at `pipeline.jsx:388`** shadowing the stylesheet. Files `pipeline.jsx`/`components.jsx`/`styles.css`. New memories `project_pipeline-grid-card-view` + `reference_css-grid-1fr-mincontent-blowout`. LIVE but 3 files uncommitted; connecting-lines NOT in this branch → deferred)*  
*Previously (#38 — **IG sync was dropping ~80% of DM'd reels — fixed & DEPLOYED to Hetzner**. `reel_dna` ig_dm **74 → 393** (+319 recovered), sweeps now `reconciled=true`. Started a `/qa-verified-plan` on the assumed null-link cause but **ran the live data first** (`scripts/ig-sync-diagnose.mjs`) which refuted it — `skipped_no_link=0`, all 34 issues were `graph_error` (messages-edge HTTP 500 "reduce the amount of data" / subcode-99). Real cause: the poller read only the latest **50 msgs/thread** and **dropped the whole conversation on a 500**; the missing reels were deeper in the same 3 threads. Fix (`backend-handoff/ig_webhook.py`): slim query to `shares{link}` (attachments/story debug-only — they were the heavy payload), `_fetch_messages()` full pagination + adaptive page-shrink (25→…→5), `_graph_request()` retry on transient 5xx/subcode-99, single-flight `_SYNC_RUNNING` guard, `graph_errors` in reconciliation, don't count the non-IG Page's expected 400 code-100. **Key gotchas:** the live Hetzner file was AHEAD of the `backend-handoff/` snapshot (had the 0077 content_type feature) → had to `diff --strip-trailing-cr` + **MERGE**, never overwrite; code is **baked into the image** so `docker compose build`+recreate (verified in-container sha each deploy); nginx 60s timeout 504s `?wait=1` → fire-and-forget + poll. New memory `project_ig-sync-graph-error-drop`. Plan `.claude/plans/fix-ig-sync-missing-urls.md` carries a SUPERSEDED banner. LIVE but `ig_webhook.py` + `scripts/ig-sync-diagnose.mjs` uncommitted)*  
*Previously (#37 — **Dirty-tree cleanup → clean prod-baseline branch (QA-verified), LOCAL/no-deploy**. Resolved the 67-file multi-thread dirty tree with **zero loss** via `/qa-verified-plan` (3 read-only verifiers + cross-check). Parked everything to `wip/parking-2026-06-19`; cut `clean/prod-baseline-2026-06-19` off `main` = `main` + the 6 live-but-uncommitted files (inbox-AI cherry-pick `75c44c6` + reel-share picker `team-chat.jsx` + DNA-overlay scroll `reel-dna-view.css`) + applied-live migs 0077/0079 + regen manifest; `npm run build` green (822 modules). QA caught 2 pre-mutation errors: live-only set was **6 files not 2** (inbox-AI on side branch would've been un-shipped) + the **junction-delete hazard** on the vA/vB comparison worktrees (could've wiped the real `node_modules` — fix: `cmd //c rmdir` junction first, verify target, then `--force`). Removed all extra worktrees (`ziflow-cleanup` + vA/vB). **Insight: the tree was never broken — World Monitor/bug-batch/IG-reconcile already committed in `main`, so resurrecting a parked feature is a cherry-pick not a rebuild.** Dirty-tree genesis traced: first build collision 06-18 ~22:00 (Reel DNA bug-batch workflow clobbering shared `dist/`); first cross-session fork 06-19 ~04:02. New memory `reference_clean-baseline-reconciliation`. Human-gated next: `/update-migrations` reconcile, ff `main`→baseline + push, bring parked WIP forward per-feature. The `:8000` dev server is still running)*  
*Previously (#36 — **Reel-share picker auto-defaults to the OPEN Rocket.Chat channel, LIVE via isolated deploy**. The Team-chat "Share a reel into chat" dropdown now follows whichever RC channel you have open in the embedded iframe (live, with a `userOverride` escape hatch), instead of always `#pipeline`. Chat is a cross-origin iframe → RC iframe-integration "send" API: `Iframe_Integration_send_enable` ON (owner enabled this session) → `room-opened` postMessage → origin-gated `message` listener in `TeamChat` (channels/groups only) → `ReelSharePicker` auto-follow. Single file `src/pages/team-chat.jsx`; built via `/qa-verified-plan` (plan `.claude/plans/moonlit-wiggling-leaf.md`). Deployed isolated off branch HEAD `reorg/tabs-monitor-roles` + only that file → `dpl_HZKJvncgi2Uj6e9anyiBuSC86GW5` (www.footagebrain.com); **the committed nav-reorg rode along live**, none of the ~58 dirty WIP files shipped. A parallel session kept mutating the tree (file reverted twice). New memory `reference_rc-iframe-room-opened`. LIVE but uncommitted)*  
*Previously (#35 — **`/space` 3D scene: hyper-real overhaul + Scene Studio, local-only**. Three passes on the owner `/space` R3F scene: (1) looping binary-black-hole merger + pulsar + battle fleet + 360° ambient + real bloom (`@react-three/postprocessing@2.19.1`/`postprocessing@6.37.0` — R3F-v8 pin) + opaque skydome (fixes alpha-canvas bloom artifacts) + quality tiers; (2) procedural shaders/textures — `celestial-shared.js` GLSL_NOISE/fresnel/plasma/accretion-disk/nebula + textured planets/hull; sun granulation+sunspots+prominences, gaseous neutron-star beams, doppler BH disk, volumetric nebula; (3) Scene Studio slide-in sidebar (per-body speed/lighting/az·el·dist/colour/size/sound sliders) + per-body procedural Web Audio. Build green, Playwright survey + studio screenshots, zero page errors. **NOT deployed/committed** (shared dirty tree — ship via isolated branch). Next: fix cube interaction (scroll lag, late column expand, freeze-on-enter, no in-column movement/drag), realism/flow polish, gate heavy shaders into QUALITY tiers. New memory `project_space-hyperreal-studio`)*  
*Previously (#34 — **Reel DNA Phase 0 "Longform Story" deconstruction engine BUILT (gate-green), NOT deployed**. The flagship reverse-engineering engine's lowest-risk slice (from the `obsidian-vault/REEL DNA/` spec; plan `.claude/plans/luminous-frolicking-pumpkin.md`): flag `format='long'` → **Analyze** → thin `?action=deconstruct` Vercel proxy (ig-sync clone) → new Hetzner worker `reel_deconstruct.py` (atomic claim, yt-dlp audio + native captions, whisper fallback, ONE free-LLM `run_narrative(model)` seam) → `narrative` jsonb → **Story panel** (hook/arc/open-loops/emotion-sparkline/retention-flags/scorecard) live via realtime. Built via `/workflow`: Wave 1 T-DB(0079)∥T-API∥T-STORE∥T-WORKER → Wave 2 T-UI, locked file ownership + adversarial QA each. Integration build green + per-role smoke PASS; short-format zero-regression. **Human-gated remaining:** apply 0079, set `REEL_DECONSTRUCT_SECRET`, deploy Hetzner worker per `REEL-DECONSTRUCT-DEPLOY.md`, `vercel --prod`. Touched ONLY the 6 owned files on a shared multi-thread WIP tree — do NOT full-tree deploy. New memory `feedback_shared-tree-keep-building`. Free-now-Claude-later)*  
*Previously (#33 — **UI restructure EXECUTED + DEPLOYED** (LIVE www.footagebrain.com, `dpl_BHgunVWZsBTAMhdZzMgh57w2v7tm`, `origin/main` `6fa7204`): Part 0 per-role Playwright smoke gate → A owner **Monitor hub** (Infra/Pulse/AI Brain sub-tabs, new `monitor-hub.jsx` + `canViewView()` helper) → B nav **11→7 groups** → C centralized `useIsOwner()`/`isOwnerRole()`/`ownsReviewQueue()`. Build + per-role smoke green each part; owner approved the layout. Worked a **shared dirty tree mutated live by a parallel session** (inbox-AI + training) — staged only own files, **deferred** `useIsOwner` swaps on inbox/reel-dna/training.jsx. Diagnosed the **0077 migration collision** → already fixed in `stash@{0}` (`0077` content_type + `0078` training quiz, independent — no action). Commits `80b1330`→`8efce38`. **THEN DEPLOYED** from an isolated worktree off `main` (cherry-pick reorg onto training `cfb06a3` → build+smoke green → `vercel --prod`); `main` fast-forwarded to `6fa7204` + pushed. Worked a shared dirty tree mutated live by a parallel session — staged only own files, deferred `useIsOwner` swaps on inbox/reel-dna/training.jsx. Diagnosed the **0077 migration collision** → already fixed in `stash@{0}`. Recipe → memory `reference_shared-dirty-tree-staging`. Next: merge `deploy/ai-inbox`→`main`; apply deferred swaps when parallel work lands)*  
*Previously (#32 — **Inbox AI reply suggestions BUILT + DEPLOYED in isolation**. Picked from the Obsidian roadmap (highest functionality-per-effort); planned via `/qa-verified-plan` (QA fixed 3 issues pre-code). Opt-in **✨ Suggest replies** per unanswered thread → 2-3 on-brand drafts → click seeds the **editable** compose box (human always edits+sends; AI never auto-sends); folded into `api/ai/monitor.js` `suggest_replies` mode (0 new Vercel fns, free OpenRouter, array-only `extractJson`, `classifyCaller` demo gate, always-200 degrade) + `suggestInboxReplies` client helper + YT/TikTok `replyToThread` pending-verification scaffold. **Deployed in ISOLATION** from a dirty 3-thread tree: `stash -u` → branch from `main` → checkout only the 4 inbox files → build → `vercel --prod` (`dpl_2efqqQ5C…`, aliased www) → switch back + `stash pop` → verified `git diff <dropped-stash>` EMPTY (zero loss). LIVE via `deploy/ai-inbox` (commit `75c44c6`). Nav reorg + training rebuild + migrations 0077/0078 HELD BACK. Recipe → memory `reference_isolated-deploy-dirty-tree`. Next: merge `deploy/ai-inbox`→`main`; deploy reorg/training deliberately (apply 0078 first))*  
*Previously (#31 — **Playwright smoke harness + Reel DNA verified; no DB/deploy/commit**. Installed `@playwright/test` + Chromium (project had NO browser automation) + `scripts/smoke-screenshot.mjs`; auth via one-time `codegen --save-storage=auth.json` (gitignored); opened the authed dashboard `/app`, waited out the "LOADING WORKFLOW…" hydrate, screenshotted **Reel DNA → renders healthy, zero uncaught page errors** (capture form, Reels/Thumbnails sub-tabs, IG Sync Health, ~28-row spreadsheet). Minor finding: the "capture a reel" bookmarklet is `<a href="javascript:…">` at `reel-dna.jsx:1197` → React future-version warning (non-breaking). Lessons: dashboard is `/app` not `/` (public 3D landing); `networkidle` never fires (R3F loop) so wait on a real element; shell gates on an all-or-nothing store hydrate. Uncommitted/undeployed; `main == origin/main == 747c4cd`. SEPARATE open thread (prior reorg-planning session): tabs/monitor/permissions reorg plan drafted-not-approved, awaiting 2 owner decisions (smoke as Part 0? · part-of-restructure?). Carried over: 0076 §2 rewrite, curl-TLS quirk, 0049 parked)*  
*Previously (#30 — **Housekeeping + restructure-readiness audit; no code/DB/deploy**. Pruned all 10 stale `.claude/worktrees/agent-*` worktrees + their 10 `worktree-agent-*` branches (crosses off the "prune worktrees" next-session item); working tree clean, `main == origin/main`. Ran a 4-subagent restructure-readiness audit → prioritized "critical things to note before restructuring" inventory (logged to the Obsidian Session Log + new memory `reference_restructure-readiness`). Key flags: **`refactor/folder-structure` branch is a TRAP** (61 behind main, would delete migrations 0068–0076 + `vercel.json`); **zero tests** so `npm run build` is necessary-but-not-sufficient (can't see runtime regressions); **add a `@/` Vite alias before any file moves**; `store.jsx` is a 3,568-line god-module (don't split the reducer); migration paths are coupled in 3 scripts + the shipped manifest; **Vercel 12/12 fn cap = no headroom**. Highest-leverage missing step: a thin Playwright smoke-test harness. Still open: 0076 §2 rewrite, curl-TLS quirk, prune remaining stale local branches, demo sandbox 0049 parked)*  
*Previously (#29 — **Cleanup batch DEPLOYED + migration 0076 §1/§3/§4 applied; all live + pushed (`238759b`)**. Deployed C1 (`ig_webhook.py`→Hetzner) + C2 (`store.jsx`→Vercel `dpl_2YX69tRfyUmx1S7PKHocPSwged2p`). Applied 0076 §1 (people priv-esc trigger), §3 (attached_footage authenticated-only), §4 (reel_dna owner/self). **🔴 INCIDENT:** 0076's `owner manage people` policy queried `people` FROM a policy ON `people` → infinite recursion → live site down (IG analyze broke) → fixed via SECURITY DEFINER `auth_is_owner()` helper, verified under real authed session. **Drift:** `0049_demo_sandbox` was marked-applied but NEVER ran (audit; isolated to 0049) → un-marked + guarded + DEFERRED. **0076 §2 (owner-only DELETE) DEFERRED** (assumed 0049's schema). Pushed main + branch. Next session: items 2-4 (0076 §2 rewrite, curl-TLS quirk, prune worktrees))*  
*Previously (#28 — **Cleanup batch COMMITTED `d930896`, NOT deployed**. Via `/qa-verified-plan` → `/workflow` (3 disjoint components, adversarial QA each): **C1** cross-platform IG-DM ingest (`ig_webhook.py` — `_detect_platform` from URL host + plain-text URL capture in the no-`shares` branch; YT/FB DM links were silently dropped) + webhook `content_type` parity; **C2** `store.jsx` `persistUpdateReelDna` `contentType`→`content_type` (in-app content_type edits were lost on reload); **C3** new `0076_rls_hardening.sql` backing UI gating with RLS (people priv-esc trigger — an RLS `WITH CHECK` role-guard is a no-op; owner-only DELETE; attached_footage→authenticated; reel_dna→owner-or-captured_by-self). Build green; local `main` FF'd. **BLOCKED:** `migrate:apply` + `git push origin main` both refused by the classifier despite verbal OK → need explicit per-action authorization. Discord→RocketChat alert DEFERRED. Pending human-gated: apply 0076, deploy C1+C2, push main)*  
*Previously (#27 — **IG DM reconciliation/monitoring + non-reel content-type ingest + 🔎 Check IG Sync button, all LIVE**. Built via `/qa-verified-plan` → `/workflow` on branch `ig-ingest-reconcile-contenttype` (committed `e4e7823`+`0b244d9`, pushed; prior Reel DNA bug-batch committed `f24421f` on `main` as the clean baseline + deployed). (A) Reconciliation: `ig_sync_runs` (0073) + `ig_ingest_log` (0074) + IG Sync Health panel (seen vs accounted, mismatch badge, amber "coverage incomplete" caveat) + owner Discord alert `?action=ig-sync-alert`. (C) Poller captures carousels/photos/stories (composite `external_ref={mid}:{i}`, `_classify_content_type`), `content_type` extended 0075 += story/video, Type column/filter. (B) `docs/ig-crosspost-feasibility.md` writeup. Migrations APPLIED; frontend DEPLOYED; **Hetzner poller deployed + verified via scp + `docker compose build/up`** — first run reconciled, surfaced `graph_errors 3` (IG's conversations edge 500s subcode 99 = a concrete cause of "some DMs not recorded"). Lessons: reconcile only proves "captured == seen this run" (IG total unknowable via polling); multi-share dedupe needs composite ext_ref + `known`-check off bare `mid`; **prod-backend SSH writes + live DB migrates are human-gated** and required explicit owner authorization this session. Open: Discord notify broken (owner fixing); branch not merged to `main`)*  
*Previously (#26 — **Reel DNA 7-issue bug-batch BUILT (green) but NOT deployed** [now committed `f24421f` + deployed], via `/qa-verified-plan` → `/workflow-file-creation` → the generated `.claude/workflows/reel-dna-bug-batch.js` (4 teams, locked disjoint file ownership). Fixes: (1) column-filter focus loss — `TextCell`/`SelectCell` hoisted out of `ColumnFilterRow` (were inline → remount per keystroke); (2) asset duplication — `UPSERT_REEL_DNA_ASSET` now dedupes by the composite key `reelDnaId+assetType+assetId`, because the optimistic row used a synthetic composite id while the realtime echo carried the `gen_random_uuid()` DB id and the reducer deduped by `id` only → both rows survived → resolver rendered the asset twice; (3) footage thumbnail (`footageBrainThumbnailUrl`) + Drive link in `reel-assets.jsx`; (4) Thumbnail DNA spreadsheet trimmed (no image/"Playlist"/timestamp, `ThumbPreview` export kept); (5) filter headers persist at 0 results (grid always renders `DnaTable`; empty notice in tbody); (6) Send-to-Pipeline migrates footage→`attached_footage_items` / locations→`linked_reel_ids` / news→`monitor_event_links` + new read-only Thumbnails/News/DNA-notes boxes (`pipeline-dna-assets.jsx`) in `detail.jsx`; (7) gallery "Hide all assets" toggle (`hideAssetsOverride` prop). `npm run build` green (✓ 19.57s); user testing localhost:8007; uncommitted, undeployed. Lessons: optimistic-vs-realtime id key mismatch → dedupe by business key; in generated workflows, parallel `npm run build` clobbers the shared dist/ → centralize the build gate to single-agent integration-QA/re-QA steps)*  
*Previously (#25 — **Training YouTube Embed toggle now PERSISTS + new "Edit the training manual" permission, both LIVE**. (1) Clicking "Embed" on a YouTube link in the manual used to revert on click-away because `YoutubeEmbedLink` held the shown-state in local `useState`; it's now stored per (module, field) as a `"<fieldPath>::embed"` JSON-array sibling row in `training_module_content` (no migration — rides existing owner-write RLS + store + realtime, so it shows for every editor). `linkifyText(text, embedOpts)`/`EditableText` gained optional `embeddedUrls`/`onToggleEmbed`, threaded via a per-field `getEmbedProps()` through `ModuleCard`→`ProseBlock`/`ListBlock` + inline `EditableText`s. (2) New `editManual` action cap in `permissions-catalog.js` (default Off for editor roles = read-only; owner always allowed via `can()`); Training `canEdit = can("editManual")` replaces the hard-coded owner check; toggle auto-renders in the Roles & permissions Actions matrix. `npm run build` green (815 modules); user verified on localhost; `vercel --prod` → `dpl_GGuS8FEC5azMBNf7twphPuGkv2Sm` (816 modules, aliased www.footagebrain.com). Lesson: persist UI sub-state as a suffix-keyed sibling row in an existing key/value content table to skip a migration and inherit its RLS/realtime. Tree uncommitted)*  
*Previously (#24 — **Training-manual linkify + YouTube embed shipped LIVE everywhere**. URLs in **all** training module sections (not just the 3 prose blocks), incl. the **owner's** editable view, now render as clickable links; YouTube URLs get an inline **Embed** toggle → a small **260px right-aligned** player. Extracted `linkifyText`/`YoutubeEmbedLink` into new shared `src/lib/linkify.jsx`; added opt-in `linkify` prop to `EditableText` applied to read-only AND **editable-not-editing** branches (owner is always `canEdit` → a read-only-only treatment was invisible to them — the root cause of the first pass "showing nothing"); link/embed clicks `stopPropagation`; embed wrapper uses `<span display:block>` not `<div>` to stay valid inside span/li. `vercel --prod` green (815 modules, `dpl_3kuHzejE9WtFg3eTceZK2LEpqG6h`). **Lesson: `vercel --prod` builds the WHOLE working tree** — this deploy also pushed the prior built-but-undeployed Reel DNA Comprehensive overhaul live; check `git status` before deploying. Tree uncommitted (16 M + `linkify.jsx`))*  
*Previously (#23 — **Reel DNA reworked into a Comprehensive-ONLY view** (built green, 814 modules, **NOT deployed** — local verification on :8004). Removed the Classic view + Classic⇄Comprehensive toggle + status/source pills + Cards/Spreadsheet sub-toggle + the `unified_cards` flag gate + the legacy `DnaCard` component. Removed the **left facet rail** → one **global search** (both sub-views) + **per-column filter headers** in the Grid. Grid rows gained a **"⤢ Card"** button → rich `UnifiedDnaCard` in a **centered modal** to attach assets; **Gallery now renders `UnifiedDnaCard`** too. Bug batch: dead "Assets →"/count-badge now opens the full-screen Assets page (`onOpenAssets` was **never forwarded** into Comprehensive — the real root cause, since Comprehensive imported the legacy `DnaCard`/`DnaTable` directly and bypassed the page's `unifiedCards ? …` switch); **thumbnails** fall back to stored `thumbnailUrl`; **location pins** → Google-Maps links, **footage names** → Drive links; **`detachAsset`** now snapshots + rolls back on persist failure; `baseList` excludes `deletedAt`. Planned via `/qa-verified-plan` (3 Explore + 1 Plan agent) then built inline. Files: `reel-dna-comprehensive.jsx` (rewrite), `reel-dna.jsx`/`.css`, `reel-assets.jsx`, `store.jsx`, `reel-dna-filters.jsx` (`export searchHaystack`). Lesson: a sub-view importing a renderer directly **bypasses any page-level renderer switch**. Awaiting owner sign-off → `vercel --prod`)*  
*Previously (#22 — shipped + DEPLOYED a **Reel DNA UX batch, built inline** (not `/workflow` — scope too small for the multi-agent token premium): (1) **font fix** — the smeared mono text was Google-Fonts faux-bold; `index.html` loaded only `JetBrains Mono:wght@400;500` while `reel-dna.css` uses `600/700` (added the missing weights + Inter 700). (2) **searchable multi-select attach picker** (`asset-attach-picker.jsx`) — `+` per category attaches many existing assets at once; attaching previously had **no UI** (only detach). (3) **UnifiedDnaCard** (`unified-dna-card.jsx`) behind owner `unified_cards` flag (default OFF; `app_settings`, mirrors `gamify_enabled`; toggle = `FeatureFlagsPanel` in roles-admin): quick-attach pickers + inline create-&-attach new thumbnail (`createThumbnailDnaCapture`) / news (`createMonitorEvent`, async) + Hide-assets + Pull-from-pipeline; renderer switch in `reel-dna.jsx`. (4) **Detach bug fix** — `ReelAssets` detached location/thumbnail with **plural** types that never matched the singular stored `asset_type` → silent no-ops; fixed to singular. No DB migration. `vercel --prod` green (815 modules, `dpl_DoXXLQtF6kz8j8MYPCM7hgGoQFiR`, aliased www). Committed on `main` (9 ahead of origin, unpushed). Lesson: a missing Google-Fonts weight = faux-bold smear (fix the font *request*, not the CSS); `assetType` is **singular** everywhere)*  
*Previously (#21 — shipped + DEPLOYED **daily-use batch 2**: 6 items via `/qa-verified-plan` — pipeline collapse persist+compact (new `user_preferences` table 0070), Discord notifications on `in_progress`/`sendBack` (`suggest.js?action=discord-notify` + Roles Admin config), training clickable-links/YouTube-click-to-embed, Facebook Reels platform (0071), Tasks-notes drag-select fix, IG-carousel `content_type` groundwork (0072). Migrations 0070–0072 applied live (74 applied · 0 pending); `vercel --prod` green (815 modules), aliased www. Committed on `main` (9 ahead of origin, unpushed). Owner must enter Discord webhook URLs before pings fire)*  
*Previously (#20 — SSH-verified the Hetzner `*/15` yt-sync cron (journalctl shows :00/:15/:30/:45; `/var/log/syslog` was stale — use journalctl); fresh-capture confirmed playlist 2→15 videos, `inserted:7`; added Thumbnails shortcut pill to Reel DNA filter bar; deployed `dpl_D1QJk7EPHeQLPNDKP8zfDzcihR73`) — previously (#19) shipped + DEPLOYED the Reel DNA **unified Assets system**: a collapsible Footage/Locations/Thumbnails/News column with count badges on every card + an in-app full-screen Assets page (all expanded, "Pull from pipeline reel" seed) + a spreadsheet count cell. Backed by a polymorphic `reel_dna_assets` join table (migration 0068, FULL unique upsert arbiter, team-wide RLS, realtime) and a team-read SELECT policy on `monitor_events` (0069). Built via `/qa-verified-plan` → `/workflow` across T-SCHEMA/T-STORE/T-UI under locked file ownership (T-UI resumed after a session-limit interruption). Migrations 0068/0069 applied; `reel_dna_assets` verified live; build green (811 modules) → `vercel --prod` (`dpl_G5rkbTJFjcFnWJP9Yu4GpUSY6aJt`, READY, aliased www). Code-review skipped; tree uncommitted on `bugfix-daily-use-batch`) — previously (#18) shipped the YouTube Thumbnails feature in Reel DNA, LIVE on www.footagebrain.com (`dpl_EBcUouQZXy2RH5DWiubQ9erE1i1p`). **(1) Thumbnails tab:** paste a YouTube link → client-derived thumbnail → six manual design genes (color/typography/face/layout/mood/subject); migration `0063_thumbnail_dna.sql`. **(2) Auto-ingest:** a PUBLIC YouTube playlist polled via its RSS/Atom feed → each video auto-catalogs into `thumbnail_dna` deduped on `video_id`; folded into `suggest.js?action=yt-sync` (`parseYouTubePlaylistFeed` in `_rss.js`, `triggerYtSync` in store, ↻ Refresh + 409 guard in UI); migration `0067_thumbnail_dna_video_id_uidx.sql` (FULL unique index). Built via `qa-verified-plan` → `workflow-file-creation` → the `yt-thumbnail-autoingest` workflow. Applied 0063 + 0067 alone (`exec_sql` one-off; World Monitor 0064–0066 left pending in this DB); `npm run build` green → `vercel --prod`; live endpoint verified `{items_seen:2,inserted:0}`. Remaining: confirm the Hetzner `*/15` cron. Code-review skipped; tree uncommitted on `bugfix-daily-use-batch`) — update this file after each deploy or feature addition.*
