REEL DNA — brand assets
=======================

Drop the logo render here:

  public/brand/reel-dna-logo.png
      The "REEL DNA" neon helix + wordmark render (the v2 image).
      Used as the hero logo on the public landing page (/).
      Recommended: transparent or pure-black background PNG,
      ~1440px wide, exported 2x for crispness.

The landing page references it at the web path:  /brand/reel-dna-logo.png

If the file is missing, the hero falls back to the CSS "REEL DNA"
wordmark automatically (no broken image), so the page still renders.
